"""帧序列（frame-sequence-v1 / schema 7）的生产件。

这一层是「写实风」路线的产品侧落点：把原先散在 `scripts/poc_*.py` 里的
抠像 / 打包 / 验收逻辑收进服务，**单一真源**——脚本退化成薄 CLI 包装，
而不是各维护一份。搬家不重写：算法逐行照搬，靠黄金回归比对产物字节。

⚠️ **本文件不许 import 任何东西。** `MIN_FRAMING_MARGIN` 要给
`scripts/一键出宠.py` 读，而那个脚本跑在哪个解释器上不保证有
numpy / scipy —— 这里一旦引入重依赖，脚本会在 import 阶段就炸。
"""

# 首帧取景两个余量（左余量、尾巴甩到最远时的右余量）的下限。
# 任一低于它就不许进视频 —— 长毛猫当初没看这行警告直接进视频，白花 4.67 算力。
# 唯一真源：`scripts/一键出宠.py` 的 `converge_framing` 也读这里。
MIN_FRAMING_MARGIN = 0.05
