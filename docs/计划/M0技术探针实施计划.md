# 桌面宠物 M0 技术探针 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `subagent-driven-development` (recommended) or `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立一个可复现的 Windows 桌宠技术探针，用实测证据验证透明窗口、动态点击区域、拖拽与恢复、陪伴/桌面模式、全屏避让和资源预算。

**Architecture:** 在全新 `desktop-pet/` Git 仓库中创建单应用 Tauri 2 工程。前端使用无 React 的 TypeScript + PixiJS 8，只承担渲染、Alpha 命中区域和探针交互；Rust 负责 Windows 窗口区域、窗口层级、偏好存储和系统检测。M0 使用生成的无隐私测试 PNG，不实现宠物身份、SQLite、云端生成或 P1 行为系统。

**Tech Stack:** Windows 10/11、Node.js 24、npm 11、Rust stable MSVC、Tauri CLI 2.11.4、Tauri crate 2.11.5、PixiJS 8.19.0、Vite 8.2.0、Vitest 4.1.10、TypeScript 7.0.2、windows-sys 0.61.2、PowerShell 5.1+。

## Global Constraints

- Windows 为第一优先平台；所有 Win32 实现放在 `src-tauri/src/platform/windows.rs`，平台无关调用只依赖 `PlatformAdapter`。
- 当前工作区不是 Git 仓库；产品仓库固定创建在 `C:\Users\Administrator\Documents\Codex\2026-08-03\wo\desktop-pet`。
- Markdown 默认使用中文文件名；`README.md`、`AGENTS.md` 等工具固定名除外。
- M0 只导入无隐私风险的透明 PNG 测试素材，不实现真实宠物上传、云端 API、SQLite、声音、Agent 或 P1 状态系统。
- 默认窗口模式是陪伴模式；桌面模式是可降级的实验能力。
- 宠物窗口不得抢焦点；透明区域必须把点击交给后方窗口。
- 采用事件唤醒和运行档位，不把渲染循环永久固定在 60 FPS。
- 性能数值是待验证预算：空闲 CPU P95 `<= 1%`、交互 CPU P95 `<= 3%`、空闲私有内存目标 `<= 120 MB`、超过 `180 MB` 必须分析、冷启动目标 `<= 3 秒`、交互延迟目标 `< 50 ms`。
- TDD：纯逻辑先写失败测试；Windows 系统能力同时提供诊断返回值和人工验证步骤。
- 每个 Task 完成后独立提交；未通过 M0 阶段闸门前不得开始 M1。

---

## 预期文件结构

```text
desktop-pet/
├─ .gitignore
├─ AGENTS.md
├─ README.md
├─ package.json
├─ package-lock.json
├─ tsconfig.json
├─ vite.config.ts
├─ index.html
├─ docs/
│  ├─ 设计/产品与技术设计方案.md
│  ├─ 计划/M0技术探针实施计划.md
│  └─ 验证记录/
│     ├─ M0手工验证清单.md
│     ├─ M0技术结论.md
│     ├─ Windows兼容性矩阵.md
│     └─ 性能基线.md
├─ public/test-assets/pet-probe.png
├─ scripts/
│  ├─ 创建探针宠物素材.ps1
│  ├─ 测量桌宠资源.ps1
│  └─ 执行M0检查.ps1
├─ src/
│  ├─ main.ts
│  ├─ styles.css
│  └─ runtime/
│     ├─ bridge.ts
│     ├─ contracts.ts
│     ├─ fullscreen.ts
│     ├─ fullscreen.test.ts
│     ├─ geometry.ts
│     ├─ geometry.test.ts
│     ├─ hit-mask.ts
│     ├─ hit-mask.test.ts
│     ├─ pet-stage.ts
│     ├─ render-scheduler.ts
│     └─ render-scheduler.test.ts
└─ src-tauri/
   ├─ build.rs
   ├─ Cargo.toml
   ├─ capabilities/default.json
   ├─ icons/icon.ico
   ├─ src/
   │  ├─ lib.rs
   │  ├─ main.rs
   │  ├─ preferences.rs
   │  ├─ windowing.rs
   │  └─ platform/
   │     ├─ mod.rs
   │     └─ windows.rs
   └─ tauri.conf.json
```

`src/runtime` 不依赖 React。设置、校准和创建界面到后续阶段才引入 UI 框架选择；M0 只允许一个按 `F12` 显示的诊断面板。

### Task 1: 建立可编译仓库与开发基线

**Files:**
- Create: `desktop-pet/.gitignore`
- Create: `desktop-pet/AGENTS.md`
- Create: `desktop-pet/README.md`
- Create: `desktop-pet/package.json`
- Create: `desktop-pet/tsconfig.json`
- Create: `desktop-pet/vite.config.ts`
- Create: `desktop-pet/index.html`
- Create: `desktop-pet/src/main.ts`
- Create: `desktop-pet/src/styles.css`
- Create: `desktop-pet/src/runtime/contracts.ts`
- Create: `desktop-pet/src-tauri/Cargo.toml`
- Create: `desktop-pet/src-tauri/build.rs`
- Create: `desktop-pet/src-tauri/tauri.conf.json`
- Create: `desktop-pet/src-tauri/capabilities/default.json`
- Create: `desktop-pet/src-tauri/src/main.rs`
- Create: `desktop-pet/src-tauri/src/lib.rs`
- Copy: `outputs/桌面宠物项目产品与技术设计方案.md` -> `desktop-pet/docs/设计/产品与技术设计方案.md`
- Copy: `outputs/桌面宠物M0技术探针实施计划.md` -> `desktop-pet/docs/计划/M0技术探针实施计划.md`

**Interfaces:**
- Consumes: 已确认设计文档和本计划。
- Produces: `npm run dev`、`npm test`、`npm run typecheck`、`npm run tauri build -- --debug`；前端常量 `PROBE_VERSION: "m0"`；Tauri 应用入口 `desktop_pet_lib::run()`。

- [ ] **Step 1: 安装缺失的 Rust MSVC 工具链并验证前置条件**

Run in PowerShell:

```powershell
winget install --id Rustlang.Rustup -e --source winget
rustup default stable-x86_64-pc-windows-msvc
rustup component add rustfmt clippy
rustc --version
cargo --version
node --version
npm --version
```

Expected: `rustc` 和 `cargo` 均返回 stable MSVC 版本；Node 返回 `v24.16.0` 或兼容的更新版本；npm 返回 `11.13.0` 或兼容的更新版本。若 `rustc` 在当前终端仍不可见，关闭并重开 PowerShell 后只重跑验证命令。

- [ ] **Step 2: 初始化独立仓库和目录，不污染 projectless 工作区根目录**

Run:

```powershell
New-Item -ItemType Directory -Path 'desktop-pet' -ErrorAction Stop
Set-Location 'desktop-pet'
git init -b main
New-Item -ItemType Directory -Force -Path 'src/runtime','src-tauri/src/platform','src-tauri/capabilities','src-tauri/icons','docs/设计','docs/计划','docs/验证记录','public/test-assets','scripts'
```

Expected: `git status --short --branch` 显示 `## No commits yet on main`；产品文件只出现在 `desktop-pet/` 下。

- [ ] **Step 3: 写入 Node/Rust 脚手架和最小失败测试**

Use `apply_patch` to create `package.json`:

```json
{
  "name": "desktop-pet",
  "private": true,
  "version": "0.0.0-m0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "pixi.js": "8.19.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "typescript": "7.0.2",
    "vite": "8.2.0",
    "vitest": "4.1.10"
  }
}
```

Create `src/runtime/contracts.ts`:

```ts
export const PROBE_VERSION = "m0" as const;

export type WindowMode = "companion" | "desktop";
export type RenderTier = "active" | "companion" | "still" | "paused";
```

Create `src/runtime/contracts.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { PROBE_VERSION } from "./contracts";

describe("probe contract", () => {
  it("pins the M0 contract version", () => {
    expect(PROBE_VERSION).toBe("m0");
  });
});
```

Create `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vitest/globals"]
  },
  "include": ["src", "vite.config.ts"]
}
```

Create `vite.config.ts`:

```ts
import { defineConfig } from "vite";

export default defineConfig({
  clearScreen: false,
  server: { port: 1420, strictPort: true },
  envPrefix: ["VITE_", "TAURI_"],
  build: { target: "es2021", minify: "esbuild", sourcemap: true },
});
```

- [ ] **Step 4: 运行测试，确认 TDD 基线先因依赖未安装而失败**

Run:

```powershell
npm test
```

Expected: FAIL，包含无法找到 `vitest` 或未安装依赖的提示。

- [ ] **Step 5: 安装锁定依赖并写入最小 Tauri 应用**

Run:

```powershell
npm install --save-exact
```

Expected: 生成 `package-lock.json`，`npm audit` 不得出现 high/critical 漏洞；若存在，记录包名和修复选择，不运行破坏性 `npm audit fix --force`。

Create `src-tauri/Cargo.toml`:

```toml
[package]
name = "desktop-pet"
version = "0.0.0"
description = "Windows desktop pet M0 probe"
edition = "2021"

[lib]
name = "desktop_pet_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = "=2.6.3"

[dependencies]
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
tauri = { version = "=2.11.5", features = ["tray-icon"] }
thiserror = "=2.0.19"
windows-sys = { version = "=0.61.2", features = [
  "Win32_Foundation",
  "Win32_Graphics_Gdi",
  "Win32_System_Threading",
  "Win32_UI_Shell",
  "Win32_UI_WindowsAndMessaging"
] }
```

Create `src-tauri/build.rs`:

```rust
fn main() {
    tauri_build::build()
}
```

Create `src-tauri/src/main.rs`:

```rust
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    desktop_pet_lib::run();
}
```

Create `src-tauri/src/lib.rs`:

```rust
#[tauri::command]
fn probe_version() -> &'static str {
    "m0"
}

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .invoke_handler(tauri::generate_handler![probe_version])
        .run(tauri::generate_context!())
        .expect("failed to run desktop pet probe");
}

#[cfg(test)]
mod tests {
    #[test]
    fn probe_version_is_m0() {
        assert_eq!(super::probe_version(), "m0");
    }
}
```

Create `src-tauri/tauri.conf.json`:

```json
{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "Desktop Pet M0",
  "version": "0.0.0",
  "identifier": "com.desktop-pet.probe",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "label": "pet",
        "title": "Desktop Pet M0",
        "width": 420,
        "height": 520,
        "transparent": true,
        "decorations": false,
        "shadow": false,
        "alwaysOnTop": true,
        "skipTaskbar": true,
        "focus": false,
        "resizable": true
      }
    ],
    "security": { "csp": "default-src 'self'; img-src 'self' asset: blob: data:; style-src 'self' 'unsafe-inline'" }
  },
  "bundle": { "active": false }
}
```

Create `src-tauri/capabilities/default.json`:

```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "M0 pet window permissions",
  "windows": ["pet"],
  "permissions": ["core:default", "core:window:default"]
}
```

Create `index.html`:

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Desktop Pet M0</title>
  </head>
  <body>
    <main id="app" aria-label="桌面宠物技术探针"></main>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

Create `src/main.ts`:

```ts
import "./styles.css";
import { PROBE_VERSION } from "./runtime/contracts";

const root = document.querySelector<HTMLElement>("#app");
if (!root) throw new Error("missing #app root");
root.dataset.probeVersion = PROBE_VERSION;
```

Create `src/styles.css`:

```css
:root, body, #app {
  width: 100%;
  height: 100%;
  margin: 0;
  overflow: hidden;
  background: transparent;
}

html, body { user-select: none; }
```

- [ ] **Step 6: 写入项目约束和复制已确认文档**

Create `.gitignore`:

```gitignore
node_modules/
dist/
src-tauri/target/
src-tauri/gen/
*.log
```

Create `AGENTS.md`:

```md
# 项目协作约束

- Codex 创建的 Markdown 默认使用中文文件名，工具强制文件名除外。
- Windows 专属代码只放在 `src-tauri/src/platform/windows.rs`。
- M0 不引入 React、SQLite、云端模型、声音或 Agent。
- 修改纯逻辑前先写失败测试；系统能力必须补充人工验证记录。
- 不把尚未实测的窗口、性能或生成能力描述为已完成。
```

Create `README.md`:

```md
# Desktop Pet

Windows 优先的轻量桌面宠物项目。当前阶段为 M0 技术探针，仅验证透明窗口、点击区域、窗口层级、全屏避让和资源基线。

## 开发

```powershell
npm install
npm test
npm run tauri dev
```
```

Copy the approved documents with PowerShell:

```powershell
Copy-Item -LiteralPath '..\outputs\桌面宠物项目产品与技术设计方案.md' -Destination 'docs\设计\产品与技术设计方案.md'
Copy-Item -LiteralPath '..\outputs\桌面宠物M0技术探针实施计划.md' -Destination 'docs\计划\M0技术探针实施计划.md'
```

- [ ] **Step 7: 验证脚手架通过**

Run:

```powershell
npm test
npm run typecheck
cargo test --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
npm run tauri info
```

Expected: 1 个 Vitest 测试通过，1 个 Rust 测试通过，TypeScript 和 Clippy 无错误；`tauri info` 显示 Rust stable MSVC、WebView2 Runtime 和 Visual Studio Build Tools。

- [ ] **Step 8: 提交仓库基线**

```powershell
git add .
git commit -m "chore: bootstrap desktop pet M0 probe"
```

Expected: 工作树干净，提交包含 lockfile、已确认设计和 M0 计划。

### Task 2: 用 Alpha 像素生成稳定的窗口命中区域契约

**Files:**
- Modify: `desktop-pet/src/runtime/contracts.ts`
- Create: `desktop-pet/src/runtime/hit-mask.ts`
- Create: `desktop-pet/src/runtime/hit-mask.test.ts`

**Interfaces:**
- Consumes: 浏览器 `ImageData` 或等价 `Uint8ClampedArray`。
- Produces: `HitRegionPayload { canvasWidth, canvasHeight, spans }`；`alphaToRegionSpans(data, width, height, options): RegionSpan[]`。Task 4 的 Rust 命令使用同名字段反序列化。

- [ ] **Step 1: 写透明、连续像素和降采样合并的失败测试**

Create `src/runtime/hit-mask.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { alphaToRegionSpans } from "./hit-mask";

function rgba(alphas: number[]): Uint8ClampedArray {
  return Uint8ClampedArray.from(alphas.flatMap((alpha) => [0, 0, 0, alpha]));
}

describe("alphaToRegionSpans", () => {
  it("returns no spans for a transparent image", () => {
    expect(alphaToRegionSpans(rgba([0, 0, 0, 0]), 2, 2)).toEqual([]);
  });

  it("merges adjacent opaque pixels into row spans", () => {
    const spans = alphaToRegionSpans(rgba([
      0, 255, 255, 0,
      255, 255, 0, 0,
    ]), 4, 2, { alphaThreshold: 32, rowStep: 1 });

    expect(spans).toEqual([
      { left: 1, top: 0, right: 3, bottom: 1 },
      { left: 0, top: 1, right: 2, bottom: 2 },
    ]);
  });

  it("expands sampled rows to cover the skipped rows", () => {
    const spans = alphaToRegionSpans(rgba([
      255, 0,
      0, 0,
      255, 255,
      0, 0,
    ]), 2, 4, { rowStep: 2, alphaThreshold: 32 });

    expect(spans).toEqual([
      { left: 0, top: 0, right: 1, bottom: 2 },
      { left: 0, top: 2, right: 2, bottom: 4 },
    ]);
  });
});
```

- [ ] **Step 2: 运行单测确认失败**

Run:

```powershell
npm test -- src/runtime/hit-mask.test.ts
```

Expected: FAIL，提示无法解析 `./hit-mask`。

- [ ] **Step 3: 定义精确契约并实现最小 Alpha 扫描算法**

Append to `src/runtime/contracts.ts`:

```ts
export interface RegionSpan {
  left: number;
  top: number;
  right: number;
  bottom: number;
}

export interface HitRegionPayload {
  canvasWidth: number;
  canvasHeight: number;
  scaleFactor: number;
  spans: RegionSpan[];
}
```

Create `src/runtime/hit-mask.ts`:

```ts
import type { RegionSpan } from "./contracts";

export interface HitMaskOptions {
  alphaThreshold?: number;
  rowStep?: number;
}

export function alphaToRegionSpans(
  data: Uint8ClampedArray,
  width: number,
  height: number,
  options: HitMaskOptions = {},
): RegionSpan[] {
  if (width <= 0 || height <= 0 || data.length !== width * height * 4) {
    throw new RangeError("RGBA data does not match width and height");
  }

  const threshold = options.alphaThreshold ?? 32;
  const rowStep = Math.max(1, Math.floor(options.rowStep ?? 2));
  const spans: RegionSpan[] = [];

  for (let top = 0; top < height; top += rowStep) {
    const bottom = Math.min(height, top + rowStep);
    let runStart: number | null = null;

    for (let x = 0; x <= width; x += 1) {
      const alpha = x < width ? data[(top * width + x) * 4 + 3] ?? 0 : 0;
      const opaque = alpha >= threshold;

      if (opaque && runStart === null) runStart = x;
      if (!opaque && runStart !== null) {
        spans.push({ left: runStart, top, right: x, bottom });
        runStart = null;
      }
    }
  }

  return spans;
}
```

- [ ] **Step 4: 验证命中区域测试和类型检查通过**

Run:

```powershell
npm test -- src/runtime/hit-mask.test.ts
npm run typecheck
```

Expected: 3 个测试通过，TypeScript 无错误。

- [ ] **Step 5: 提交 Alpha 命中区域契约**

```powershell
git add src/runtime/contracts.ts src/runtime/hit-mask.ts src/runtime/hit-mask.test.ts
git commit -m "feat: derive hit regions from pet alpha mask"
```

### Task 3: 渲染透明测试宠物并将命中区域发送给 Rust

**Files:**
- Create: `desktop-pet/scripts/创建探针宠物素材.ps1`
- Create: `desktop-pet/public/test-assets/pet-probe.png` (generated)
- Create: `desktop-pet/src/runtime/geometry.ts`
- Create: `desktop-pet/src/runtime/geometry.test.ts`
- Create: `desktop-pet/src/runtime/bridge.ts`
- Create: `desktop-pet/src/runtime/pet-stage.ts`
- Modify: `desktop-pet/src/main.ts`
- Modify: `desktop-pet/src/styles.css`

**Interfaces:**
- Consumes: `alphaToRegionSpans`、测试 PNG、Tauri 命令 `apply_hit_region`。
- Produces: `computeContainRect(source, viewport)`；`PetStage.mount(root): Promise<void>`；发送 `HitRegionPayload`。

- [ ] **Step 1: 写 contain 布局的失败测试**

Create `src/runtime/geometry.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { computeContainRect } from "./geometry";

describe("computeContainRect", () => {
  it("centers a portrait asset without cropping", () => {
    expect(computeContainRect(
      { width: 200, height: 400 },
      { width: 400, height: 400 },
    )).toEqual({ x: 100, y: 0, width: 200, height: 400, scale: 1 });
  });

  it("scales down oversized assets", () => {
    expect(computeContainRect(
      { width: 800, height: 400 },
      { width: 400, height: 300 },
    )).toEqual({ x: 0, y: 50, width: 400, height: 200, scale: 0.5 });
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
npm test -- src/runtime/geometry.test.ts
```

Expected: FAIL，提示无法解析 `./geometry`。

- [ ] **Step 3: 实现纯布局逻辑**

Create `src/runtime/geometry.ts`:

```ts
export interface Size { width: number; height: number }
export interface LayoutRect extends Size { x: number; y: number; scale: number }

export function computeContainRect(source: Size, viewport: Size): LayoutRect {
  if (source.width <= 0 || source.height <= 0 || viewport.width <= 0 || viewport.height <= 0) {
    throw new RangeError("sizes must be positive");
  }
  const scale = Math.min(viewport.width / source.width, viewport.height / source.height);
  const width = source.width * scale;
  const height = source.height * scale;
  return {
    x: (viewport.width - width) / 2,
    y: (viewport.height - height) / 2,
    width,
    height,
    scale,
  };
}
```

- [ ] **Step 4: 创建可重复生成的透明 PNG 测试素材**

Create `scripts/创建探针宠物素材.ps1`:

```powershell
param([string]$OutputPath = 'public/test-assets/pet-probe.png')

Add-Type -AssemblyName System.Drawing
$resolved = [System.IO.Path]::GetFullPath((Join-Path (Get-Location) $OutputPath))
$directory = [System.IO.Path]::GetDirectoryName($resolved)
[System.IO.Directory]::CreateDirectory($directory) | Out-Null
$bitmap = New-Object System.Drawing.Bitmap 512,512,[System.Drawing.Imaging.PixelFormat]::Format32bppArgb
$graphics = [System.Drawing.Graphics]::FromImage($bitmap)
$graphics.SmoothingMode = [System.Drawing.Drawing2D.SmoothingMode]::AntiAlias
$graphics.Clear([System.Drawing.Color]::Transparent)
$fur = New-Object System.Drawing.SolidBrush ([System.Drawing.Color]::FromArgb(255,72,94,86))
$inner = New-Object System.Drawing.SolidBrush ([System.Drawing.Color]::FromArgb(255,220,174,169))
$eye = New-Object System.Drawing.SolidBrush ([System.Drawing.Color]::FromArgb(255,245,205,74))
$graphics.FillEllipse($fur,106,154,300,324)
$graphics.FillEllipse($fur,126,68,260,236)
$graphics.FillPolygon($fur,[System.Drawing.Point[]]@((New-Object System.Drawing.Point 145,118),(New-Object System.Drawing.Point 168,28),(New-Object System.Drawing.Point 222,94)))
$graphics.FillPolygon($fur,[System.Drawing.Point[]]@((New-Object System.Drawing.Point 290,94),(New-Object System.Drawing.Point 344,28),(New-Object System.Drawing.Point 367,118)))
$graphics.FillEllipse($inner,176,159,55,44)
$graphics.FillEllipse($inner,281,159,55,44)
$graphics.FillEllipse($eye,190,171,22,18)
$graphics.FillEllipse($eye,300,171,22,18)
$bitmap.Save($resolved,[System.Drawing.Imaging.ImageFormat]::Png)
$graphics.Dispose(); $bitmap.Dispose(); $fur.Dispose(); $inner.Dispose(); $eye.Dispose()
Write-Output $resolved
```

Run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/创建探针宠物素材.ps1
```

Expected: `public/test-assets/pet-probe.png` 为 `512 x 512`、带透明背景的测试图，不包含真实用户素材。

- [ ] **Step 5: 实现 Tauri bridge 和 PixiJS 舞台**

Create `src/runtime/bridge.ts`:

```ts
import { invoke } from "@tauri-apps/api/core";
import type { HitRegionPayload } from "./contracts";

export interface HitRegionEvidence {
  spanCount: number;
  applied: boolean;
  strategy: string;
  scaleFactor: number;
}

export function applyHitRegion(payload: HitRegionPayload): Promise<HitRegionEvidence> {
  return invoke("apply_hit_region", { payload });
}
```

Create `src/runtime/pet-stage.ts`:

```ts
import { Application, Assets, Sprite } from "pixi.js";
import { applyHitRegion } from "./bridge";
import { computeContainRect } from "./geometry";
import { alphaToRegionSpans } from "./hit-mask";

export class PetStage {
  private readonly app = new Application();

  async mount(root: HTMLElement): Promise<void> {
    await this.app.init({
      resizeTo: root,
      backgroundAlpha: 0,
      antialias: true,
      autoStart: false,
      preference: "webgl",
    });
    root.replaceChildren(this.app.canvas);

    const texture = await Assets.load("/test-assets/pet-probe.png");
    const sprite = new Sprite(texture);
    const layout = computeContainRect(
      { width: texture.width, height: texture.height },
      { width: root.clientWidth, height: root.clientHeight },
    );
    sprite.position.set(layout.x, layout.y);
    sprite.scale.set(layout.scale);
    this.app.stage.addChild(sprite);
    this.app.render();

    const source = texture.source.resource as HTMLImageElement;
    const canvas = document.createElement("canvas");
    canvas.width = root.clientWidth;
    canvas.height = root.clientHeight;
    const context = canvas.getContext("2d", { willReadFrequently: true });
    if (!context) throw new Error("2D canvas is unavailable for hit-mask extraction");
    context.drawImage(source, layout.x, layout.y, layout.width, layout.height);
    const image = context.getImageData(0, 0, canvas.width, canvas.height);
    const spans = alphaToRegionSpans(image.data, image.width, image.height, {
      alphaThreshold: 32,
      rowStep: 2,
    });
    await applyHitRegion({
      canvasWidth: canvas.width,
      canvasHeight: canvas.height,
      scaleFactor: window.devicePixelRatio,
      spans,
    });
  }
}
```

Replace `src/main.ts`:

```ts
import "./styles.css";
import { PetStage } from "./runtime/pet-stage";

const root = document.querySelector<HTMLElement>("#app");
if (!root) throw new Error("missing #app root");

await new PetStage().mount(root);
```

- [ ] **Step 6: 验证纯逻辑测试通过；Tauri 运行预期因命令尚未注册而报告明确错误**

Run:

```powershell
npm test -- src/runtime/geometry.test.ts src/runtime/hit-mask.test.ts
npm run typecheck
npm run tauri dev
```

Expected: 5 个单测通过，类型检查通过；窗口可启动并显示测试宠物，但开发者控制台出现 `apply_hit_region` 未注册错误。该失败证明 Task 4 的 Rust 边界尚未实现。

- [ ] **Step 7: 提交渲染舞台和测试素材**

```powershell
git add public/test-assets scripts/创建探针宠物素材.ps1 src
git commit -m "feat: render transparent M0 pet fixture"
```

### Task 4: 用 Windows 窗口区域实现透明像素点击穿透

**Files:**
- Create: `desktop-pet/src-tauri/src/platform/mod.rs`
- Create: `desktop-pet/src-tauri/src/platform/windows.rs`
- Create: `desktop-pet/src-tauri/src/windowing.rs`
- Modify: `desktop-pet/src-tauri/src/lib.rs`

**Interfaces:**
- Consumes: 前端 `HitRegionPayload`，字段名严格为 `canvasWidth`、`canvasHeight`、`scaleFactor`、`spans`。`spans` 使用 CSS 像素，Rust 在调用 Win32 前转换为物理像素。
- Produces: Tauri command `apply_hit_region(payload) -> HitRegionEvidence { spanCount, applied, strategy, scaleFactor }`；`PlatformAdapter::apply_hit_region(hwnd, physical_spans)`。Task 6 继续扩展同一 trait。

- [ ] **Step 1: 在 Rust 写 Span 校验和裁切失败测试**

Create `src-tauri/src/windowing.rs`:

```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, Deserialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub struct RegionSpan {
    pub left: i32,
    pub top: i32,
    pub right: i32,
    pub bottom: i32,
}

#[derive(Debug, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct HitRegionPayload {
    pub canvas_width: i32,
    pub canvas_height: i32,
    pub scale_factor: f64,
    pub spans: Vec<RegionSpan>,
}

#[derive(Debug, Serialize)]
#[serde(rename_all = "camelCase")]
pub struct HitRegionEvidence {
    pub span_count: usize,
    pub applied: bool,
    pub strategy: &'static str,
    pub scale_factor: f64,
}

pub fn normalize_spans(payload: &HitRegionPayload) -> Result<Vec<RegionSpan>, &'static str> {
    if payload.canvas_width <= 0 || payload.canvas_height <= 0 || payload.scale_factor <= 0.0 {
        return Err("canvas dimensions must be positive");
    }
    let spans = payload.spans.iter().filter_map(|span| {
        let clipped = RegionSpan {
            left: span.left.clamp(0, payload.canvas_width),
            top: span.top.clamp(0, payload.canvas_height),
            right: span.right.clamp(0, payload.canvas_width),
            bottom: span.bottom.clamp(0, payload.canvas_height),
        };
        (clipped.left < clipped.right && clipped.top < clipped.bottom).then_some(clipped)
    }).collect();
    Ok(spans)
}

pub fn scale_spans(spans: &[RegionSpan], scale_factor: f64) -> Vec<RegionSpan> {
    spans.iter().map(|span| RegionSpan {
        left: (span.left as f64 * scale_factor).floor() as i32,
        top: (span.top as f64 * scale_factor).floor() as i32,
        right: (span.right as f64 * scale_factor).ceil() as i32,
        bottom: (span.bottom as f64 * scale_factor).ceil() as i32,
    }).collect()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn rejects_non_positive_canvas() {
        let payload = HitRegionPayload { canvas_width: 0, canvas_height: 10, scale_factor: 1.0, spans: vec![] };
        assert_eq!(normalize_spans(&payload), Err("canvas dimensions must be positive"));
    }

    #[test]
    fn clips_spans_and_removes_empty_rows() {
        let payload = HitRegionPayload {
            canvas_width: 100,
            canvas_height: 50,
            scale_factor: 1.0,
            spans: vec![
                RegionSpan { left: -5, top: 2, right: 20, bottom: 4 },
                RegionSpan { left: 30, top: 3, right: 30, bottom: 5 },
            ],
        };
        assert_eq!(normalize_spans(&payload).unwrap(), vec![
            RegionSpan { left: 0, top: 2, right: 20, bottom: 4 },
        ]);
    }

    #[test]
    fn scales_css_spans_outward_for_high_dpi_windows() {
        let scaled = scale_spans(
            &[RegionSpan { left: 1, top: 2, right: 3, bottom: 4 }],
            1.5,
        );
        assert_eq!(scaled, vec![RegionSpan { left: 1, top: 3, right: 5, bottom: 6 }]);
    }
}
```

- [ ] **Step 2: 运行 Rust 测试确认模块尚未接入 crate**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml windowing
```

Expected: FAIL 或运行 0 个 `windowing` 测试，因为 `lib.rs` 尚未声明模块。

- [ ] **Step 3: 定义平台接口和 Windows Region 实现**

Create `src-tauri/src/platform/mod.rs`:

```rust
use crate::windowing::RegionSpan;

#[derive(Debug, thiserror::Error)]
pub enum PlatformError {
    #[error("Windows API {operation} failed with code {code}")]
    WindowsApi { operation: &'static str, code: u32 },
    #[error("platform capability unavailable: {0}")]
    Unavailable(&'static str),
}

pub trait PlatformAdapter: Send + Sync {
    fn configure_pet_window(&self, hwnd: isize) -> Result<(), PlatformError>;
    fn apply_hit_region(&self, hwnd: isize, spans: &[RegionSpan]) -> Result<(), PlatformError>;
}

#[cfg(target_os = "windows")]
mod windows;

#[cfg(target_os = "windows")]
pub use windows::WindowsPlatformAdapter;
```

Create `src-tauri/src/platform/windows.rs`:

```rust
use windows_sys::Win32::{
    Foundation::{GetLastError, HWND},
    Graphics::Gdi::{CombineRgn, CreateRectRgn, DeleteObject, RGN_OR},
    UI::WindowsAndMessaging::{
        GetWindowLongPtrW, SetWindowLongPtrW, SetWindowRgn, GWL_EXSTYLE,
        WS_EX_NOACTIVATE, WS_EX_TOOLWINDOW,
    },
};

use crate::{
    platform::{PlatformAdapter, PlatformError},
    windowing::RegionSpan,
};

pub struct WindowsPlatformAdapter;

impl PlatformAdapter for WindowsPlatformAdapter {
    fn configure_pet_window(&self, hwnd: isize) -> Result<(), PlatformError> {
        unsafe {
            let style = GetWindowLongPtrW(hwnd as HWND, GWL_EXSTYLE);
            windows_sys::Win32::Foundation::SetLastError(0);
            let previous = SetWindowLongPtrW(
                hwnd as HWND,
                GWL_EXSTYLE,
                style | WS_EX_NOACTIVATE as isize | WS_EX_TOOLWINDOW as isize,
            );
            if previous == 0 && GetLastError() != 0 {
                return Err(last_error("SetWindowLongPtrW"));
            }
            Ok(())
        }
    }

    fn apply_hit_region(&self, hwnd: isize, spans: &[RegionSpan]) -> Result<(), PlatformError> {
        unsafe {
            let aggregate = CreateRectRgn(0, 0, 0, 0);
            if aggregate.is_null() {
                return Err(last_error("CreateRectRgn"));
            }

            for span in spans {
                let row = CreateRectRgn(span.left, span.top, span.right, span.bottom);
                if row.is_null() {
                    let _ = DeleteObject(aggregate);
                    return Err(last_error("CreateRectRgn"));
                }
                let combined = CombineRgn(aggregate, aggregate, row, RGN_OR);
                let _ = DeleteObject(row);
                if combined == 0 {
                    let _ = DeleteObject(aggregate);
                    return Err(last_error("CombineRgn"));
                }
            }

            if SetWindowRgn(hwnd as HWND, aggregate, 1) == 0 {
                let _ = DeleteObject(aggregate);
                return Err(last_error("SetWindowRgn"));
            }
            Ok(())
        }
    }
}

fn last_error(operation: &'static str) -> PlatformError {
    PlatformError::WindowsApi { operation, code: unsafe { GetLastError() } }
}
```

- [ ] **Step 4: 注册命令并只在初始化后持有平台适配器**

Replace `src-tauri/src/lib.rs`:

```rust
mod platform;
mod windowing;

use std::sync::Arc;
use platform::{PlatformAdapter, WindowsPlatformAdapter};
use tauri::Manager;
use windowing::{normalize_spans, scale_spans, HitRegionEvidence, HitRegionPayload};

struct AppState {
    platform: Arc<dyn PlatformAdapter>,
}

#[tauri::command]
fn probe_version() -> &'static str { "m0" }

#[tauri::command]
fn apply_hit_region(
    window: tauri::WebviewWindow,
    state: tauri::State<'_, AppState>,
    payload: HitRegionPayload,
) -> Result<HitRegionEvidence, String> {
    let spans = normalize_spans(&payload).map_err(str::to_owned)?;
    let physical_spans = scale_spans(&spans, payload.scale_factor);
    let hwnd = window.hwnd().map_err(|error| error.to_string())?.0 as isize;
    state.platform.apply_hit_region(hwnd, &physical_spans).map_err(|error| error.to_string())?;
    Ok(HitRegionEvidence {
        span_count: physical_spans.len(),
        applied: true,
        strategy: "win32-window-region",
        scale_factor: payload.scale_factor,
    })
}

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .manage(AppState { platform: Arc::new(WindowsPlatformAdapter) })
        .invoke_handler(tauri::generate_handler![probe_version, apply_hit_region])
        .setup(|app| {
            let window = app.get_webview_window("pet").ok_or("pet window missing")?;
            let hwnd = window.hwnd()?.0 as isize;
            app.state::<AppState>()
                .platform
                .configure_pet_window(hwnd)
                .map_err(|error| error.to_string())?;
            window.set_focus().ok();
            Ok(())
        })
        .run(tauri::generate_context!())
        .expect("failed to run desktop pet probe");
}

#[cfg(test)]
mod tests {
    #[test]
    fn probe_version_is_m0() { assert_eq!(super::probe_version(), "m0"); }
}
```

The temporary `window.set_focus().ok()` is intentional for the next failing acceptance check.

- [ ] **Step 5: 运行测试和人工检查，确认抢焦点行为不合格**

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml
npm run tauri dev
```

Expected automated result: Rust 测试通过。Expected manual result: 宠物轮廓外区域可点击后方窗口，但应用启动时因 `set_focus()` 抢焦点，因此“不抢焦点”检查 FAIL。

- [ ] **Step 6: 删除抢焦点调用并验证透明区穿透**

Replace the `setup` block in `src-tauri/src/lib.rs` with:

```rust
.setup(|app| {
    let window = app.get_webview_window("pet").ok_or("pet window missing")?;
    let hwnd = window.hwnd()?.0 as isize;
    app.state::<AppState>()
        .platform
        .configure_pet_window(hwnd)
        .map_err(|error| error.to_string())?;
    window.set_always_on_top(true)?;
    Ok(())
})
```

Run:

```powershell
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
npm run tauri dev
```

Expected manual result:

1. 启动应用时当前编辑器仍保持焦点。
2. 点击宠物有色像素时点击由宠物窗口接收。
3. 点击宠物四角透明区域时，后方窗口收到点击。
4. 调整窗口尺寸后若命中区域未更新，记录为 Task 5 必须修复的问题，不把 Task 4 误判为完整通过。

- [ ] **Step 7: 提交 Windows 命中区域探针**

```powershell
git add src-tauri
git commit -m "feat: apply alpha-shaped Windows hit region"
```

### Task 5: 实现拖拽、缩放、翻转和安全位置恢复

**Files:**
- Modify: `desktop-pet/src/runtime/contracts.ts`
- Modify: `desktop-pet/src/runtime/bridge.ts`
- Modify: `desktop-pet/src/runtime/pet-stage.ts`
- Modify: `desktop-pet/src/runtime/geometry.ts`
- Modify: `desktop-pet/src/runtime/geometry.test.ts`
- Create: `desktop-pet/src-tauri/src/preferences.rs`
- Modify: `desktop-pet/src-tauri/src/lib.rs`

**Interfaces:**
- Consumes: Tauri `start_dragging` 和窗口位置/尺寸 API；M0 原子 JSON 偏好。
- Produces: `ProbePreferences { x, y, width, height, scale, flipped, mode }`；命令 `load_preferences`、`save_preferences`、`begin_drag`；`clampRectToWorkArea`。

- [ ] **Step 1: 写屏幕安全恢复的失败测试**

Append to `src/runtime/geometry.test.ts`:

```ts
import { clampRectToWorkArea } from "./geometry";

it("keeps at least 64 pixels visible after a display is removed", () => {
  expect(clampRectToWorkArea(
    { x: 2500, y: 900, width: 420, height: 520 },
    { x: 0, y: 0, width: 1920, height: 1080 },
    64,
  )).toEqual({ x: 1856, y: 900, width: 420, height: 520 });
});
```

- [ ] **Step 2: 运行测试确认函数不存在**

Run:

```powershell
npm test -- src/runtime/geometry.test.ts
```

Expected: FAIL，提示 `clampRectToWorkArea` 未导出。

- [ ] **Step 3: 实现安全位置计算**

Append to `src/runtime/geometry.ts`:

```ts
export interface PositionedRect extends Size { x: number; y: number }

export function clampRectToWorkArea(
  rect: PositionedRect,
  workArea: PositionedRect,
  visiblePixels = 64,
): PositionedRect {
  const visibleX = Math.min(visiblePixels, rect.width);
  const visibleY = Math.min(visiblePixels, rect.height);
  return {
    ...rect,
    x: Math.min(workArea.x + workArea.width - visibleX, Math.max(workArea.x - rect.width + visibleX, rect.x)),
    y: Math.min(workArea.y + workArea.height - visibleY, Math.max(workArea.y - rect.height + visibleY, rect.y)),
  };
}
```

- [ ] **Step 4: 实现原子 JSON 偏好存储**

Create `src-tauri/src/preferences.rs`:

```rust
use serde::{Deserialize, Serialize};
use std::{fs, io, path::Path};

#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
#[serde(rename_all = "camelCase")]
pub struct ProbePreferences {
    pub x: i32,
    pub y: i32,
    pub width: u32,
    pub height: u32,
    pub scale: f64,
    pub flipped: bool,
    pub mode: String,
}

impl Default for ProbePreferences {
    fn default() -> Self {
        Self { x: 1200, y: 500, width: 420, height: 520, scale: 1.0, flipped: false, mode: "companion".into() }
    }
}

pub fn load(path: &Path) -> io::Result<ProbePreferences> {
    if !path.exists() { return Ok(ProbePreferences::default()); }
    serde_json::from_slice(&fs::read(path)?).map_err(io::Error::other)
}

pub fn save(path: &Path, value: &ProbePreferences) -> io::Result<()> {
    if let Some(parent) = path.parent() { fs::create_dir_all(parent)?; }
    let temporary = path.with_extension("json.tmp");
    fs::write(&temporary, serde_json::to_vec_pretty(value).map_err(io::Error::other)?)?;
    fs::rename(temporary, path)
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn round_trips_preferences_atomically() {
        let root = std::env::temp_dir().join(format!("desktop-pet-{}", std::process::id()));
        let path = root.join("preferences.json");
        let value = ProbePreferences { x: 1, y: 2, ..ProbePreferences::default() };
        save(&path, &value).unwrap();
        assert_eq!(load(&path).unwrap(), value);
        let _ = fs::remove_dir_all(root);
    }
}
```

Register `mod preferences;` and add commands in `src-tauri/src/lib.rs`:

```rust
#[tauri::command]
fn load_preferences(app: tauri::AppHandle) -> Result<preferences::ProbePreferences, String> {
    let path = app.path().app_config_dir().map_err(|error| error.to_string())?.join("m0-preferences.json");
    preferences::load(&path).map_err(|error| error.to_string())
}

#[tauri::command]
fn save_preferences(app: tauri::AppHandle, value: preferences::ProbePreferences) -> Result<(), String> {
    let path = app.path().app_config_dir().map_err(|error| error.to_string())?.join("m0-preferences.json");
    preferences::save(&path, &value).map_err(|error| error.to_string())
}

#[tauri::command]
fn begin_drag(window: tauri::WebviewWindow) -> Result<(), String> {
    window.start_dragging().map_err(|error| error.to_string())
}
```

Add these three names to `tauri::generate_handler!`.

- [ ] **Step 5: 接入拖拽、缩放、翻转和窗口 resize 后命中区域重算**

Append to `src/runtime/contracts.ts`:

```ts
export interface ProbePreferences {
  x: number; y: number; width: number; height: number;
  scale: number; flipped: boolean; mode: WindowMode;
}
```

Append to `src/runtime/bridge.ts`:

```ts
import type { ProbePreferences } from "./contracts";

export const loadPreferences = (): Promise<ProbePreferences> => invoke("load_preferences");
export const savePreferences = (value: ProbePreferences): Promise<void> => invoke("save_preferences", { value });
export const beginDrag = (): Promise<void> => invoke("begin_drag");
```

Replace the Task 3 `PetStage` class with the following complete class body; keep the existing imports and add `getCurrentWindow`, `beginDrag`, `loadPreferences`, and `savePreferences`:

```ts
import { PhysicalPosition, PhysicalSize } from "@tauri-apps/api/dpi";
import { availableMonitors, getCurrentWindow, primaryMonitor } from "@tauri-apps/api/window";
import { Application, Assets, Sprite } from "pixi.js";
import { applyHitRegion, beginDrag, loadPreferences, savePreferences } from "./bridge";
import type { ProbePreferences } from "./contracts";
import { clampRectToWorkArea, computeContainRect } from "./geometry";
import { alphaToRegionSpans } from "./hit-mask";

export class PetStage {
  private readonly app = new Application();
  private sprite!: Sprite;
  private source!: CanvasImageSource;
  private root!: HTMLElement;
  private preferences!: ProbePreferences;
  private baseScale = 1;
  private saveTimer: number | undefined;

  async mount(root: HTMLElement): Promise<void> {
    this.root = root;
    this.preferences = await loadPreferences();
    const petWindow = getCurrentWindow();
    await this.restoreWindowPlacement();

    await this.app.init({
      resizeTo: root,
      backgroundAlpha: 0,
      antialias: true,
      autoStart: false,
      preference: "webgl",
    });
    root.replaceChildren(this.app.canvas);
    const texture = await Assets.load("/test-assets/pet-probe.png");
    this.source = texture.source.resource as CanvasImageSource;
    this.sprite = new Sprite(texture);
    this.app.stage.addChild(this.sprite);
    await this.layoutAndApplyRegion();

    this.app.canvas.addEventListener("pointerdown", (event) => {
      if (event.button !== 0) return;
      void beginDrag().then(() => this.captureWindowPlacement());
    });
    this.app.canvas.addEventListener("wheel", (event) => {
      event.preventDefault();
      const delta = event.deltaY < 0 ? 0.1 : -0.1;
      void this.setScale(Math.min(1.5, Math.max(0.5, this.preferences.scale + delta)));
    }, { passive: false });
    this.app.canvas.addEventListener("dblclick", () => {
      void this.setFlipped(!this.preferences.flipped);
    });
    window.addEventListener("resize", () => void this.layoutAndApplyRegion());
  }

  private async layoutAndApplyRegion(): Promise<void> {
    const layout = computeContainRect(
      { width: this.sprite.texture.width, height: this.sprite.texture.height },
      { width: this.root.clientWidth, height: this.root.clientHeight },
    );
    this.baseScale = layout.scale;
    this.sprite.anchor.set(0.5, 0);
    this.sprite.position.set(this.root.clientWidth / 2, layout.y);
    this.sprite.scale.set(
      (this.preferences.flipped ? -1 : 1) * this.baseScale * this.preferences.scale,
      this.baseScale * this.preferences.scale,
    );
    this.app.render();
    await this.updateHitRegion();
  }

  private async updateHitRegion(): Promise<void> {
    const canvas = document.createElement("canvas");
    canvas.width = this.root.clientWidth;
    canvas.height = this.root.clientHeight;
    const context = canvas.getContext("2d", { willReadFrequently: true });
    if (!context) throw new Error("2D canvas is unavailable for hit-mask extraction");
    const width = this.sprite.texture.width * this.baseScale * this.preferences.scale;
    const height = this.sprite.texture.height * this.baseScale * this.preferences.scale;
    const x = (canvas.width - width) / 2;
    const y = this.sprite.y;
    context.save();
    context.translate(this.preferences.flipped ? canvas.width : 0, 0);
    context.scale(this.preferences.flipped ? -1 : 1, 1);
    context.drawImage(this.source, x, y, width, height);
    context.restore();
    const image = context.getImageData(0, 0, canvas.width, canvas.height);
    await applyHitRegion({
      canvasWidth: canvas.width,
      canvasHeight: canvas.height,
      scaleFactor: window.devicePixelRatio,
      spans: alphaToRegionSpans(image.data, image.width, image.height, { alphaThreshold: 32, rowStep: 2 }),
    });
  }

  private async setScale(scale: number): Promise<void> {
    this.preferences.scale = scale;
    await this.layoutAndApplyRegion();
    this.scheduleSave();
  }

  private async setFlipped(flipped: boolean): Promise<void> {
    this.preferences.flipped = flipped;
    await this.layoutAndApplyRegion();
    this.scheduleSave();
  }

  private async captureWindowPlacement(): Promise<void> {
    const petWindow = getCurrentWindow();
    const [position, size] = await Promise.all([petWindow.outerPosition(), petWindow.outerSize()]);
    Object.assign(this.preferences, {
      x: position.x,
      y: position.y,
      width: size.width,
      height: size.height,
    });
    this.scheduleSave();
  }

  private async restoreWindowPlacement(): Promise<void> {
    const petWindow = getCurrentWindow();
    const monitors = await availableMonitors();
    const saved = {
      x: this.preferences.x,
      y: this.preferences.y,
      width: this.preferences.width,
      height: this.preferences.height,
    };
    const intersects = monitors.some((monitor) => {
      const area = { x: monitor.position.x, y: monitor.position.y, width: monitor.size.width, height: monitor.size.height };
      return saved.x < area.x + area.width && saved.x + saved.width > area.x
        && saved.y < area.y + area.height && saved.y + saved.height > area.y;
    });
    let restored = saved;
    if (!intersects) {
      const monitor = await primaryMonitor();
      if (monitor) {
        restored = clampRectToWorkArea(saved, {
          x: monitor.position.x,
          y: monitor.position.y,
          width: monitor.size.width,
          height: monitor.size.height,
        });
      }
    }
    Object.assign(this.preferences, restored);
    await petWindow.setSize(new PhysicalSize(restored.width, restored.height));
    await petWindow.setPosition(new PhysicalPosition(restored.x, restored.y));
  }

  private scheduleSave(): void {
    window.clearTimeout(this.saveTimer);
    this.saveTimer = window.setTimeout(() => void savePreferences(this.preferences), 250);
  }
}
```

Tauri window positions/sizes above are physical pixels；命中遮罩先在 CSS 像素中生成，再由 Rust 使用 `scaleFactor` 转换。Task 9 在每个 DPI 设置下验证这一约定。

- [ ] **Step 6: 验证持久化与显示器变化**

Run:

```powershell
npm test -- src/runtime/geometry.test.ts
cargo test --manifest-path src-tauri/Cargo.toml preferences
npm run tauri dev
```

Expected automated result: geometry 和 preferences 测试通过。Manual checks:

1. 拖拽宠物、在宠物上滚轮缩放、双击翻转后退出并重开，状态恢复。
2. 改变窗口大小后透明区仍穿透。
3. 将宠物放到第二显示器，断开该显示器后重开，至少 64 像素可见。
4. 拖拽过程中不高频写入 `m0-preferences.json`。

- [ ] **Step 7: 提交交互与恢复能力**

```powershell
git add src src-tauri
git commit -m "feat: persist M0 pet placement and transforms"
```

### Task 6: 建立陪伴模式、实验性桌面模式和托盘恢复入口

**Files:**
- Modify: `desktop-pet/src-tauri/src/platform/mod.rs`
- Modify: `desktop-pet/src-tauri/src/platform/windows.rs`
- Modify: `desktop-pet/src-tauri/src/lib.rs`
- Modify: `desktop-pet/src/runtime/bridge.ts`
- Create: `desktop-pet/docs/验证记录/M0手工验证清单.md`

**Interfaces:**
- Consumes: `WindowMode = "companion" | "desktop"`、宠物 HWND。
- Produces: `set_window_mode(mode) -> WindowModeEvidence { requested, applied, strategy, parentHwnd }`；托盘菜单 `陪伴模式/桌面模式/显示或隐藏/退出`。

- [ ] **Step 1: 写模式字符串校验失败测试**

Add to `src-tauri/src/windowing.rs`:

```rust
#[derive(Debug, Clone, Copy, Deserialize, Serialize, PartialEq, Eq)]
#[serde(rename_all = "camelCase")]
pub enum WindowMode { Companion, Desktop }

#[cfg(test)]
mod mode_tests {
    use super::*;

    #[test]
    fn rejects_unknown_window_mode() {
        assert!(serde_json::from_str::<WindowMode>("\"floating\"").is_err());
    }
}
```

Run:

```powershell
cargo test --manifest-path src-tauri/Cargo.toml rejects_unknown_window_mode
```

Expected: FAIL until `Serialize` is imported in `windowing.rs`; then add `Serialize` to the existing serde import and verify PASS.

- [ ] **Step 2: 扩展平台接口，不让调用方依赖 WorkerW**

Append to `src-tauri/src/platform/mod.rs`:

```rust
use crate::windowing::WindowMode;

#[derive(Debug, serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct WindowModeEvidence {
    pub requested: WindowMode,
    pub applied: bool,
    pub strategy: &'static str,
    pub parent_hwnd: Option<isize>,
}
```

Add to `PlatformAdapter`:

```rust
fn set_window_mode(&self, hwnd: isize, mode: WindowMode) -> Result<WindowModeEvidence, PlatformError>;
```

The public interface must not expose `Progman` or `WorkerW`; those are Windows implementation details.

- [ ] **Step 3: 实现陪伴模式与 WorkerW 实验探针**

Generate the tray/window icon from the already-created non-user test asset:

```powershell
npm run tauri icon public/test-assets/pet-probe.png
```

Expected: `src-tauri/icons/icon.ico` and the standard Tauri icon set are generated from the synthetic fixture.

Extend the imports in `src-tauri/src/platform/windows.rs` and add the complete helper plus trait method below:

```rust
use std::{ffi::c_void, ptr::{null, null_mut}};
use windows_sys::Win32::UI::WindowsAndMessaging::{
    EnumWindows, FindWindowExW, FindWindowW, SendMessageTimeoutW, SetParent, SetWindowPos,
    HWND_TOPMOST, SMTO_NORMAL, SWP_NOACTIVATE, SWP_NOMOVE, SWP_NOSIZE, SWP_SHOWWINDOW,
};
use crate::{
    platform::{PlatformAdapter, PlatformError, WindowModeEvidence},
    windowing::{RegionSpan, WindowMode},
};

const SPAWN_WORKERW_MESSAGE: u32 = 0x052C;

fn wide(value: &str) -> Vec<u16> {
    value.encode_utf16().chain(std::iter::once(0)).collect()
}

unsafe extern "system" fn find_workerw_callback(hwnd: *mut c_void, lparam: isize) -> i32 {
    let shell_view = wide("SHELLDLL_DefView");
    let workerw = wide("WorkerW");
    let view = FindWindowExW(hwnd, null_mut(), shell_view.as_ptr(), null());
    if !view.is_null() {
        let target = FindWindowExW(null_mut(), hwnd, workerw.as_ptr(), null());
        if !target.is_null() {
            *(lparam as *mut *mut c_void) = target;
            return 0;
        }
    }
    1
}

fn find_desktop_workerw() -> Result<isize, PlatformError> {
    unsafe {
        let progman_name = wide("Progman");
        let progman = FindWindowW(progman_name.as_ptr(), null());
        if progman.is_null() {
            return Err(PlatformError::Unavailable("Progman window not found"));
        }

        let mut message_result = 0usize;
        let sent = SendMessageTimeoutW(
            progman,
            SPAWN_WORKERW_MESSAGE,
            0,
            0,
            SMTO_NORMAL,
            1_000,
            &mut message_result,
        );
        if sent == 0 {
            return Err(last_error("SendMessageTimeoutW"));
        }

        let mut workerw: *mut c_void = null_mut();
        let _ = EnumWindows(Some(find_workerw_callback), &mut workerw as *mut _ as isize);
        if workerw.is_null() {
            return Err(PlatformError::Unavailable("WorkerW desktop host not found"));
        }
        Ok(workerw as isize)
    }
}

fn set_window_mode(&self, hwnd: isize, mode: WindowMode) -> Result<WindowModeEvidence, PlatformError> {
    match mode {
        WindowMode::Companion => {
            unsafe {
                let _ = SetParent(hwnd as *mut c_void, null_mut());
                if SetWindowPos(
                    hwnd as *mut c_void,
                    HWND_TOPMOST,
                    0, 0, 0, 0,
                    SWP_NOMOVE | SWP_NOSIZE | SWP_NOACTIVATE | SWP_SHOWWINDOW,
                ) == 0 {
                    return Err(last_error("SetWindowPos"));
                }
            }
            Ok(WindowModeEvidence {
                requested: mode,
                applied: true,
                strategy: "topmost-no-activate",
                parent_hwnd: None,
            })
        }
        WindowMode::Desktop => {
            let workerw = find_desktop_workerw()?;
            unsafe { windows_sys::Win32::Foundation::SetLastError(0); }
            let previous = unsafe { SetParent(hwnd as *mut c_void, workerw as *mut c_void) };
            if previous.is_null() && unsafe { GetLastError() } != 0 {
                return Err(last_error("SetParent"));
            }
            Ok(WindowModeEvidence {
                requested: mode,
                applied: true,
                strategy: "workerw-parent",
                parent_hwnd: Some(workerw),
            })
        }
    }
}
```

Keep all `Progman`/`WorkerW` symbols private to `windows.rs`. If `cargo check` reports a signature difference for the pinned `windows-sys 0.61.2`, correct the type at the call site without changing the public trait or evidence schema, then retain the compiler error and resolution in the task review notes.

- [ ] **Step 4: 注册模式命令和托盘菜单**

Add command in `src-tauri/src/lib.rs`:

```rust
#[tauri::command]
fn set_window_mode(
    window: tauri::WebviewWindow,
    state: tauri::State<'_, AppState>,
    mode: windowing::WindowMode,
) -> Result<platform::WindowModeEvidence, String> {
    let hwnd = window.hwnd().map_err(|error| error.to_string())?.0 as isize;
    state.platform.set_window_mode(hwnd, mode).map_err(|error| error.to_string())
}
```

Add this helper in `src-tauri/src/lib.rs` and call `build_tray(app)?;` from `.setup`:

```rust
use tauri::{menu::{Menu, MenuItem}, tray::TrayIconBuilder, Manager};

fn build_tray(app: &tauri::App) -> tauri::Result<()> {
    let companion = MenuItem::with_id(app, "companion", "陪伴模式", true, None::<&str>)?;
    let desktop = MenuItem::with_id(app, "desktop", "桌面模式（实验性）", true, None::<&str>)?;
    let toggle = MenuItem::with_id(app, "toggle", "显示或隐藏", true, None::<&str>)?;
    let quit = MenuItem::with_id(app, "quit", "退出", true, None::<&str>)?;
    let menu = Menu::with_items(app, &[&companion, &desktop, &toggle, &quit])?;
    let mut builder = TrayIconBuilder::new().menu(&menu);
    if let Some(icon) = app.default_window_icon() {
        builder = builder.icon(icon.clone());
    }
    builder
        .on_menu_event(|app, event| {
            let Some(window) = app.get_webview_window("pet") else { return; };
            match event.id().as_ref() {
                "companion" | "desktop" => {
                    let mode = if event.id().as_ref() == "companion" {
                        windowing::WindowMode::Companion
                    } else {
                        windowing::WindowMode::Desktop
                    };
                    if let (Ok(hwnd), state) = (window.hwnd(), app.state::<AppState>()) {
                        let _ = state.platform.set_window_mode(hwnd.0 as isize, mode);
                    }
                }
                "toggle" => {
                    if window.is_visible().unwrap_or(false) { let _ = window.hide(); }
                    else { let _ = window.show(); }
                }
                "quit" => app.exit(0),
                _ => {}
            }
        })
        .build(app)?;
    Ok(())
}
```

Append to `src/runtime/bridge.ts`:

```ts
import type { WindowMode } from "./contracts";

export interface WindowModeEvidence {
  requested: WindowMode;
  applied: boolean;
  strategy: string;
  parentHwnd: number | null;
}

export const setWindowMode = (mode: WindowMode): Promise<WindowModeEvidence> =>
  invoke("set_window_mode", { mode });
```

- [ ] **Step 5: 编译检查并运行两种模式的人工失败/恢复测试**

Run:

```powershell
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
npm run tauri dev
```

Expected manual checks:

- 陪伴模式：宠物位于普通窗口上方，不抢焦点。
- 桌面模式：宠物只在显示桌面或桌面图标层附近可见。
- `explorer.exe` 重启后，若桌面模式丢失，托盘“陪伴模式”必须恢复宠物；不得让用户只能从任务管理器退出。
- 桌面模式失败时 `WindowModeEvidence.applied=false` 或命令返回明确错误；陪伴模式仍可用。

- [ ] **Step 6: 创建固定格式的人工验证清单**

Create `docs/验证记录/M0手工验证清单.md`:

```md
# M0 手工验证清单

| 编号 | 场景 | 操作 | 预期 | 结果 | 证据路径 |
|---|---|---|---|---|---|
| W-01 | 陪伴模式焦点 | 在记事本输入时启动桌宠 | 记事本继续接收输入 | 未测试 | `docs/验证记录/证据/W-01/` |
| W-02 | 透明区穿透 | 点击宠物四角透明区域 | 后方窗口收到点击 | 未测试 | `docs/验证记录/证据/W-02/` |
| W-03 | 宠物区点击 | 点击宠物身体 | 宠物窗口收到指针事件 | 未测试 | `docs/验证记录/证据/W-03/` |
| W-04 | 桌面模式 | 从托盘切换桌面模式 | 普通窗口遮住宠物，桌面可见 | 未测试 | `docs/验证记录/证据/W-04/` |
| W-05 | Explorer 恢复 | 桌面模式下重启 Explorer | 可从托盘恢复陪伴模式 | 未测试 | `docs/验证记录/证据/W-05/` |
```

Only replace `未测试` with `通过`、`实验性` or `未通过` after performing the action and recording evidence.

- [ ] **Step 7: 提交窗口模式探针**

```powershell
git add src src-tauri docs/验证记录/M0手工验证清单.md
git commit -m "feat: probe companion and desktop window modes"
```

### Task 7: 验证全屏检测与自动隐藏策略

**Files:**
- Create: `desktop-pet/src/runtime/fullscreen.ts`
- Create: `desktop-pet/src/runtime/fullscreen.test.ts`
- Modify: `desktop-pet/src/runtime/bridge.ts`
- Modify: `desktop-pet/src/main.ts`
- Modify: `desktop-pet/src-tauri/src/platform/mod.rs`
- Modify: `desktop-pet/src-tauri/src/platform/windows.rs`
- Modify: `desktop-pet/src-tauri/src/lib.rs`

**Interfaces:**
- Consumes: 当前前台窗口矩形、所在显示器矩形、自身 PID。
- Produces: `FullscreenSnapshot { isFullscreen, foregroundHwnd, monitorRect, reason }`；`classifyFullscreen`；Rust command `probe_fullscreen`。

- [ ] **Step 1: 写全屏分类容差与普通最大化失败测试**

Create `src/runtime/fullscreen.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { classifyFullscreen } from "./fullscreen";

describe("classifyFullscreen", () => {
  const monitor = { left: 0, top: 0, right: 1920, bottom: 1080 };

  it("accepts a borderless window within two pixels", () => {
    expect(classifyFullscreen({ left: -1, top: 0, right: 1921, bottom: 1080 }, monitor, 2)).toBe(true);
  });

  it("rejects a maximized work-area window that leaves the taskbar visible", () => {
    expect(classifyFullscreen({ left: 0, top: 0, right: 1920, bottom: 1040 }, monitor, 2)).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
npm test -- src/runtime/fullscreen.test.ts
```

Expected: FAIL，提示无法解析 `./fullscreen`。

- [ ] **Step 3: 实现纯分类函数和快照类型**

Create `src/runtime/fullscreen.ts`:

```ts
export interface ScreenRect { left: number; top: number; right: number; bottom: number }

export function classifyFullscreen(window: ScreenRect, monitor: ScreenRect, tolerance = 2): boolean {
  return Math.abs(window.left - monitor.left) <= tolerance
    && Math.abs(window.top - monitor.top) <= tolerance
    && Math.abs(window.right - monitor.right) <= tolerance
    && Math.abs(window.bottom - monitor.bottom) <= tolerance;
}

export interface FullscreenSnapshot {
  isFullscreen: boolean;
  foregroundHwnd: number | null;
  monitorRect: ScreenRect | null;
  reason: "foreground-covers-monitor" | "not-fullscreen" | "own-window" | "no-foreground";
}
```

- [ ] **Step 4: 在平台接口中实现可诊断的 Windows 全屏探针**

Add to `PlatformAdapter`:

```rust
fn probe_fullscreen(&self, own_pid: u32) -> Result<FullscreenSnapshot, PlatformError>;
```

Add these definitions to `platform/mod.rs`:

```rust
#[derive(Debug, Clone, Copy, serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct ScreenRect { pub left: i32, pub top: i32, pub right: i32, pub bottom: i32 }

#[derive(Debug, serde::Serialize)]
#[serde(rename_all = "camelCase")]
pub struct FullscreenSnapshot {
    pub is_fullscreen: bool,
    pub foreground_hwnd: Option<isize>,
    pub monitor_rect: Option<ScreenRect>,
    pub reason: &'static str,
}
```

Add the complete method to `impl PlatformAdapter for WindowsPlatformAdapter` and extend imports with the named APIs:

```rust
fn probe_fullscreen(&self, own_pid: u32) -> Result<FullscreenSnapshot, PlatformError> {
    use windows_sys::Win32::{
        Graphics::Gdi::{GetMonitorInfoW, MonitorFromWindow, MONITORINFO, MONITOR_DEFAULTTONEAREST},
        UI::WindowsAndMessaging::{GetForegroundWindow, GetWindowRect, GetWindowThreadProcessId},
    };

    unsafe {
        let foreground = GetForegroundWindow();
        if foreground.is_null() {
            return Ok(FullscreenSnapshot {
                is_fullscreen: false,
                foreground_hwnd: None,
                monitor_rect: None,
                reason: "no-foreground",
            });
        }

        let mut foreground_pid = 0u32;
        GetWindowThreadProcessId(foreground, &mut foreground_pid);
        if foreground_pid == own_pid {
            return Ok(FullscreenSnapshot {
                is_fullscreen: false,
                foreground_hwnd: Some(foreground as isize),
                monitor_rect: None,
                reason: "own-window",
            });
        }

        let mut window_rect = std::mem::zeroed();
        if GetWindowRect(foreground, &mut window_rect) == 0 {
            return Err(last_error("GetWindowRect"));
        }
        let monitor = MonitorFromWindow(foreground, MONITOR_DEFAULTTONEAREST);
        let mut monitor_info: MONITORINFO = std::mem::zeroed();
        monitor_info.cbSize = std::mem::size_of::<MONITORINFO>() as u32;
        if GetMonitorInfoW(monitor, &mut monitor_info) == 0 {
            return Err(last_error("GetMonitorInfoW"));
        }

        let monitor_rect = ScreenRect {
            left: monitor_info.rcMonitor.left,
            top: monitor_info.rcMonitor.top,
            right: monitor_info.rcMonitor.right,
            bottom: monitor_info.rcMonitor.bottom,
        };
        let tolerance = 2;
        let is_fullscreen = (window_rect.left - monitor_rect.left).abs() <= tolerance
            && (window_rect.top - monitor_rect.top).abs() <= tolerance
            && (window_rect.right - monitor_rect.right).abs() <= tolerance
            && (window_rect.bottom - monitor_rect.bottom).abs() <= tolerance;

        Ok(FullscreenSnapshot {
            is_fullscreen,
            foreground_hwnd: Some(foreground as isize),
            monitor_rect: Some(monitor_rect),
            reason: if is_fullscreen { "foreground-covers-monitor" } else { "not-fullscreen" },
        })
    }
}
```

Add `FullscreenSnapshot` and `ScreenRect` to the existing `crate::platform` import in `windows.rs`. The reason strings must exactly match the TypeScript union.

Register:

```rust
#[tauri::command]
fn probe_fullscreen(state: tauri::State<'_, AppState>) -> Result<platform::FullscreenSnapshot, String> {
    state.platform.probe_fullscreen(std::process::id()).map_err(|error| error.to_string())
}
```

- [ ] **Step 5: 先用 750ms 前端探针验证隐藏后能否恢复**

Append to `src/runtime/bridge.ts`:

```ts
import type { FullscreenSnapshot } from "./fullscreen";
export const probeFullscreen = (): Promise<FullscreenSnapshot> => invoke("probe_fullscreen");
```

Add to `src/main.ts`:

```ts
import { getCurrentWindow } from "@tauri-apps/api/window";
import { probeFullscreen } from "./runtime/bridge";

let hiddenForFullscreen = false;
window.setInterval(async () => {
  const snapshot = await probeFullscreen();
  const petWindow = getCurrentWindow();
  if (snapshot.isFullscreen && !hiddenForFullscreen) {
    hiddenForFullscreen = true;
    await petWindow.hide();
  } else if (!snapshot.isFullscreen && hiddenForFullscreen) {
    hiddenForFullscreen = false;
    await petWindow.show();
  }
}, 750);
```

This polling loop is an M0 probe, not the accepted M1 architecture. Record its CPU cost and whether a hidden WebView continues the timer.

- [ ] **Step 6: 验证普通最大化、视频全屏、Alt+Tab 和自动恢复**

Run:

```powershell
npm test -- src/runtime/fullscreen.test.ts
npm run typecheck
cargo test --manifest-path src-tauri/Cargo.toml
npm run tauri dev
```

Expected:

- 普通最大化窗口不隐藏宠物。
- 浏览器视频全屏时隐藏。
- `Alt+Tab` 离开全屏应用后 1.5 秒内恢复。
- 如果隐藏后前端定时器暂停导致无法恢复，将结果标记为 FAIL，并在 M0 技术结论中要求 M1 改用 Rust 定时器或 WinEventHook；不得用更高频轮询掩盖问题。

- [ ] **Step 7: 提交全屏策略探针**

```powershell
git add src src-tauri
git commit -m "feat: probe fullscreen avoidance behavior"
```

### Task 8: 引入渲染档位并采集真实资源趋势

**Files:**
- Create: `desktop-pet/src/runtime/render-scheduler.ts`
- Create: `desktop-pet/src/runtime/render-scheduler.test.ts`
- Modify: `desktop-pet/src/runtime/pet-stage.ts`
- Create: `desktop-pet/scripts/测量桌宠资源.ps1`
- Create: `desktop-pet/docs/验证记录/性能基线.md`

**Interfaces:**
- Consumes: `RenderTier`、PixiJS `Ticker`、应用进程 PID。
- Produces: `RenderScheduler.setTier(tier)`；CSV 列 `timestamp,state,totalCpuPercent,totalPrivateMb,processCount,handleCount,threadCount`；P95/峰值汇总。

- [ ] **Step 1: 写档位帧率和暂停行为失败测试**

Create `src/runtime/render-scheduler.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { RenderScheduler } from "./render-scheduler";

describe("RenderScheduler", () => {
  it("stops continuous rendering in still and paused tiers", () => {
    const start = vi.fn();
    const stop = vi.fn();
    const scheduler = new RenderScheduler({ start, stop, setMaxFps: vi.fn(), renderOnce: vi.fn() });
    scheduler.setTier("still");
    scheduler.setTier("paused");
    expect(stop).toHaveBeenCalledTimes(2);
    expect(start).not.toHaveBeenCalled();
  });

  it("uses 24 fps for companion and 60 fps for temporary active interaction", () => {
    const setMaxFps = vi.fn();
    const scheduler = new RenderScheduler({ start: vi.fn(), stop: vi.fn(), setMaxFps, renderOnce: vi.fn() });
    scheduler.setTier("companion");
    scheduler.setTier("active");
    expect(setMaxFps.mock.calls).toEqual([[24], [60]]);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run:

```powershell
npm test -- src/runtime/render-scheduler.test.ts
```

Expected: FAIL，提示无法解析 `./render-scheduler`。

- [ ] **Step 3: 实现最小调度器并接入 PixiJS ticker**

Create `src/runtime/render-scheduler.ts`:

```ts
import type { RenderTier } from "./contracts";

export interface RenderDriver {
  start(): void;
  stop(): void;
  setMaxFps(fps: number): void;
  renderOnce(): void;
}

export class RenderScheduler {
  constructor(private readonly driver: RenderDriver) {}

  setTier(tier: RenderTier): void {
    if (tier === "active") {
      this.driver.setMaxFps(60);
      this.driver.start();
      return;
    }
    if (tier === "companion") {
      this.driver.setMaxFps(24);
      this.driver.start();
      return;
    }
    this.driver.stop();
    if (tier === "still") this.driver.renderOnce();
  }
}
```

In `PetStage`, construct:

```ts
this.scheduler = new RenderScheduler({
  start: () => this.app.start(),
  stop: () => this.app.stop(),
  setMaxFps: (fps) => { this.app.ticker.maxFPS = fps; },
  renderOnce: () => this.app.render(),
});
```

Default to `still`; switch to `active` on pointer down and back to `still` 400 ms after pointer up. Task M2 later adds the real `companion` micro-animation tier.

- [ ] **Step 4: 创建包含子进程的资源采样脚本**

Create `scripts/测量桌宠资源.ps1`:

```powershell
param(
  [Parameter(Mandatory=$true)][int]$RootPid,
  [ValidateSet('idle','interaction','hidden')][string]$State = 'idle',
  [int]$DurationSeconds = 300,
  [string]$OutputPath = "docs/验证记录/性能采样/$State.csv"
)

$logicalProcessors = [Environment]::ProcessorCount
$resolved = [IO.Path]::GetFullPath((Join-Path (Get-Location) $OutputPath))
[IO.Directory]::CreateDirectory([IO.Path]::GetDirectoryName($resolved)) | Out-Null
$previousCpu = @{}
$rows = New-Object System.Collections.Generic.List[object]

function Get-ProcessTree([int]$ParentPid) {
  $all = Get-CimInstance Win32_Process
  $ids = New-Object System.Collections.Generic.HashSet[int]
  [void]$ids.Add($ParentPid)
  do {
    $changed = $false
    foreach ($process in $all) {
      if ($ids.Contains([int]$process.ParentProcessId) -and $ids.Add([int]$process.ProcessId)) { $changed = $true }
    }
  } while ($changed)
  return $ids
}

for ($second = 0; $second -lt $DurationSeconds; $second++) {
  $ids = Get-ProcessTree $RootPid
  $processes = @($ids | ForEach-Object { Get-Process -Id $_ -ErrorAction SilentlyContinue })
  $cpuPercent = 0.0
  foreach ($process in $processes) {
    $current = [double]$process.CPU
    if ($previousCpu.ContainsKey($process.Id)) {
      $cpuPercent += (($current - $previousCpu[$process.Id]) / $logicalProcessors) * 100.0
    }
    $previousCpu[$process.Id] = $current
  }
  $rows.Add([PSCustomObject]@{
    timestamp = (Get-Date).ToString('o')
    state = $State
    totalCpuPercent = [Math]::Round($cpuPercent,3)
    totalPrivateMb = [Math]::Round((($processes | Measure-Object PrivateMemorySize64 -Sum).Sum / 1MB),2)
    processCount = $processes.Count
    handleCount = ($processes | Measure-Object Handles -Sum).Sum
    threadCount = ($processes | ForEach-Object { $_.Threads.Count } | Measure-Object -Sum).Sum
  })
  Start-Sleep -Seconds 1
}

$rows | Export-Csv -LiteralPath $resolved -NoTypeInformation -Encoding UTF8
$sortedCpu = @($rows.totalCpuPercent | Sort-Object)
$p95Index = [Math]::Max(0,[Math]::Ceiling($sortedCpu.Count * 0.95) - 1)
[PSCustomObject]@{
  State = $State
  Samples = $rows.Count
  CpuP95 = $sortedCpu[$p95Index]
  PrivateMbPeak = ($rows.totalPrivateMb | Measure-Object -Maximum).Maximum
  Output = $resolved
} | Format-List
```

- [ ] **Step 5: 采集空闲、交互和隐藏三组数据**

Build and start the debug executable, resolve its root PID automatically, then run:

```powershell
npm run tauri build -- --debug
$petProcess = Start-Process -FilePath '.\src-tauri\target\debug\desktop-pet.exe' -PassThru
$petPid = $petProcess.Id
powershell -ExecutionPolicy Bypass -File scripts/测量桌宠资源.ps1 -RootPid $petPid -State idle -DurationSeconds 300
powershell -ExecutionPolicy Bypass -File scripts/测量桌宠资源.ps1 -RootPid $petPid -State interaction -DurationSeconds 120
powershell -ExecutionPolicy Bypass -File scripts/测量桌宠资源.ps1 -RootPid $petPid -State hidden -DurationSeconds 300
Stop-Process -Id $petPid
```

During `interaction`, continuously drag and resize for the first 60 seconds; before the `hidden` command, hide the app from the tray. `Stop-Process` is scoped to the exact PID returned by `Start-Process`; do not kill by a broad process-name wildcard.

Expected: three CSV files and console summaries. Do not average away spikes; report P95 and peak private memory separately.

- [ ] **Step 6: 写入有证据的性能基线**

Create `docs/验证记录/性能基线.md` with this fixed schema and replace only the measurement cells:

```md
# M0 性能基线

## 测试环境

- Windows 版本：由 `winver` 和 `Get-CimInstance Win32_OperatingSystem` 记录
- CPU/内存：由 `Get-CimInstance Win32_ComputerSystem` 记录
- WebView2：由 `npm run tauri info` 记录
- 构建类型：debug

| 状态 | 时长 | CPU P95 | 私有内存峰值 | 进程峰值 | 结论 | CSV |
|---|---:|---:|---:|---:|---|---|
| 空闲 | 300 秒 | 未测 | 未测 | 未测 | 未测试 | `性能采样/idle.csv` |
| 交互 | 120 秒 | 未测 | 未测 | 未测 | 未测试 | `性能采样/interaction.csv` |
| 隐藏 | 300 秒 | 未测 | 未测 | 未测 | 未测试 | `性能采样/hidden.csv` |

## 预算判断

- 空闲 CPU P95 预算：`<= 1%`
- 交互 CPU P95 预算：`<= 3%`
- 空闲私有内存目标：`<= 120 MB`；超过 `180 MB` 必须专项分析
- 隐藏状态：不得存在高频轮询造成的持续 CPU 占用

结论只能写“通过”“有条件通过”或“未通过”，并引用 CSV；不能使用单张任务管理器截图替代采样。
```

- [ ] **Step 7: 验证调度测试并提交性能工具**

Run:

```powershell
npm test -- src/runtime/render-scheduler.test.ts
npm run typecheck
git add src scripts/测量桌宠资源.ps1 docs/验证记录
git commit -m "perf: add M0 render tiers and resource sampling"
```

Expected: 2 个 scheduler 测试通过；提交包含三份无用户内容的原始 CSV 和 `性能基线.md` 汇总，便于复核 P95 结论。

### Task 9: 完成 Windows 兼容性矩阵、阶段结论和构建闸门

**Files:**
- Create: `desktop-pet/docs/验证记录/Windows兼容性矩阵.md`
- Create: `desktop-pet/docs/验证记录/M0技术结论.md`
- Create: `desktop-pet/scripts/执行M0检查.ps1`
- Modify: `desktop-pet/README.md`

**Interfaces:**
- Consumes: Tasks 1–8 的程序、测试、人工记录和性能数据。
- Produces: M0 阶段结论；M1 必须采用的 `PlatformAdapter` 决策；一条全量检查命令。

- [ ] **Step 1: 创建不可含糊的兼容性矩阵**

Create `docs/验证记录/Windows兼容性矩阵.md`:

```md
# Windows 兼容性矩阵

结果只能使用：通过、实验性、未通过、未测试。

| 系统 | DPI | 显示器 | 场景 | 结果 | 证据 |
|---|---:|---|---|---|---|
| Windows 11 当前设备 | 100% | 单屏 | 陪伴模式、穿透、拖拽 | 未测试 | `证据/Win11-100-single/` |
| Windows 11 当前设备 | 125% | 单屏 | 位置与命中区域 | 未测试 | `证据/Win11-125-single/` |
| Windows 11 当前设备 | 150% | 双屏混合 DPI | 跨屏与恢复 | 未测试 | `证据/Win11-150-dual/` |
| Windows 11 当前设备 | 200% | 单屏 | 缩放与命中区域 | 未测试 | `证据/Win11-200-single/` |
| Windows 11 当前设备 | 当前值 | 当前显示器 | 睡眠、唤醒、锁屏 | 未测试 | `证据/Win11-resume/` |
| Windows 11 当前设备 | 当前值 | 当前显示器 | 全屏视频与 Alt+Tab | 未测试 | `证据/Win11-fullscreen/` |
| Windows 11 当前设备 | 当前值 | 当前显示器 | Explorer 重启与桌面模式 | 未测试 | `证据/Win11-explorer/` |
| Windows 10 22H2 测试机 | 100% | 单屏 | P0 核心路径 | 未测试 | `证据/Win10-22H2/` |
```

If Windows 10 hardware is unavailable, leave that row `未测试`; do not infer compatibility from Windows 11.

- [ ] **Step 2: 创建一键检查脚本并先验证它能发现格式问题**

Create `scripts/执行M0检查.ps1`:

```powershell
$ErrorActionPreference = 'Stop'
npm test
npm run typecheck
npm run build
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo test --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
npm run tauri build -- --debug
git diff --check
Write-Output 'M0 automated checks passed.'
```

Intentionally add one trailing space to a test line, run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/执行M0检查.ps1
```

Expected: FAIL at `git diff --check` with a whitespace error. Remove the trailing space using `apply_patch`, rerun, and expect the final line `M0 automated checks passed.`

- [ ] **Step 3: 执行全部人工矩阵，不用未测试项换取通过结论**

Perform every locally available row in `M0手工验证清单.md` and `Windows兼容性矩阵.md`. For each performed row:

1. Record OS build, DPI, monitor layout and app commit SHA.
2. Record result as `通过`、`实验性` or `未通过`.
3. Store screenshot, short screen recording or command output under the declared evidence path.
4. Keep unavailable environments as `未测试`.

Expected: Windows 11 current-device rows have evidence; Windows 10 may remain `未测试` but prevents any “Windows 10 已验证” claim.

- [ ] **Step 4: 编写 M0 技术结论并冻结 M1 输入**

Create `docs/验证记录/M0技术结论.md` with this exact decision structure:

```md
# M0 技术结论

## 阶段判定

- 总体：未评估
- 默认陪伴模式：未评估
- Alpha 窗口区域穿透：未评估
- 实验性桌面模式：未评估
- 全屏避让：未评估
- 性能预算：未评估

## 实测证据

| 能力 | 实现策略 | 结果 | 证据 |
|---|---|---|---|
| 透明窗口 | Tauri transparent WebView2 | 未评估 | `M0手工验证清单.md` |
| 点击穿透 | Win32 SetWindowRgn | 未评估 | `M0手工验证清单.md` |
| 陪伴模式 | Topmost + no activate | 未评估 | `Windows兼容性矩阵.md` |
| 桌面模式 | WorkerW parent | 未评估 | `Windows兼容性矩阵.md` |
| 全屏避让 | 前端 750ms 探针 | 未评估 | `Windows兼容性矩阵.md` |
| 资源基线 | 进程树时间序列采样 | 未评估 | `性能基线.md` |

## M1 冻结决策

逐项写明：保留当前实现、替换实现、仅保留实验模式，或阻止进入 M1。每个决定必须引用实测证据。

## 未验证范围

明确列出未取得设备或场景的项目。未验证项不等于通过。

## 下一阶段条件

只有默认陪伴模式、穿透、位置恢复和合理资源基线通过，才允许编写 M1 详细实施计划。桌面模式可作为实验性能力降级，不单独阻止 M1。
```

Replace every `未评估` using evidence. The overall result can only be:

- `通过，可进入 M1`
- `有条件通过，可进入 M1，桌面模式保持实验性`
- `未通过，停止并调整技术路线`

- [ ] **Step 5: 更新 README 的真实当前状态**

Append to `README.md`:

```md
## M0 状态

M0 的当前结论见 [M0技术结论](docs/验证记录/M0技术结论.md)。该文档区分已通过、实验性、未通过和未测试能力；README 不单独重复可能过期的兼容性结论。
```

- [ ] **Step 6: 运行最终自动闸门并审查工作树**

Run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/执行M0检查.ps1
git status --short
git diff --check
git diff --stat
```

Expected: 全部自动检查通过；工作树只包含本 Task 的文档、脚本和 README 变更；不存在临时密钥、真实宠物照片或构建输出。性能原始 CSV 属于应提交的验证证据。

- [ ] **Step 7: 提交 M0 结论**

```powershell
git add README.md scripts/执行M0检查.ps1 docs/验证记录
git commit -m "docs: record M0 Windows feasibility verdict"
git status --short --branch
```

Expected: 工作树干净。只有当 `M0技术结论.md` 的总体结果允许进入 M1 时，下一次会话才使用 `writing-plans` 编写 `M1-P0运行时实施计划.md`。

## M0 完成定义

M0 不是“窗口能打开”即完成。必须同时具备：

1. 可构建、可测试、可重复启动的 Tauri/PixiJS 探针。
2. 默认陪伴模式、透明区域穿透、拖拽和位置恢复的实测证据。
3. 桌面模式的明确结论，允许为实验性或未通过。
4. 全屏隐藏与恢复的明确结论，包括前端轮询是否可接受。
5. 空闲、交互和隐藏状态的时间序列资源数据。
6. Windows 11 当前设备的 DPI、显示器、睡眠唤醒和 Explorer 恢复记录。
7. 对 Windows 10 等未取得环境保持“未测试”，不做推断性通过。
8. 一份冻结 M1 平台实现输入的中文技术结论。
