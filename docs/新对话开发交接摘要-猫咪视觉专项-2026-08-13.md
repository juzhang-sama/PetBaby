# 猫咪视觉专项新对话开发交接摘要

## 当前结论

标准猫验证阶段已经完成，下一步从 Task 3 角色包与绑定契约继续，不需要重做 Task 1–2，也不得跳过角色包门槛直接进入运行时动作。

开发主线优先级已经校准为：

1. 真实宠物照片生成效果与身份还原；
2. 角色动效、陪伴感和交互反馈；
3. 边缘藏身与动态尾巴；
4. 领养猫内容扩展。

## 工作区

- 仓库：`D:\petBaby\desktop-pet`
- 隔离工作树：`D:\petBaby\desktop-pet\.worktrees\cat-visual-runtime`
- 分支：`feature/cat-visual-runtime`
- 当前提交：以该工作树内 `git log -1 --oneline` 为准。
- 不要修改主工作区或 `.worktrees\breath-sway-runtime`。
- 不要覆盖主工作区未跟踪的 `docs\新对话开发交接摘要.md`。

## 已完成阶段

### 基线稳定性

- 前端全量测试：`917/917`。
- Rust 全量测试：`686/686`。

### Task 1：标准猫视觉验证

- 唯一正式候选已经生成并留档。
- 透明图、五类背景板和五处局部证据齐全。
- `StandardCatVisualAcceptanceV1` 为 `10/10 pass`。
- 用户已授权后续纯主观视觉确认可默认同意；技术失败、证据缺失、安全或付费边界不能默认放行。

### Task 2：标准猫可绑定分层源

- 状态：`DONE`。
- 正式 KRA：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\标准猫-A-v1.kra`。
- SHA-256：`FF783AC837D29A2155C14E368B229F6EF61843D46ABCDCEEF9816643DA1406D8`。
- `LayerContractV1`：20 个必需图层，名称唯一。
- KRA：`2048 × 2048 RGBA`，reserve 默认隐藏。
- 中性重组：RGBA 差异值数量 `0`。
- 遮挡补画：耳后头部、眼睑下眼球、口鼻下脸部、前肢后胸腹、尾根后身体均已完成。
- 独立图层 `tail`、`bodyUnderTail`、`tailReserve` 已为“身体藏到屏幕外，只显示动态尾巴”留出绑定基础。

详细证据见：

- `docs\验证记录\标准猫分层源验收.md`
- `.superpowers\sdd\task-2-report.md`
- `D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\工作稿\验收证据`

## 总体开发计划与当前位置

```text
标准猫验证（Task 1–2，已完成）
  ↓
角色包与绑定契约（Task 3–4，下一步）
  ↓
运行时动作与音效（Task 5–9）
  ↓
边缘藏身与动态尾巴（Task 10–13）
  ↓
照片分身猫（高优先级）与领养猫（低优先级）（Task 14–20）
  ↓
集中与全分支验收
```

边缘藏身不是“缩成尾巴图标”：身体必须完全位于对应屏幕边缘之外，屏幕内仅保留原角色的独立尾巴；尾巴仍需有持续、自然、可被状态机驱动的摆动。双屏环境下必须按宠物所在显示器的 work area 与边缘方向计算，不得把主屏坐标硬编码到副屏。

## 下一轮开始方式

1. 阅读原始规格与现有 Task 3–4 计划/简报。
2. 核对角色包格式、Live2D 绑定输入和运行时消费接口。
3. 先写绑定契约与失败门槛，再在 Live2D 中制作最小可运行角色包。
4. 用标准猫跑通加载、中性姿态、独立眨眼、耳尾参数和身体/尾巴解耦。
5. Task 4 结束做角色包阶段集中审查，再进入 Task 5。

不得把静态 PNG 位移冒充骨骼或参数化动作，也不得在标准猫角色包没有通过前批量制作照片分身猫或领养猫。

## 全局 Agent 行为规则

- 任一命令、测试、构建、代理、生成、审查、GUI 或外部调用连续超过 2 分钟，立即检查任务、进程、日志、Git、文件时间戳和产物变化。
- 无真实进展时停止等待并纠偏；同一工具错误连续两次必须停止盲目重试。
- 只有长命令返回真实 cell ID 时才能使用命令等待；等待代理只能使用代理等待能力。
- 没有真实证据不得声称“正在执行”或“已经完成”。
- 采用精简审查流程：逐 Task 保留实现者自检、规定测试和控制器验收；大阶段结束集中审查；最终全分支审查。

该规则已记录在全局记忆：`C:\Users\Administrator\.codex\memories\extensions\ad_hoc\notes\2026-08-13-全局操作超时检查规则.md`。

## 本地工具

- Krita：`D:\krita\Krita (x64)\bin\krita.exe`
- Live2D Cubism Editor 5.3：`D:\Live2D Cubism Editor\Live2D Cubism 5.3`
- 当前机器为双屏；GUI 操作前先观察全桌面和目标窗口所在显示器，所有点击均以最新截图/观察结果为依据。

## 外部资产回退点

- 正式分层源：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\标准猫-A-v1.kra`
- 上一候选备份：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\备份\标准猫-A-v1-候选1-20260813-2038.kra`
- 结构基线备份：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\备份\标准猫-A-v1-结构基线-20260813-1739.kra`
- 补画参考：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\工作稿\标准猫-A-v1-补画参考.png`

正式 KRA 已有备份；后续如需重导出，先校验新产物再替换，禁止在未验证时覆盖正式文件。
