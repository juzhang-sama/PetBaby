# 照片分身确定性纹理合成实施计划

> **历史方案：** 该文档保留已完成的确定性 alpha/canonical 基础设施记录；照片身份外观的后续实现以 `2026-08-17-照片分身路线校准设计.md` 和 `2026-08-18-照片分身修复与重验实施计划.md` 为准。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 GPT-image-2 的职责限制为宠物 RGB 身份外观生成，由受控后端确定性应用 body module alpha，并通过 Python、Rust、真实 Live2D 和 20 个真实样本门完成闭环验收。

**Architecture:** Python 后端生成全不透明 UV 工作画布和区域图，验证 provider 返回的完整 RGB 后，用纯函数合成 canonical RGBA PNG。后端保存不含照片的 attempt 审计，Rust 桌面端独立复验模块合同、canonical hash、逐像素 alpha 和透明 RGB，最后通过 fake UI 动态门与真实 20 样本门放行。

**Tech Stack:** Python 3、Pillow、pytest、FastAPI、Rust、rusqlite、image、sha2、Tauri 2、TypeScript、Vitest、PixiJS/Live2D、PowerShell、Playwright CLI。

## Global Constraints

- 唯一工作树是 `D:\petBaby\desktop-pet\.worktrees\cat-visual-runtime`，唯一分支是 `feature/cat-visual-runtime`。
- 当前工作树已有用户所有的未提交修复；不得回退、覆盖或把无关文件混入任务提交。
- `docs/新对话开发交接摘要-真实宠物主线与动效适配-2026-08-14.md`、`docs/验证记录/首只真实照片分身验收.md`、`docs/验证记录/证据/首只真实照片分身/`、`.omo/`、`cat-a-breath-qa/` 和 `呼吸运行时验收.html` 是受保护的既有脏路径，本计划不得顺手暂存。
- revision 13、job `63847454926843658af5209f637863f7`、provider task `106786157` 和 artifact SHA-256 `5503e493829423f316a348c9c96a54681383d0efb7a76417c9fa2ac0c653e9a0` 保持不可变。
- GPT-image-2 只负责 RGB 身份外观；body module 是 alpha、UV 拓扑、孔洞和半透明边缘的唯一权威。
- Provider 输入第一张必须是 2048×2048 全不透明工作画布，随后是顺序不变的 1 至 8 张宠物照片。
- Provider 输出只接受 RGB PNG 或全不透明 RGBA PNG；任一必需区域变更比例必须至少为 `95%`。
- Canonical 合成公式固定为：模块 alpha 大于零时复制 provider RGB，模块 alpha 为零时 RGB 归零，alpha 始终逐像素复制模块 alpha。
- 任一失败都关闭当前 attempt，不得降级为标准猫、静态 PNG、v3 包或跳过 builder。
- 当前三个 body module 的 alpha 相同，不可作为跨拓扑复用证据；必须通过六类独立拓扑 fixture。
- lk888 API id 固定为 `gpt-image-2`，产品名记录为 `GPT-image-2.0`；`apiContractVersion` 固定记录为 `lk888-media-generate-v1`。
- lk888 保存期限和隐私政策版本仍记录为 `unverified`，上游公开删除 API 记录为 `unsupported`；不得伪报远端已删除。
- 原照片和 provider raw 文件按终态立即清理；revision 13 是经用户确认保留的一次性历史 fixture。
- 真实样本使用用户已授权的 `01-photos` 八张非人类宠物照片；真实调用只在 Task 8 离线门通过后进入。
- 20 个样本首次技术成功至少 `17/20`，三次内至少 `19/20`；所有最终接受项人工视觉必须 `100%` 通过。
- 单个命令、测试、服务、GUI 或等待超过两分钟时，立即检查 PID、日志增量、Git 状态和产物时间戳；无进展就停止并定位原因。
- 每个任务先产生有效 RED，再做最小 GREEN；每次只提交该任务列出的路径。

## 文件结构与职责

- `services/appearance-generation/src/photo_avatar_backend/texture_compositor.py`：纯 RGB 校验、区域覆盖统计和 canonical alpha 合成，不读取仓库路径。
- `services/appearance-generation/src/photo_avatar_backend/uv_guides.py`：从模块合同生成全不透明工作画布、区域图和索引。
- `services/appearance-generation/src/photo_avatar_backend/pipelines.py`：冻结模块快照、调用 provider、合成 canonical artifact。
- `services/appearance-generation/src/photo_avatar_backend/audit.py`：定义和验证不含照片的 attempt 审计合同。
- `services/appearance-generation/src/photo_avatar_backend/job_store.py`：原子保存 canonical artifact 和终态审计，清理 raw artifact 时保留审计。
- `services/appearance-generation/src/photo_avatar_backend/app.py`：向桌面端返回 canonical artifact 和经过过滤的审计元数据。
- `apps/desktop/src-tauri/src/creation/photo_avatar/provider.rs`：严格反序列化后端纹理审计合同。
- `apps/desktop/src-tauri/src/creation/photo_avatar/store.rs`：把 canonical 审计与 artifact 绑定到不可变 revision。
- `apps/desktop/src-tauri/src/runtime_assets/photo_avatar_builder.rs`：从本地 module 独立复验 canonical PNG。
- `scripts/验证照片分身确定性纹理合成.ps1`：离线总门和 revision 13 只读回归。
- `scripts/照片分身真实20样本验收.ps1`：冻结样本矩阵、执行真实 UI 样本并汇总成功率。

---

### Task 0: 固化当前已复审修复基线

**Files:**
- Modify: `apps/desktop/settings.html`
- Modify: `apps/desktop/src-tauri/Cargo.toml`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/manager.rs`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/provider.rs`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/store.rs`
- Modify: `apps/desktop/src-tauri/src/lib.rs`
- Modify: `apps/desktop/src-tauri/src/runtime_assets/photo_avatar_builder.rs`
- Modify: `apps/desktop/src/creation/api.ts`
- Modify: `apps/desktop/src/creation/api.test.ts`
- Modify: `apps/desktop/src/settings.test.ts`
- Modify: `apps/desktop/src/settings/photo-avatar-creation-view.ts`
- Modify: `apps/desktop/src/settings/photo-avatar-creation-view.test.ts`
- Modify: `apps/desktop/src/settings/photo-avatar-live2d-preview.ts`
- Modify: `scripts/live2d/cubism-runtime.ts`
- Modify: `services/appearance-generation/src/photo_avatar_backend/app.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/config.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/job_store.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/lk888_client.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/pipelines.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_atlas_pipeline.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_config.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_desktop_contract_fixture.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_identity_pipeline.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_job_store.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_lk888_client.py`

**Interfaces:**
- Consumes: 当前工作树中已经通过独立复审的 `sessionId:revision` renderer 身份、Python/Rust alpha 复验和 fake atlas 修复。
- Produces: 一个可定位的基线提交；后续任务只在该提交之上增加确定性合成，不再把当前修复与新功能混成一个 diff。

- [ ] **Step 1: 核对差异只包含已复审范围**

Run:

```powershell
git diff --name-only
git diff -- apps/desktop/src/settings/photo-avatar-creation-view.ts apps/desktop/src-tauri/src/runtime_assets/photo_avatar_builder.rs services/appearance-generation/src/photo_avatar_backend/pipelines.py
git diff --check
```

Expected: `git diff --check` 退出码为 0；不得暂存 `.omo/`、运行日志、revision 13 artifact 或未列出的证据目录。

- [ ] **Step 2: 重跑已复审自动化基线**

Run:

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend -q
Pop-Location
npm --prefix apps/desktop test -- --run
npm --prefix apps/desktop run typecheck
npm --prefix apps/desktop run build
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml
cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --check
```

Expected: Python、前端和 Rust 全部退出 0；若数量与历史 `166/1032/810` 不同，以当前发现的测试总数为准，但不得有失败或跳过新增失败。

- [ ] **Step 3: 精确暂存并提交基线**

```powershell
git add -- apps/desktop/settings.html apps/desktop/src-tauri/Cargo.toml apps/desktop/src-tauri/src/creation/photo_avatar/manager.rs apps/desktop/src-tauri/src/creation/photo_avatar/provider.rs apps/desktop/src-tauri/src/creation/photo_avatar/store.rs apps/desktop/src-tauri/src/lib.rs apps/desktop/src-tauri/src/runtime_assets/photo_avatar_builder.rs apps/desktop/src/creation/api.ts apps/desktop/src/creation/api.test.ts apps/desktop/src/settings.test.ts apps/desktop/src/settings/photo-avatar-creation-view.ts apps/desktop/src/settings/photo-avatar-creation-view.test.ts apps/desktop/src/settings/photo-avatar-live2d-preview.ts scripts/live2d/cubism-runtime.ts services/appearance-generation/src/photo_avatar_backend/app.py services/appearance-generation/src/photo_avatar_backend/config.py services/appearance-generation/src/photo_avatar_backend/job_store.py services/appearance-generation/src/photo_avatar_backend/lk888_client.py services/appearance-generation/src/photo_avatar_backend/pipelines.py services/appearance-generation/src/photo_avatar_backend/test_atlas_pipeline.py services/appearance-generation/src/photo_avatar_backend/test_config.py services/appearance-generation/src/photo_avatar_backend/test_desktop_contract_fixture.py services/appearance-generation/src/photo_avatar_backend/test_identity_pipeline.py services/appearance-generation/src/photo_avatar_backend/test_job_store.py services/appearance-generation/src/photo_avatar_backend/test_lk888_client.py
git diff --cached --check
git diff --cached --name-only
git commit -m "fix: 关闭照片分身运行时验证缺口"
```

Expected: staged 列表只包含本 Task 路径，提交成功，其他用户文件仍保持原状态。

---

### Task 1: 实现纯确定性 RGB 校验与 alpha 合成器

**Files:**
- Create: `services/appearance-generation/src/photo_avatar_backend/texture_compositor.py`
- Create: `services/appearance-generation/src/photo_avatar_backend/test_texture_compositor.py`

**Interfaces:**
- Consumes: `provider_png: bytes`、`work_canvas_png: bytes`、`region_map_png: bytes`、`module_alpha: bytes`。
- Produces: `compose_canonical_texture(...) -> CanonicalTexture`，供 Task 3 pipeline 使用。

- [ ] **Step 1: 写六类拓扑和透明 provider RED**

在 `test_texture_compositor.py` 中用小尺寸内存 PNG 构造连续轮廓、断开岛、孔洞、半透明边缘、贴边轮廓和不同 alpha 对照组：

```python
def test_composer_uses_only_module_alpha_and_zeros_hidden_rgb():
    provider = png("RGB", (8, 8), lambda x, y: (20 + x, 40 + y, 80))
    canvas = png("RGBA", (8, 8), lambda x, y: (255, 0, 255, 255))
    regions = png("L", (8, 8), lambda x, y: 1 if 1 <= x <= 6 else 0)
    alpha = bytes(0 if x == 0 else 128 if x == 1 else 255 for y in range(8) for x in range(8))

    result = compose_canonical_texture(
        provider_png=provider,
        work_canvas_png=canvas,
        region_map_png=regions,
        module_alpha=alpha,
        minimum_change_ratio=0.95,
    )

    image = Image.open(BytesIO(result.png)).convert("RGBA")
    assert image.getpixel((0, 0)) == (0, 0, 0, 0)
    assert image.getpixel((1, 0))[3] == 128
    assert image.getchannel("A").tobytes() == alpha
```

同时添加：RGBA 任一 alpha 非 255 拒绝、区域变化率低于 0.95 拒绝、区域全黑拒绝、区域图与 alpha 不一致拒绝、相同输入两次 canonical hash 完全一致。

- [ ] **Step 2: 运行 RED**

Run:

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_texture_compositor.py -q
Pop-Location
```

Expected: collection 失败并明确提示 `photo_avatar_backend.texture_compositor` 不存在。

- [ ] **Step 3: 实现无仓库路径依赖的纯合成接口**

在 `texture_compositor.py` 从 `PIL` 导入 `Image, __version__ as PILLOW_VERSION`，并实现以下公开合同：

```python
COMPOSER_VERSION = "deterministic-alpha-v1"
PNG_ENCODER_VERSION = f"pillow-{PILLOW_VERSION}"

@dataclass(frozen=True)
class RegionCoverage:
    region_id: int
    pixel_count: int
    changed_pixels: int
    change_ratio: float

@dataclass(frozen=True)
class CoverageReport:
    minimum_change_ratio: float
    regions: tuple[RegionCoverage, ...]

    def to_wire(self) -> dict[str, object]:
        return {
            "minimumChangeRatio": self.minimum_change_ratio,
            "regions": [
                {
                    "regionId": item.region_id,
                    "pixelCount": item.pixel_count,
                    "changedPixels": item.changed_pixels,
                    "changeRatio": item.change_ratio,
                }
                for item in self.regions
            ],
        }

@dataclass(frozen=True)
class CanonicalTexture:
    png: bytes
    provider_raw_sha256: str
    canonical_sha256: str
    source_alpha_sha256: str
    coverage: CoverageReport

def compose_canonical_texture(
    *,
    provider_png: bytes,
    work_canvas_png: bytes,
    region_map_png: bytes,
    module_alpha: bytes,
    minimum_change_ratio: float = 0.95,
) -> CanonicalTexture:
    provider = _decode_provider_rgb(provider_png)
    canvas = _decode_rgba(work_canvas_png, "work canvas")
    regions = _decode_region_map(region_map_png)
    if provider.size != canvas.size or provider.size != regions.size:
        raise ContractError("texture composition dimensions do not match")
    if len(module_alpha) != provider.width * provider.height:
        raise ContractError("module alpha length does not match texture")
    coverage = _coverage(provider, canvas, regions, module_alpha, minimum_change_ratio)
    rgba = bytearray(provider.width * provider.height * 4)
    for index, rgb in enumerate(provider.getdata()):
        alpha = module_alpha[index]
        offset = index * 4
        rgba[offset:offset + 3] = bytes(rgb if alpha else (0, 0, 0))
        rgba[offset + 3] = alpha
    image = Image.frombytes("RGBA", provider.size, bytes(rgba))
    output = BytesIO()
    image.save(output, format="PNG", optimize=False, compress_level=9)
    png_bytes = output.getvalue()
    return CanonicalTexture(
        png=png_bytes,
        provider_raw_sha256=hashlib.sha256(provider_png).hexdigest(),
        canonical_sha256=hashlib.sha256(png_bytes).hexdigest(),
        source_alpha_sha256=hashlib.sha256(module_alpha).hexdigest(),
        coverage=coverage,
    )
```

`_decode_provider_rgb` 只接受 `RGB` 或全不透明 `RGBA`；`_coverage` 要求所有 module alpha 大于零的像素都有非零 region ID，且每个 region 的变化率 `>=0.95`、像素不全为黑色。

- [ ] **Step 4: 运行 GREEN 和模块全量测试**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_texture_compositor.py -q
python -m pytest src/photo_avatar_backend -q
Pop-Location
```

Expected: 两条命令退出 0；六类 fixture 均逐像素通过。

- [ ] **Step 5: 提交纯合成器**

```powershell
git add -- services/appearance-generation/src/photo_avatar_backend/texture_compositor.py services/appearance-generation/src/photo_avatar_backend/test_texture_compositor.py
git diff --cached --check
git commit -m "feat: 添加确定性纹理合成器"
```

---

### Task 2: 生成全不透明工作画布与区域图资产

**Files:**
- Modify: `services/appearance-generation/src/photo_avatar_backend/uv_guides.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_uv_guides.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/索引.json`
- Create: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-slender-v1.work.png`
- Create: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-balanced-v1.work.png`
- Create: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-rounded-v1.work.png`
- Create: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-slender-v1.regions.png`
- Create: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-balanced-v1.regions.png`
- Create: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-rounded-v1.regions.png`
- Modify: `scripts/生成照片分身身份中立UV引导.ps1`

**Interfaces:**
- Consumes: module `模块.json` 和 neutral texture。
- Produces: `build_work_canvas(neutral_png: bytes) -> WorkCanvasBundle` 以及 schema v2 索引字段，供 Task 3 读取。

- [ ] **Step 1: 写画布、区域图和索引 RED**

在 `test_uv_guides.py` 增加：

```python
def test_work_canvas_is_fully_opaque_and_region_map_matches_source_alpha():
    bundle = build_work_canvas(_neutral_png("body-balanced-v1"))
    work = _open_rgba(bundle.work_canvas_png)
    regions = Image.open(BytesIO(bundle.region_map_png))
    source = _open_rgba(_neutral_png("body-balanced-v1"))

    assert set(work.getchannel("A").getdata()) == {255}
    assert regions.mode == "L"
    assert all(
        (region > 0) == (alpha > 0)
        for region, alpha in zip(regions.getdata(), source.getchannel("A").getdata())
    )
    assert hashlib.sha256(bundle.source_alpha).hexdigest() == bundle.source_alpha_sha256
```

索引测试必须断言每个模块包含 `workCanvasPath`、`regionMapPath`、`workCanvasSha256`、`regionMapSha256`、`sourceTextureSha256` 和 `sourceAlphaSha256`，并拒绝越界路径与同步篡改。

- [ ] **Step 2: 运行 RED**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_uv_guides.py -q
Pop-Location
```

Expected: 失败原因是 `build_work_canvas` 或新索引字段尚不存在。

- [ ] **Step 3: 实现 schema v2 生成器**

在 `uv_guides.py` 新增：

```python
GENERATOR_VERSION = "opaque-uv-work-canvas-v2"
BACKGROUND_COLOR = (17, 19, 23, 255)

@dataclass(frozen=True)
class WorkCanvasBundle:
    work_canvas_png: bytes
    region_map_png: bytes
    source_alpha: bytes
    source_alpha_sha256: str

def build_work_canvas(neutral_png: bytes) -> WorkCanvasBundle:
    source = _decode_neutral(neutral_png)
    alpha = source.getchannel("A")
    work = Image.new("RGBA", source.size, BACKGROUND_COLOR)
    regions = Image.new("L", source.size, 0)
    work_pixels = work.load()
    region_pixels = regions.load()
    alpha_pixels = alpha.load()
    for y in range(source.height):
        row = min(2, (y * 3) // source.height)
        for x in range(source.width):
            if alpha_pixels[x, y] > 0:
                column = min(1, (x * 2) // source.width)
                region_id = row * 2 + column + 1
                work_pixels[x, y] = (*_PALETTE[region_id - 1], 255)
                region_pixels[x, y] = region_id
    return WorkCanvasBundle(
        work_canvas_png=_encode_png(work),
        region_map_png=_encode_png(regions),
        source_alpha=alpha.tobytes(),
        source_alpha_sha256=hashlib.sha256(alpha.tobytes()).hexdigest(),
    )
```

保留旧透明 guide 字段直到 Task 3 pipeline 切换完成，避免本 Task 破坏现有全量测试。PowerShell 包装脚本继续只调用模块入口，但输出文案改为“工作画布和区域图”。

- [ ] **Step 4: 生成资产并验证可重复性**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/生成照片分身身份中立UV引导.ps1
$before = Get-FileHash services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/*.work.png,services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/*.regions.png -Algorithm SHA256
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/生成照片分身身份中立UV引导.ps1
$after = Get-FileHash services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/*.work.png,services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/*.regions.png -Algorithm SHA256
Compare-Object $before.Hash $after.Hash
```

Expected: `Compare-Object` 无输出；三个工作画布 alpha 全为 255，三个区域图与各自模块 alpha 正像素完全对应。

- [ ] **Step 5: 运行 GREEN 并提交资产合同**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_uv_guides.py src/photo_avatar_backend/test_atlas_pipeline.py -q
Pop-Location
git add -- services/appearance-generation/src/photo_avatar_backend/uv_guides.py services/appearance-generation/src/photo_avatar_backend/test_uv_guides.py services/appearance-generation/src/photo_avatar_backend/assets/uv-guides scripts/生成照片分身身份中立UV引导.ps1
git diff --cached --check
git commit -m "feat: 生成照片分身不透明UV工作画布"
```

Expected: 测试退出 0；提交包含生成器、索引、三套 work/regions 资产和包装脚本。

---

### Task 3: 将 renderTextureAtlas 切换为 canonical 合成

**Files:**
- Modify: `services/appearance-generation/src/photo_avatar_backend/pipelines.py:79-85`
- Modify: `services/appearance-generation/src/photo_avatar_backend/pipelines.py:430-622`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_atlas_pipeline.py`
- Delete: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-slender-v1.png`
- Delete: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-balanced-v1.png`
- Delete: `services/appearance-generation/src/photo_avatar_backend/assets/uv-guides/body-rounded-v1.png`

**Interfaces:**
- Consumes: Task 1 `compose_canonical_texture` 和 Task 2 schema v2 索引。
- Produces: 扩展后的 `TextureArtifact`，其 `png/sha256` 始终指 canonical，不再指 provider raw。

- [ ] **Step 1: 把现有 atlas 测试改成 opaque provider RED**

更新 fake client，使合法 provider 输出是 `RGB` 或全不透明 `RGBA`，并断言第一输入为 work canvas：

```python
artifact = render_texture_atlas(request, client, GUIDE_INDEX)
assert Image.open(BytesIO(client.images[0])).getchannel("A").getextrema() == (255, 255)
assert client.images[1:] == [source.png for source in request.source_images]
assert artifact.provider_raw_sha256 == hashlib.sha256(client.output).hexdigest()
assert artifact.sha256 == hashlib.sha256(artifact.png).hexdigest()
assert artifact.png != client.output
assert _alpha(artifact.png) == _alpha(_neutral_png("body-balanced-v1"))
```

增加低于 95% 变更、透明 RGBA、工作画布/区域图 hash 篡改、标准猫 canonical 和透明像素 RGB 未归零的失败测试。

- [ ] **Step 2: 运行 RED**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_atlas_pipeline.py -q
Pop-Location
```

Expected: 失败显示旧 pipeline 仍要求 provider alpha 与模块完全相等，且 `TextureArtifact` 缺少 raw/canonical 字段。

- [ ] **Step 3: 扩展 TextureArtifact 并切换 pipeline**

实现以下类型和关键流程：

```python
@dataclass(frozen=True)
class TextureArtifact:
    png: bytes
    sha256: str
    provider_raw_sha256: str
    provider_task_id: str
    body_module_id: str
    body_module_contract_sha256: str
    source_texture_sha256: str
    source_alpha_sha256: str
    work_canvas_sha256: str
    region_map_sha256: str
    composer_version: str
    png_encoder_version: str
    coverage_report: dict[str, object]

work_canvas = _read_indexed_asset(entry, "workCanvasPath", "workCanvasSha256")
region_map = _read_indexed_asset(entry, "regionMapPath", "regionMapSha256")
task_id = client.submit_image(prompt, [work_canvas, *photos])
provider_png = client.download(state.result_url)
canonical = compose_canonical_texture(
    provider_png=provider_png,
    work_canvas_png=work_canvas,
    region_map_png=region_map,
    module_alpha=neutral_alpha,
    minimum_change_ratio=0.95,
)
_reject_standard_cat_canonical(canonical.png)
return TextureArtifact(
    png=canonical.png,
    sha256=canonical.canonical_sha256,
    provider_raw_sha256=canonical.provider_raw_sha256,
    provider_task_id=task_id,
    body_module_id=module_id,
    body_module_contract_sha256=expected_contract_hash,
    source_texture_sha256=hashlib.sha256(neutral_bytes).hexdigest(),
    source_alpha_sha256=canonical.source_alpha_sha256,
    work_canvas_sha256=hashlib.sha256(work_canvas).hexdigest(),
    region_map_sha256=hashlib.sha256(region_map).hexdigest(),
    composer_version=COMPOSER_VERSION,
    png_encoder_version=PNG_ENCODER_VERSION,
    coverage_report=canonical.coverage.to_wire(),
)
```

Prompt 明确要求输出完整不透明 RGB，不再要求 provider 保留 alpha。切换完成后删除旧透明 guide 文件和旧 `guideSha256/relativePath` 索引字段。

- [ ] **Step 4: 运行 GREEN 与 Python 全量**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_texture_compositor.py src/photo_avatar_backend/test_uv_guides.py src/photo_avatar_backend/test_atlas_pipeline.py -q
python -m pytest src/photo_avatar_backend -q
Pop-Location
```

Expected: 全部退出 0；成功测试证明 provider raw 与 canonical hash 分离。

- [ ] **Step 5: 提交 pipeline 切换**

```powershell
git add -- services/appearance-generation/src/photo_avatar_backend/pipelines.py services/appearance-generation/src/photo_avatar_backend/test_atlas_pipeline.py services/appearance-generation/src/photo_avatar_backend/assets/uv-guides
git diff --cached --check
git commit -m "feat: 确定性合成照片分身纹理"
```

---

### Task 4: 保存不可变 attempt 审计并扩展后端 wire

**Files:**
- Create: `services/appearance-generation/src/photo_avatar_backend/audit.py`
- Create: `services/appearance-generation/src/photo_avatar_backend/test_audit.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/config.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_config.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/job_store.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_job_store.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/app.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_app.py`

**Interfaces:**
- Consumes: Task 3 `TextureArtifact` 的全部 hash、版本和覆盖报告。
- Produces: `AttemptAuditV1.to_wire()`、后端 `result.audit` 和终态 `audits/<jobId>.json`。

- [ ] **Step 1: 写终态审计 RED**

在 `test_audit.py` 和 `test_job_store.py` 增加：

```python
def test_terminal_audit_survives_artifact_and_session_cleanup(tmp_path):
    store = JobStore(tmp_path, runner=SuccessfulTextureRunner())
    submitted = store.submit(_request())
    store.run_reserved(submitted.job_id)
    audit_path = tmp_path / "audits" / f"{submitted.job_id}.json"
    before = audit_path.read_bytes()

    store.delete_session(submitted.provider_session_id)

    assert audit_path.read_bytes() == before
    assert list((tmp_path / "artifacts").glob("*.png")) == []
    assert b"pngBase64" not in before
    assert b"LK888_API_KEY" not in before
```

增加失败 attempt、崩溃恢复、相同终态重复写入无操作、不同终态重复写入拒绝、未知字段拒绝，以及 app wire 只暴露安全 audit 字段的测试。

- [ ] **Step 2: 运行 RED**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_audit.py src/photo_avatar_backend/test_job_store.py src/photo_avatar_backend/test_app.py -q
Pop-Location
```

Expected: 失败显示 `audit.py`、`audits/` 和 `result.audit` 尚不存在。

- [ ] **Step 3: 实现严格审计合同**

在 `audit.py` 定义：

```python
AUDIT_SCHEMA_VERSION = 1
API_CONTRACT_VERSION = "lk888-media-generate-v1"
PRIVACY_POLICY_VERSION = "unverified"
RETENTION_POLICY = "unverified"
UPSTREAM_DELETE_API = "unsupported"

@dataclass(frozen=True)
class AttemptAuditV1:
    session_id: str
    revision: int
    attempt: int
    provider_task_id: str | None
    provider_model: str
    provider_raw_sha256: str | None
    canonical_sha256: str | None
    body_module_id: str | None
    module_contract_sha256: str | None
    source_texture_sha256: str | None
    source_alpha_sha256: str | None
    work_canvas_sha256: str | None
    region_map_sha256: str | None
    composer_version: str | None
    png_encoder_version: str | None
    coverage_report: dict[str, object] | None
    status: str
    error_code: str | None
    created_at: str
    completed_at: str

    def to_wire(self) -> dict[str, object]:
        return {
            "schemaVersion": AUDIT_SCHEMA_VERSION,
            "sessionId": self.session_id,
            "revision": self.revision,
            "attempt": self.attempt,
            "provider": "lk888",
            "providerModel": self.provider_model,
            "modelDisplayName": "GPT-image-2.0",
            "apiContractVersion": API_CONTRACT_VERSION,
            "privacyPolicyVersion": PRIVACY_POLICY_VERSION,
            "retentionPolicy": RETENTION_POLICY,
            "upstreamDeleteApi": UPSTREAM_DELETE_API,
            "providerTaskId": self.provider_task_id,
            "providerRawSha256": self.provider_raw_sha256,
            "canonicalSha256": self.canonical_sha256,
            "bodyModuleId": self.body_module_id,
            "moduleContractSha256": self.module_contract_sha256,
            "sourceTextureSha256": self.source_texture_sha256,
            "sourceAlphaSha256": self.source_alpha_sha256,
            "workCanvasSha256": self.work_canvas_sha256,
            "regionMapSha256": self.region_map_sha256,
            "composerVersion": self.composer_version,
            "pngEncoderVersion": self.png_encoder_version,
            "coverageReport": self.coverage_report,
            "status": self.status,
            "errorCode": self.error_code,
            "createdAt": self.created_at,
            "completedAt": self.completed_at,
        }
```

审计验证必须采用精确字段集合、lowercase SHA-256 和枚举值，不接收照片、prompt、URL token 或密钥字段。

- [ ] **Step 4: 原子保存终态审计并接入 app**

`JobStore` 在 terminal success/failure/cancel 时调用：

```python
def _write_terminal_audit(self, job: _Job, audit: AttemptAuditV1) -> None:
    path = self._audits_dir / f"{job.job_id}.json"
    payload = json.dumps(
        audit.to_wire(), ensure_ascii=False, sort_keys=True, separators=(",", ":")
    ).encode("utf-8")
    path.parent.mkdir(parents=True, exist_ok=True)
    if path.exists():
        if path.read_bytes() != payload:
            raise JobStoreError("terminal audit conflicts with immutable record")
        return
    temporary = path.with_name(f".{uuid4().hex}.tmp")
    try:
        temporary.write_bytes(payload)
        temporary.replace(path)
    finally:
        temporary.unlink(missing_ok=True)
```

`PipelineRunner.audit_context(request)` 必须在 provider 调用前从冻结索引和 config 返回模型、API 合同、module/work/region/source hash 与版本；`JobStore.reserve` 把该对象写入 job state 的 `auditContext`，使进程崩溃后的失败审计仍有调用前快照。成功 TextureArtifact 将 raw/canonical hash 和 coverage 合并进审计；失败 attempt 缺失的产物字段保持 `null`。

终态审计写入必须幂等：目标不存在时通过同目录临时文件原子 replace；目标已存在且字节完全相同则成功返回，字节不同则报冲突，绝不覆盖。`delete_session` 删除 job/raw/canonical 文件但不删除 `audits/`。`_job_wire` 的 textureAtlas result 增加 `audit`，其 canonical SHA 必须与顶层 `sha256` 一致。

- [ ] **Step 5: 运行 GREEN 并提交**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_audit.py src/photo_avatar_backend/test_job_store.py src/photo_avatar_backend/test_app.py -q
python -m pytest src/photo_avatar_backend -q
Pop-Location
git add -- services/appearance-generation/src/photo_avatar_backend/audit.py services/appearance-generation/src/photo_avatar_backend/test_audit.py services/appearance-generation/src/photo_avatar_backend/config.py services/appearance-generation/src/photo_avatar_backend/test_config.py services/appearance-generation/src/photo_avatar_backend/job_store.py services/appearance-generation/src/photo_avatar_backend/test_job_store.py services/appearance-generation/src/photo_avatar_backend/app.py services/appearance-generation/src/photo_avatar_backend/test_app.py
git diff --cached --check
git commit -m "feat: 保存照片分身纹理审计"
```

---

### Task 5: 桌面端严格解析并持久化 canonical 审计

**Files:**
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/provider.rs:126-225`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/domain.rs`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/manager.rs:534-594`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/store.rs:52-64`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/store.rs:551-663`
- Modify: `apps/desktop/src-tauri/src/storage/migrate.rs`
- Modify: `apps/desktop/src-tauri/src/lib.rs`

**Interfaces:**
- Consumes: Task 4 后端 `result.audit`。
- Produces: Rust `CanonicalTextureAuditV1` 和与 `photo_avatar_artifacts` 同 revision 的 `audit_json`。

- [ ] **Step 1: 写 Rust wire 与迁移 RED**

在 `provider.rs` 测试中要求未知字段、错误 canonical hash、错误 module ID、非 lowercase hash 被拒绝；在 `migrate.rs` 测试中要求新迁移增加 nullable `audit_json`，旧数据库可无损升级。

```rust
let state: RemoteJobState = serde_json::from_value(json!({
    "state": "succeeded",
    "result": {
        "resultType": "textureAtlas",
        "artifactUrl": "https://backend.test/artifact.png",
        "sha256": canonical_sha,
        "width": 2048,
        "height": 2048,
        "audit": valid_texture_audit(canonical_sha)
    },
    "error": null
})).unwrap();
assert!(matches!(state.result, Some(ProviderStepResult::TextureAtlas { .. })));
```

- [ ] **Step 2: 运行 RED**

```powershell
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml creation::photo_avatar::provider::tests::texture_atlas_audit -- --nocapture
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml storage::migrate::tests::v11_adds_photo_avatar_artifact_audit -- --nocapture
```

Expected: 编译或断言失败，明确缺少 `CanonicalTextureAuditV1` 与 v11 列。

- [ ] **Step 3: 实现严格 Rust 类型**

在 `domain.rs` 增加共享类型，`provider.rs` 只负责 wire 反序列化和固定值验证：

```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
#[serde(rename_all = "camelCase", deny_unknown_fields)]
pub struct CanonicalTextureAuditV1 {
    pub schema_version: u32,
    pub session_id: String,
    pub revision: u32,
    pub attempt: u8,
    pub provider: String,
    pub provider_model: String,
    pub model_display_name: String,
    pub api_contract_version: String,
    pub privacy_policy_version: String,
    pub retention_policy: String,
    pub upstream_delete_api: String,
    pub provider_task_id: String,
    pub provider_raw_sha256: String,
    pub canonical_sha256: String,
    pub body_module_id: String,
    pub module_contract_sha256: String,
    pub source_texture_sha256: String,
    pub source_alpha_sha256: String,
    pub work_canvas_sha256: String,
    pub region_map_sha256: String,
    pub composer_version: String,
    pub png_encoder_version: String,
    pub coverage_report: serde_json::Value,
    pub status: String,
    pub error_code: Option<String>,
    pub created_at: String,
    pub completed_at: String,
}
```

`ProviderStepResult::TextureAtlas` 增加 `audit`。Provider 层验证 `schemaVersion == 1`、provider/model/API 固定值、所有 hash lowercase、`status == "succeeded"`、`errorCode == null`、`audit.canonicalSha256 == result.sha256`；Manager 再用当前 run 验证 session/revision/attempt。

- [ ] **Step 4: 迁移并与 artifact 原子绑定**

在 `MIGRATIONS` 末尾加入 v11：

```sql
ALTER TABLE photo_avatar_artifacts ADD COLUMN audit_json TEXT;
```

`ProviderArtifact` 增加 `audit_json: Option<String>`。TextureAtlas commit 必须保存 canonical audit；PreviewPackage 允许 `None`。Manager 在 commit 前验证 audit body module 与当前 profile 一致、module contract hash 与当次请求一致；失败时调用 `fail_attempt`，不得进入 `buildV5`。

- [ ] **Step 5: 运行 GREEN、迁移全量并提交**

```powershell
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml creation::photo_avatar -- --nocapture
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml storage::migrate -- --nocapture
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml
cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --check
git add -- apps/desktop/src-tauri/src/creation/photo_avatar/provider.rs apps/desktop/src-tauri/src/creation/photo_avatar/domain.rs apps/desktop/src-tauri/src/creation/photo_avatar/manager.rs apps/desktop/src-tauri/src/creation/photo_avatar/store.rs apps/desktop/src-tauri/src/storage/migrate.rs apps/desktop/src-tauri/src/lib.rs
git diff --cached --check
git commit -m "feat: 持久化照片分身canonical审计"
```

---

### Task 6: 强化 Rust builder 的独立 canonical 复验

**Files:**
- Modify: `apps/desktop/src-tauri/src/runtime_assets/photo_avatar_builder.rs:70-180`
- Modify: `apps/desktop/src-tauri/src/runtime_assets/photo_avatar_builder.rs:390-460`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/manager.rs:213-248`

**Interfaces:**
- Consumes: Task 5 `CanonicalTextureAuditV1` 和 canonical PNG。
- Produces: `BuildPhotoAvatarRequest.texture_audit`；构建、复开、展示和安装均执行同一独立验证。

- [ ] **Step 1: 写透明 RGB、审计 hash 和同步篡改 RED**

在 builder 测试中增加：

```rust
#[test]
fn builder_rejects_hidden_rgb_and_audit_even_when_manifest_hash_was_updated() {
    let fixture = Fixture::new();
    let mut request = fixture.request("body-balanced-v1", 73);
    request.texture_png = fixture.atlas_with_hidden_rgb_outside_alpha();
    request.texture_sha256 = sha256(&request.texture_png);
    request.texture_audit.canonical_sha256 = request.texture_sha256.clone();

    let error = fixture.builder().build_preview(request).unwrap_err();

    assert!(error.contains("transparent RGB"), "{error}");
}
```

再覆盖 source alpha hash 错误、module contract hash 错误、canonical hash 错误，以及构建后同步改 texture/manifest hash 仍被 `validate_preview` 拒绝。

- [ ] **Step 2: 运行 RED**

```powershell
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml runtime_assets::photo_avatar_builder::tests::builder_rejects_hidden_rgb_and_audit_even_when_manifest_hash_was_updated -- --nocapture
```

Expected: 编译失败或测试失败，因为 BuildPhotoAvatarRequest 尚未携带审计或未检查透明 RGB。

- [ ] **Step 3: 实现统一 canonical 验证函数**

将验证集中到一个私有函数，所有入口复用：

```rust
fn validate_canonical_texture(
    texture_png: &[u8],
    declared_sha256: &str,
    audit: &CanonicalTextureAuditV1,
    module: &BodyModuleManifest,
    module_dir: &Path,
) -> Result<(), String> {
    if sha256_hex(texture_png) != declared_sha256
        || audit.canonical_sha256 != declared_sha256
    {
        return Err("canonical texture hash mismatch".into());
    }
    let texture = decode_rgba(texture_png, "canonical texture")?;
    let neutral = decode_rgba(
        &read_regular_file(&module_dir.join(&module.files["neutralTexture"]))?,
        "body module neutral texture",
    )?;
    for (canonical, source) in texture.pixels().zip(neutral.pixels()) {
        if canonical[3] != source[3] {
            return Err("canonical alpha does not match body module".into());
        }
        if canonical[3] == 0 && canonical.0[..3] != [0, 0, 0] {
            return Err("canonical transparent RGB must be zero".into());
        }
    }
    let alpha: Vec<u8> = texture.pixels().map(|pixel| pixel[3]).collect();
    if sha256_hex(&alpha) != audit.source_alpha_sha256 {
        return Err("canonical source alpha hash mismatch".into());
    }
    Ok(())
}
```

Manager 从 artifact `audit_json` 严格反序列化后传入 builder。

- [ ] **Step 4: 覆盖四条入口并运行 GREEN**

确保 `build_preview`、`validate_preview`、`preview_manifest/preview_file` 展示和 `install_preview` 都走 `validate_canonical_texture`。

```powershell
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml runtime_assets::photo_avatar_builder -- --nocapture
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml creation::photo_avatar::manager -- --nocapture
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml
```

Expected: 全部退出 0；同步篡改无法绕过模块逐像素复验。

- [ ] **Step 5: 提交 Rust 独立复验**

```powershell
git add -- apps/desktop/src-tauri/src/runtime_assets/photo_avatar_builder.rs apps/desktop/src-tauri/src/creation/photo_avatar/manager.rs
git diff --cached --check
git commit -m "fix: 独立复验照片分身canonical纹理"
```

---

### Task 7: 完成跨语言 fake provider 与崩溃恢复闭环

**Files:**
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_desktop_contract_fixture.py`
- Modify: `services/appearance-generation/src/photo_avatar_backend/test_job_store.py`
- Modify: `apps/desktop/src-tauri/src/creation/photo_avatar/provider.rs`
- Modify: `apps/desktop/src-tauri/src/lib.rs`
- Modify: `apps/desktop/src/settings/photo-avatar-creation-view.test.ts`

**Interfaces:**
- Consumes: Task 3 canonical pipeline、Task 4 audit wire、Task 5/6 Rust 验证。
- Produces: 一条从 opaque provider RGB 到 canonical、builder、runtime check、cleanup 的无网络集成 fixture。

- [ ] **Step 1: 写跨语言 RED**

Python fixture handler 返回全不透明 provider RGB，Rust 侧断言：

```rust
let ProviderStepResult::TextureAtlas { sha256, audit, .. } = texture.result.unwrap() else {
    panic!("expected canonical texture result");
};
assert_eq!(sha256, audit.canonical_sha256);
assert_ne!(audit.provider_raw_sha256, audit.canonical_sha256);
assert_eq!(audit.composer_version, "deterministic-alpha-v1");
```

增加 provider 成功后、canonical 写入前模拟崩溃的恢复测试；恢复后只能从已验证输入重新合成，不能把 raw 当 canonical。

- [ ] **Step 2: 运行 RED**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_desktop_contract_fixture.py src/photo_avatar_backend/test_job_store.py -q
Pop-Location
```

Expected: 失败显示 fixture wire 没有 audit 或 fake output 仍是旧 alpha 结构。

- [ ] **Step 3: 更新 fake provider 边界**

Python fake image client 返回全不透明 RGB；后端负责 canonical。Rust `FakePhotoAvatarProvider` 作为桌面单元测试替身仍返回 canonical PNG，但必须同时构造合法 `CanonicalTextureAuditV1`，且 provider raw hash 与 canonical hash 不同。

前端重叠 poll 测试必须保留：同 `sessionId:revision` 只挂载一个 renderer，迟到旧 revision handle 只销毁自己。

- [ ] **Step 4: 运行跨语言、前端与全量 GREEN**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend/test_desktop_contract_fixture.py src/photo_avatar_backend/test_job_store.py -q
python -m pytest src/photo_avatar_backend -q
Pop-Location
npm --prefix apps/desktop test -- --run src/settings/photo-avatar-creation-view.test.ts src/settings.test.ts
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml
```

Expected: 全部退出 0；测试状态中没有 raw artifact 直通 builder。

- [ ] **Step 5: 提交跨语言闭环**

```powershell
git add -- services/appearance-generation/src/photo_avatar_backend/test_desktop_contract_fixture.py services/appearance-generation/src/photo_avatar_backend/test_job_store.py apps/desktop/src-tauri/src/creation/photo_avatar/provider.rs apps/desktop/src-tauri/src/lib.rs apps/desktop/src/settings/photo-avatar-creation-view.test.ts
git diff --cached --check
git commit -m "test: 验证照片分身canonical跨语言闭环"
```

---

### Task 8: 建立 revision 13 只读回归与 fake UI 离线放行门

**Files:**
- Create: `scripts/验证照片分身确定性纹理合成.ps1`
- Modify: `scripts/录制照片分身动作证据.ps1`
- Create: `docs/验证记录/照片分身确定性纹理合成离线验收.md`
- Create: `docs/验证记录/证据/照片分身确定性纹理合成-fake/**`

**Interfaces:**
- Consumes: Task 1-7 的自动化和真实 `Live2DRenderer` fake fixture。
- Produces: 无真实 provider 调用的离线放行记录；只有该记录通过，Task 9 才能执行真实样本。

- [ ] **Step 1: 写 revision 13 只读验证 RED**

PowerShell 脚本必须固定 artifact 路径与 SHA，仅读取并复算：

```powershell
$revision13 = 'services/appearance-generation/output/task-11-runtime/artifacts/a3eb6a61778c48cd85136aa4e5f67f83.png'
$expected = '5503e493829423f316a348c9c96a54681383d0efb7a76417c9fa2ac0c653e9a0'
if ((Get-FileHash -LiteralPath $revision13 -Algorithm SHA256).Hash.ToLowerInvariant() -ne $expected) {
    throw 'revision 13 artifact changed'
}
```

随后调用 Python 只读分析，断言 alpha mismatch 为 `3,390,353`、模块内透明缺失为 `54,911`、缺失 RGB 全零，并断言旧 raw artifact 不能通过 `compose_canonical_texture` 的 opaque provider 输入门。

- [ ] **Step 2: 运行 RED**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/验证照片分身确定性纹理合成.ps1 -Revision13Only
```

Expected: 脚本尚不存在而失败；revision 13 文件和 job JSON 不发生时间戳变化。

- [ ] **Step 3: 实现两分钟受控的离线总门**

脚本依次执行 Python focused/full、前端 focused/full、Rust focused/full、typecheck、build、fmt、diff check。每个长命令通过 `Start-Process -PassThru -RedirectStandardOutput/-RedirectStandardError` 获得真实 PID；120 秒未退出时读取日志尾部与产物时间戳，有增量才继续，否则停止进程并失败。

脚本不得接受跳过 alpha、跳过 revision13 或跳过真实 renderer 的参数。

- [ ] **Step 4: 录制 fake UI 八动作与两种打断**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/录制照片分身动作证据.ps1 -Fixture balanced-success -Output 'docs/验证记录/证据/照片分身确定性纹理合成-fake'
```

Expected: `证据索引.json` 记录 v5、`cat-spatial-live2d-v1`、runtime check 通过、每个动作 neutral/peak/fallback 像素差大于 20、两种打断截图非空；屏幕上只有一只 fixture 宠物。

- [ ] **Step 5: 人工检查并写离线验收**

逐张检查脸、眼睑、耳根、胸腹、尾根、内部孔洞、白线和纹理错岛。文档明确写：fake 只证明 canonical 链路和动态，不证明真实宠物身份相似；revision 13 仍失败且不可安装。

- [ ] **Step 6: 提交离线放行证据**

```powershell
git add -- scripts/验证照片分身确定性纹理合成.ps1 scripts/录制照片分身动作证据.ps1 docs/验证记录/照片分身确定性纹理合成离线验收.md docs/验证记录/证据/照片分身确定性纹理合成-fake
git diff --cached --check
git commit -m "test: 验收照片分身确定性纹理合成"
```

---

### Task 9: 冻结并执行真实 20 样本技术成功率门

**Files:**
- Create: `scripts/照片分身真实20样本验收.ps1`
- Create: `docs/验证记录/证据/照片分身真实20样本/样本矩阵.json`
- Create: `docs/验证记录/证据/照片分身真实20样本/结果.json`
- Create: `docs/验证记录/证据/照片分身真实20样本/动态证据/**`
- Create: `docs/验证记录/照片分身真实20样本验收.md`

**Interfaces:**
- Consumes: 用户授权的 `01-photos` 八张宠物照片、Task 8 离线放行提交、运行中的桌面应用和受控后端。
- Produces: 不可删样的 20 revision 矩阵、attempt 结果、技术成功率和人工视觉结论。

- [ ] **Step 1: 写矩阵冻结与 DryRun RED**

脚本参数固定为：

```powershell
param(
    [Parameter(Mandatory)][string]$PhotoRoot,
    [Parameter(Mandatory)][string]$AuthorizationRecord,
    [switch]$DryRun,
    [switch]$Execute
)
if ($DryRun -eq $Execute) { throw 'choose exactly one of DryRun or Execute' }
```

脚本要求 PhotoRoot 恰有八张可解码宠物图片，按 ordinal 计算 SHA-256。20 个样本固定为 1、2、4、8 张照片各 5 个，使用轮转起点保证八张照片都出现；每个样本写入 `sampleId`、photo ordinals/hashes、code commit、模型、工作画布 hash、区域图 hash、module contract hash 和 `minimumChangeRatio: 0.95`。

- [ ] **Step 2: 运行 DryRun RED**

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/照片分身真实20样本验收.ps1 -PhotoRoot $env:PHOTO_AVATAR_TEST_PHOTO_ROOT -AuthorizationRecord $env:PHOTO_AVATAR_AUTHORIZATION_RECORD -DryRun
```

Expected: 脚本尚不存在而失败；没有网络请求、backend job 或新 revision。

- [ ] **Step 3: 实现不触网 DryRun**

授权记录必须是 UTF-8 JSON，精确包含：

```json
{
  "authorized": true,
  "material": "non-human-pet",
  "provider": "lk888.ai",
  "model": "gpt-image-2",
  "sampleCount": 20,
  "maximumAttemptsPerSample": 3
}
```

DryRun 只写矩阵，随后再次读取并验证 20 个唯一 sample、1/2/4/8 分布各 5 个、八张 photo hash 均出现、HEAD 与 Task 8 放行提交一致。

- [ ] **Step 4: 审阅冻结矩阵后执行真实 UI 样本**

Execute 模式必须先检查：Task 8 文档为通过、本计划所有 implementation paths 相对 HEAD 无未提交变化、backend/desktop PID 存在、8787/1420 端口监听、授权记录与矩阵 hash 未变化。受保护的既有脏路径可以继续存在，但不得在执行期间变化。它通过桌面真实创建入口逐样本创建 revision，不从命令行直接调用 provider；每个 sample 最多三次 `renderTextureAtlas` attempt。

Run:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/照片分身真实20样本验收.ps1 -PhotoRoot $env:PHOTO_AVATAR_TEST_PHOTO_ROOT -AuthorizationRecord $env:PHOTO_AVATAR_AUTHORIZATION_RECORD -Execute
```

Expected: 生成 20 个不可删样结果；每个 attempt 有 task ID、耗时、错误码、provider raw hash、canonical hash、模块/画布/区域 hash、coverage report 和 Live2D load 结果。Provider raw 文件在终态清理，审计 hash 保留。

- [ ] **Step 5: 计算成功率并执行人工视觉门**

脚本只按冻结的 20 个 sample 计算：

```powershell
$firstPass = @($results | Where-Object { $_.technicalSuccessAttempt -eq 1 }).Count
$threePass = @($results | Where-Object { $_.technicalSuccessAttempt -in 1,2,3 }).Count
if ($firstPass -lt 17) { throw "first-attempt gate failed: $firstPass/20" }
if ($threePass -lt 19) { throw "three-attempt gate failed: $threePass/20" }
```

对每个技术成功 sample 录制真实 renderer 中性姿态、八动作峰值/回退和两种打断。人工逐项标记脸型、毛色、花纹、眼睛、显著特征、耳根、胸腹、尾根、孔洞、白线和错岛。只有视觉通过的 revision 可进入候选；最终接受并安装的 revision 必须全部视觉通过。

- [ ] **Step 6: 提交脱敏真实验收记录**

不得提交原照片、provider raw PNG、API key、base64 或含凭据日志。提交矩阵中的照片仅保留 hash/ordinal，动态证据只保留 canonical 预览截图和审计 JSON。

```powershell
git add -- scripts/照片分身真实20样本验收.ps1 docs/验证记录/证据/照片分身真实20样本 docs/验证记录/照片分身真实20样本验收.md
git diff --cached --check
git commit -m "test: 验收照片分身真实20样本"
```

---

### Task 10: 最终全量复审与阶段放行

**Files:**
- Modify: `docs/验证记录/照片分身确定性纹理合成离线验收.md`
- Modify: `docs/验证记录/照片分身真实20样本验收.md`
- Create: `docs/验证记录/照片分身确定性纹理合成阶段审查.md`

**Interfaces:**
- Consumes: Task 0-9 的提交、测试、fake UI 和真实样本证据。
- Produces: 是否允许继续照片分身后续阶段的唯一阶段结论。

- [ ] **Step 1: 运行最终全量验证**

```powershell
Push-Location services/appearance-generation
python -m pytest src/photo_avatar_backend -q
Pop-Location
npm --prefix apps/desktop test -- --run
npm --prefix apps/desktop run typecheck
npm --prefix apps/desktop run build
cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml
cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml --check
git diff --check
git status --short
```

Expected: 所有命令退出 0；本计划 implementation paths clean；仅允许全局约束中列明的受保护既有脏路径继续存在且内容未变化。

- [ ] **Step 2: 做独立规格与代码复审**

复审必须逐条回答：

- Provider 是否只能贡献 RGB，且任意 provider alpha 都不会进入 canonical。
- 六类拓扑是否逐像素通过，而不是只验证当前三个相同 alpha module。
- raw/canonical/module/work/region hash 是否可追溯且不含照片。
- backend cleanup 是否删除 raw/canonical 临时文件但保留终态审计。
- Rust 是否在构建、复开、展示、安装四条路径独立复验。
- revision 13 是否原字节保留且仍被拒绝。
- 是否达到 `17/20`、`19/20` 和最终接受项视觉 `100%`。
- 是否不存在第二只宠物、破碎宠物、标准猫或静态降级。

任一答案为否，阶段结论必须为失败并返回对应 Task 修复。

- [ ] **Step 3: 写阶段审查并提交**

阶段审查记录每个提交 hash、测试命令与结果、20 样本分母、失败样本原因、人工视觉结论、revision 13 hash、cleanup 状态和是否放行。不能只写“已通过”。

```powershell
git add -- docs/验证记录/照片分身确定性纹理合成离线验收.md docs/验证记录/照片分身真实20样本验收.md docs/验证记录/照片分身确定性纹理合成阶段审查.md
git diff --cached --check
git commit -m "docs: 放行照片分身确定性纹理合成阶段"
```

## 完成判据

- 当前已复审修复先形成独立基线提交，新合成工作不混入旧 diff。
- Provider 第一输入全不透明，只接受完整 RGB 或全不透明 RGBA。
- 每个必需区域变更率至少 95%，低覆盖和标准猫复制关闭。
- Canonical alpha 逐像素等于模块，完全透明像素 RGB 全零，相同输入产生相同 hash。
- Python backend 与 Rust builder 独立验证，raw artifact 永不直通桌面安装。
- 审计保留 provider raw/canonical/module/work/region hash、版本和覆盖报告，不保存照片、prompt 或密钥。
- revision 13 原字节、job 和失败事实不可变，不能被新合成器伪装成功。
- 六类跨拓扑 fixture、跨语言 fake、崩溃恢复、八动作和两种打断全部通过。
- 真实 20 样本首次技术成功至少 17，三次内至少 19，最终接受项视觉 100% 通过。
- 全量 Python、TypeScript、Rust、typecheck、build、fmt 和 diff check 通过，阶段审查明确放行。
