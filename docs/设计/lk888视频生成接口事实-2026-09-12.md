# lk888 视频生成接口事实（实测）

> 2026-09-12。来源 = 直接调 `https://api.lk888.ai/v1/skills/models/*` 与 `/pricing`（只读接口，未产生费用）。
> 目的 = 给「上传照片 → 生成视频 → 抠像 → 帧序列」唯一路线提供唯一可信的接口依据。

---

## 一、模型名：仓库里那份缓存已经过期，别信

| 来源 | 内容 | 判定 |
|---|---|---|
| 仓库根 `v1-skills-models.json` | 135 个模型，无 `seedance-2.0-guanfang` | ⚠️ **过期** |
| 线上 `GET /v1/skills/models` | **167 个**模型，含全部 seedance 系列 | ✅ 以此为准 |
| 仓库根 `models.json` | `seedance-2-0-official` 等（`{success,data,...}` 结构） | ❌ **不是本站协议**，模型名对不上 lk888，不可用于 `/v1/media/generate` |

线上 seedance 系列共 16 个。**老王指定的 `seedance-2.0-guanfang`（SD 2.0 Official Full-Spec）真实存在且本 key 可用。**

常用四个：

| 模型名 | 显示名 | 计费 |
|---|---|---|
| `seedance-2.0-guanfang` | SD 2.0 官方全功能版 | 按 token |
| `seedance-2.0-guanfang-anmiao` | SD 2.0 官方全功能版 · 按秒 | 按秒（可预估） |
| `seedance-2.0-anmiao` | SD 2.0 首尾帧 · 按秒 | 按秒 |
| `kwvideo-v2` | SD 2.0 首尾帧（XH 渠道） | 按 token |

---

## 二、每个模型有两条协议；我们走「本站媒体协议」

`seedance-2.0-guanfang` 的 `protocols` 数组给出两条：

| | 火山方舟格式（默认） | **本站媒体协议（我们用这条）** |
|---|---|---|
| base | `{domain}/api/v3` | `{domain}` |
| 创建 | `POST /api/v3/contents/generations/tasks` | **`POST /v1/media/generate`** |
| 查询 | `GET /api/v3/contents/generations/tasks/{id}` | **`GET /v1/skills/task-status?task_id=`** |
| model 字段 | `doubao-seedance-2-0-260128`（`-fast-` / `-mini-`） | **`seedance-2.0-guanfang`** |
| 请求体 | 火山原文（content 按 role 排） | 本站 `{model, prompt, params}` |

→ **走本站协议不需要碰火山格式**，现有 `photo_avatar_backend.lk888_client` 的提交/轮询/下载骨架可直接复用。

---

## 三、`seedance-2.0-guanfang` 的参数表（本站协议）

| 参数 | 类型 | 必填 | 取值 | 说明 |
|---|---|---|---|---|
| `mode` | segment | 否 | `shouweizhen` / `cankaosheng` | 首尾帧 / 参考生。**这是我们的图生视频入口** |
| `version` | select | **是** | `Mini` / `快速` / `标准` | 映射 Ark model 字段 |
| `duration` | select | **是** | `auto` / `4`…`15` | 按秒版**没有 `auto`**，必须整数 |
| `resolution` | select | **是** | `480p` / `720p` / `1080p` / `4K` | `1080p`/`4K` 仅 `version=标准` |
| `aspect_ratio` | select | 否 | `adaptive` / `16:9` / `4:3` / `1:1` / `3:4` / `9:16` / `21:9` | 我们要 `1:1` |
| `images` | upload | 否 | 数组 | 按 mode 解读：首帧 / 首尾帧 / 参考图（≤9） |
| `_quan_neng_mode` | select | 否 | `quan_neng` / `edit` / `extend` | 全能参考/改片/续写，本路线不用 |
| `image_url` / `video_url` / `audio_url` | upload | 否 | 数组 | 全能参考用；**视频只能传公网 URL** |

**图片可 base64 内联**（仅 `POST /v1/media/generate` 支持）：单文件解码后 ≤10MB、单次合计 ≤30MB、请求体 ≤50MB。1024×1024 绿幕首帧 PNG 约 1~2MB，安全。

---

## 四、价格与本 key 余额（会决定能不能真跑）

`GET /v1/skills/balance` → **`balance = 2.7 算力`**（`used = 303.6`）。

按秒计费版（`官方特惠` 渠道，activated）单价：

| 配置 | 算力/秒 | 5s 合计 |
|---|---|---|
| 标准 + 720p | 0.7776 | 3.89 ❌ 超余额 |
| 快速 + 720p | 0.6192 | 3.10 ❌ |
| Mini + 720p | 0.3888 | 1.94 ⚠️ 勉强 |
| **Mini + 480p** | **0.1728** | **0.86 ✅** |

按 token 版（`官方特惠`）只有单价、无法预估单条总价 → **首次真跑建议用按秒 + Mini + 480p + 4s（≈0.69 算力）**，留出余额给重跑。

---

## 五、既有资产的实测规格（对齐目标）

| 文件 | 分辨率 | fps | 时长 | 帧数 |
|---|---|---|---|---|
| 毛砌墙-1比1-640-呼吸摇尾-5s.mp4 | 640×640 | 24 | 5.04s | 121 |
| 建国 组合循环-原始.mp4 | 640×640 | 24 | 12.04s | 289 |
| 建国 grab-release-原始-v5.mp4 | 640×640 | 24 | 5.04s | 121 |

→ 历史产物统一 **640×640 / 24fps**，画幅 `1:1`。

---

## 六、这次落地的能力

- `photo_avatar_backend/config.py`：新增 `video_model`（`LK888_VIDEO_MODEL`，默认 `seedance-2.0-guanfang`，沿用 `_fixed_model` 白名单写法）。
- `photo_avatar_backend/lk888_client.py`：
  - 抽出 `_submit(model, prompt, params)`，`submit_image` 与新 `submit_video` 共用同一套信封校验（错误分类/诊断不变）。
  - 新增 `submit_video(prompt, *, images, version, duration, resolution, aspect_ratio, mode)`。
  - `download` 抽出 `_download_artifact(url, kind)`，新增 `download_video`（认 `video/*` 或 `application/octet-stream`，并要求 `ftyp` 魔数；上限 256MiB；仍禁重定向、强制 HTTPS）。PNG 路径行为不变（20MiB）。
  - 轮询仍用 `poll_image`（状态接口与模型无关，已加注释）。
- `scripts/poc_生成绿幕视频.py`：首帧 + 提示词 → 视频，补上「母版 → 绿幕首帧 → **绿幕视频** → 抠像」链路缺的第 3 环。提示词按 `【主提示词】/【负向提示词】` 分节解析（已在 3 份真实提示词上验证）。

---

## 七、已知风险 / 未验证

1. **`mode` 的参数类型是 `segment`**，不是文档里介绍过的 `select`/`upload`/`input` 之一 —— 是否被服务端接受**未经真跑验证**。
2. 网页上有的旋钮 API 没有对应参数：**运动强度**、**音频开关**、**独立负向提示词字段**。建国那批视频是网页手工做的，**API 出来的结果可能与之不同**；脚本目前把负向词追加进主提示词作为替代。
3. 按 token 版单价无法预估总价，容易一次超预算。
4. 全链路（视频 → 抠像 → 帧序列打包 → 安装）仍未打通；本次只补了「视频生成」这一环。
