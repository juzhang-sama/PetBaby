# 动作精修流程（标准化）

> 2026-09-20 定。对象：写实风新宠的 one-shot / 交互动作（`yawn` / `lick` / `grab-release`）。
>
> **为什么要这份文档**：内置 04/05/06 的动作支是**人工精修过的** ——
> yawn/lick 只有 119~121 帧、grab-release 只有 81 帧且带 `holdRange: [30, 59]`；
> 而新产线产出的是**整支 12 秒原封不动**（287 帧、无 `holdRange`）。
> 后果就是 pet-7228 的拎起完全不可用（拖拽时整支循环、松手从第 0 帧重播）。
> 本文把「建国身上那道人工工序」变成**固定流水线**，不再依赖某个人的手感。

---

## 零、「效果一样」是组数，不是形容词

对标的全部内容 = **把内置宠的 manifest 参数当规格抄下来**。人工的**手法**不用管，
人工产物的**数字**必须对齐：

| 动作 | 内置 05 建国 | pet-7228 现状（2026-09-20 精修后） |
|---|---|---|
| `idle-combo` | 288 帧 × 42ms = **12.1s**（loop） | 287 帧 = 12.1s ✅ |
| `yawn` | 119 帧 × 42ms = **5.0s** | **119 帧 = 5.0s** ✅（原 287 帧 12.1s，差 2.4×） |
| `lick` | 119 帧 × 42ms = **5.0s** | **119 帧 = 5.0s** ✅（原 287 帧 12.1s，差 2.4×） |
| `grab-release` | **81 帧 = 3.4s** + `holdRange[30,59]`（窗口 30 帧 ≈1.26s） | **81 帧 = 3.4s** + `holdRange[35,57]`（窗口 23 帧 ≈0.97s）✅（原 287 帧 12.1s，差 3.5×） |

（内置 04/06 同量级：yawn/lick 119~121 帧。`06-guodong` 也一样。）

**读法**：
- 硬指标 = **帧数 × 帧时长 = 单次播放时长**。用户点一下打哈欠，建国 5 秒结束，7228 播 12 秒 ——
  「效果不一样」就是这么来的，跟画面好不好看无关。
- `idle-combo` 之所以天生对齐，是因为它本来就是「12 秒的一段呼吸循环」；
  **三支动作全都不对齐**，因为产线视频固定 12 秒，而人工版本是把这 12 秒里
  **有用的那几秒裁出来 + 把动作压回正常速度**。⇒ **`frameRange` / `frameTarget` 不是可选项，
  是「效果对标」的必需项**（2026-09-20 已实现，见「二、③ 定」）。
- `holdRange` 的**窗口宽度**也在规格里（建国 30 帧 ≈1.26s vs 我们 66 帧 ≈2.8s）：
  窗口太宽，拖久了会看到它在循环里「晃」。

**两把尺子**（都是只读、0 算力）：

| 工具 | 量什么 |
|---|---|
| `output/对标内置宠-量化表.py` | 所有内置宠 + 已装新宠的 帧数 / 帧时长 / 单次时长 / `holdRange` / 是否进 `idleSchedule` |
| `output/精修-分段节奏表.py <frames目录> --hold lo hi` | 单支动作切成**变姿 / 保持 / 收回 / 废料**四段，各给帧数与秒数 + 画面变化量曲线 |

---

## 一、精修 = 3 个决定

| # | 决定 | 落点 | 谁做 | 现状 |
|---|---|---|---|---|
| 1 | 留哪一段 + 压到多少帧（`frameRange` / `frameTarget`） | `actions/<id>.json` → 打包层 | 人看节奏表定稿 | ✅ **已实现**（2026-09-20） |
| 2 | 保持窗口（`holdRange`） | `actions/<id>.json` | **只能人定稿** | ✅ 键已有，服务只读不猜 |
| 3 | 这支素材要不要重出 | ——（人的决定） | 人看联络表 + 指标 | 靠肉眼 |

**三个决定的共同点：都发生在「视频已生成、还没打包」之间 ⇒ 重打包是 0 算力 ⇒ 可以反复调，不花钱。**

这是整条流程能成立的经济基础：**花钱的只有「生成」，之后想怎么改就怎么改。**

⚠️ **「机器出候选，人点一下」是这条流程的固定形状**：机器已经能给候选
（`精修-量动作区间.py` 的稳定段、`--sheet` 的白底联络表），
人的活被压到「从候选里挑一个」——但**不能压到零**，
因为「这段姿态是『被拎起』还是『收回后端坐』」机器分不出来（09-20 实证：自动候选挑的正是端坐段）。

🔴 **顺序必须是「先裁区间 → 再标保持窗口」**：`holdRange` 是**帧下标**，
一旦裁掉前面一段，所有下标都要平移。09-20 先标了 `[104,169]`，
等 `frameRange` 做出来之后**这个值必须重算**（否则指向错误的位置）。
⇒ 已经装了 `holdRange` 的动作，做裁剪时要当成「要重标」的动作处理。

---

## 二、流水线

### ① 生成视频 —— 花钱，每支一次

- 入口：`generateMotionSource`（服务侧）或 `output/真跑验收-重出grab-release-7228.py --confirm`（单支重出探针）。
- 已有 mp4 一律复用（`_action_videos` 扫 scratch 的 `actions/`）⇒ 只补缺的那支。
- ⚠️ **产线锁死 `VIDEO_DURATION = "12"`**（`frame_pipeline.py`），
  而动作脚本原本只写「约 5 秒」⇒ 模型被迫自己填满多出来的 7 秒 ⇒ **加戏**。
  **所以动作脚本的时间轴必须按 12 秒写**（见下面「四、已验证记录」）。

### ② 量 —— 0 算力，自动

```bash
<python> output/精修-量动作区间.py <动作.mp4>
```

产出（全部是**事实**，不是结论）：

- 逐帧 bbox 曲线（`y0/y1/x0/x1`）与头顶余量统计；
- 首末帧与首帧的差（判「有没有收回去」）；
- **稳定段候选**：相邻帧 bbox 每个边都 ≤4px 的连续段（默认 ≥60 帧），
  按长度排序给前 5 段，并给出最长那段的 `holdRange` 建议值。

判据说明：

- 绿幕判据用 `g - max(r, b) < 40`（**不能**写死 `(0,255,0)` —— 480p 实测是 `(3,178,4)`）；
  传**抠像后的 `frames` 目录**时改用 alpha 阈值（那才是运行时会画的像素）。
- 🔴 **「稳定」的判据是「窗口内极差」，不是「相邻帧变化」**。2026-09-20 实测教训：
  先用相邻帧判据跑出一段 `f84~f203`（120 帧，看着很长），但它是**缓慢漂移** ——
  每步只变 1~2px、首尾却差 45px ⇒ 播放器从 `f203` 硬切回 `f84` 时会**跳变**。
  换成极差判据（窗口内**任意两帧**的 bbox 每边都 ≤ 阈值）后，同一支素材给出
  `f104~f169`（66 帧 ≈ 2.8 秒，接缝差 7px）—— 这才是真能循环的窗口。
  （对照：内置建国的 `[30, 59]` 是 30 帧 ≈ 1.26 秒，新窗口比它还宽裕一倍。）
- ⚠️ **别信服务侧那个「最窄段」候选**：`frames/hold_range.py` 的自动候选是
  「宽度 ≤ 最窄×1.15 的最长连续段」，2026-09-20 实测它挑的是**尾部「收回后的端坐」**
  （确实最窄、也稳定，但语义是「没被拎起」）⇒ 拿它当 `holdRange`，拖拽时循环的是端坐姿态。
  **稳定 ≠ 该拿来循环，语义必须人眼在联络表上确认**（`--sheet` 出图）。

### ③ 定 —— 0 算力，人工

先在**动作配置**里写三个数（`assets/motion-prompts/actions/<id>.json`）：

```jsonc
{
  "holdRange":   [104, 169],   // 仅交互动作需要：悬空保持窗口（帧下标闭区间）
  "frameRange":  [0, 236],     // 留哪一段（丢掉收回完成后的静止废料）
  "frameTarget": 81            // 把这一段**均匀重采样**成多少帧
}
```

- `holdRange`：**必须由人填**（理由见「三、关键契约」）。没有它 = 拎起不可用。
- `frameRange` / `frameTarget`：**整支压缩到内置宠的节奏**（`frameTarget` 只能变小，不能变大）。
- 🔴 **三个数一律用「原始抠像帧」下标**（`精修-分段节奏表.py` 量出来的那一套）。
  重采样后的坐标由**打包层**算（`plan_frame_indices` / `map_to_planned_index`），
  并且会把结果打进日志、写进 manifest 的 `holdRange` —— **人只维护一套坐标**，
  否则「量的时候一套、填的时候另一套」必然错位。

**怎么定这三个数**：跑 `精修-分段节奏表.py <frames目录> --hold <lo> <hi>`（0 算力），
它把素材切成**四段**并给每段的帧数与秒数：

| 段 | 起止 | 拿它干什么 |
|---|---|---|
| 变姿 | 首帧 → `holdRange[0]` | 抬起来花多久；和目标（建国 ≈1.26s）差多少倍 |
| 保持 | `holdRange` 窗口 | **循环播放，长度不影响手感** |
| 收回 | `holdRange[1]` → 姿态回到首帧 | 放下花多久（建国 ≈0.88s） |
| 废料 | 收回完成 → 结尾 | **`frameRange` 该砍的** |

⚠️ 脚本还会另跑一条**「画面变化量」曲线**（`动 f0007~f0096`/`静 128 帧`）——
⚠️ **别只看 bbox**：7228 的变姿是「四条腿从收在身下改成自然下垂」，
整只猫 bbox **几乎不动**（y0 只从 21 变 16），拿 `holdRange.lo` 当变姿终点只是**假设**。
画面变化量才看得出动作真实发生在哪几秒。

⚠️ **帧下标要对「打包后的真实帧序列」数**：mp4 抽帧（24fps 得 289 帧）与打包链路
（287 帧）会差 1~2 帧。**先打包一次拿到真实帧序列，再量、再填、再打包** ——
两次打包都是 0 算力，别为了省一次把下标填错。
（补 `frameRange`/`frameTarget` 之后，还要**再量一遍包内帧序**确认三段节奏真的对上了。）

### ④ 打包 —— 0 算力，自动，可反复

```bash
<python> output/打包-7228.py <attempt>      # attempt 只能是 1..3
```

- `attempt` 是幂等键的一部分（`(sessionId, revision, step, attempt)`）。
- 🔴 **`attempt` 上限 = 3**（`contracts._require_int(payload, "attempt", ..., maximum=3)`），
  第 4 次会直接被 `ContractError: attempt is outside the allowed range` 拒掉。
- **同一个 attempt 想重跑**：把已有目录**改名留档**（`attempt-3` → `attempt-3-无holdRange`），
  再跑同一个号。`build_frame_sequence` 要求**输出目录为空** —— 目录非空会抛 `ValueError`，
  被 `_safe_error` 兜底成 `temporaryUnavailable / provider failed`（**看着像网络问题，其实不是**）。
- **⏱ 一次打包约 8 分钟**（四支动作各自抽帧 + 抠像 + 四项验收 + 打 webp + zip）；
  改一个 `holdRange` 就要重跑一遍，所以**下标先量准再填**，别靠试。
- 验收结论带动作前缀（`grab-release:1-尾巴完整`）；`overall_passed` 要 idle 与每支都过。
- ⚠️ `overall_passed=False` 时脚本 exit 8（不是崩溃）。已知的稳定 FAIL：
  `idle-combo:1-尾巴完整`（面积波动判据）、`lick:1-尾巴完整` —— **这两项旧包就有，与改动无关**。

### ⑤ 装到桌面 —— 0 算力，人工确认

- 把产物挂到 **job** 上（`POST /v1/photo-avatar/steps`，`step=packFrameSequence`），
  否则前端拿不到 artifact。
- 用 `output/换上预览包.py <sessionId> <rev> <artifactUrl> <zipSha> --confirm`，
  它要动**三个**地方：
  1. 预览目录 `photo-avatar-frame-previews/<session>/<rev>/`；
  2. **安装目录** `pets/<petId>/assets/`（= `variants.manifest_path` 的父目录）；
  3. `photo_avatar_artifacts.sha256`。
- 🔴 **桌面真正加载的是安装目录，不是预览目录。** 只换预览目录 ⇒ 重启后看到的还是旧包。
  会话已到 `completed` 时必须走第 2 步。DB 里**没有**任何存帧数/哈希的列
  （`variants` 只有 5 列、`pets` 只有元数据）⇒ 除上面三处外没有别的要同步。
- 🔴 **这个脚本要跑 4~5 分钟**（拷 3 × 1149 个文件）。**必须后台跑 + 无缓冲**：
  ```
  ... python.exe -u output/换上预览包.py ... --confirm > output/换包日志.txt 2>&1
  ```
  否则会被工具的 120 秒超时 SIGTERM 掉，而且 **Python 的 stdout 是块缓冲、
  被杀时不 flush ⇒ 日志 0 字节**，看起来像「脚本秒退、什么都没干」——实际它已经改到一半。
- 🔴 **不许在脚本里 `rmtree` staging**：环境有**批量删除守卫**，一次删 > 50 个文件会被拦
  （实测 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":452,...}`）并终止进程。
  ⇒ staging 用**带时间戳的唯一目录名**（永不复用 = 永不需要删），收尾**挪进留档区**而不是删。
- ✅ 覆盖用 `shutil.copy2`（幂等），**不要用 `os.replace`**：中途被杀后 `os.replace` 会让
  staging 半空、预览目录半新半旧，重跑时清单比对直接堵死；`copy2` 重跑一遍即可。
- ✅ 验收用 `output/查换包状态.py <sessionId> <rev> <zipSha>`：它比
  **预览目录整树 sha == 安装目录整树 sha**、库 sha 一致、以及 `holdRange` 实际值。
  ⚠️（该脚本读 `actionId`，不是 `id`。）
- ⚠️ app 运行中改的文件，**要重启 app** 才看得到。

---

## 三、关键契约（改错一处就白跑）

### `holdRange` 的语义与失效条件

播放器（`frame-sequence-renderer.ts`）的判据只有一行：

```
switchAction:  if (loop && holdRange) this.holdHeld = true
currentFrame:  holdHeld ? 在 [lo, hi] 内循环 : index = (elapsed / duration) % frames.length
playMotion:    releasing = holdHeld && 同一动作 && !loop
```

**没有 `holdRange` 时**：

1. `carried`（`playMotion(..., {loop:true})`）退化成**整支无限循环** ⇒ 拖拽期间看不到「保持」；
2. `landed` 的 `releasing` 判据不成立 ⇒ 播放进度被**清零** ⇒ **从第 0 帧重播整支**，
   而不是续播「收回」尾段。

⇒ 用户看到的就是「点击后反馈过长」+「放下后没收回」。**这不是错值，是完全没有这个键。**

### 为什么不能自动标定

2026-09-14 在 05 的真实资产上试过三条线索，**互相矛盾**：

| 线索 | 给出 | 问题 |
|---|---|---|
| 速诊规则（宽度 ≤ 最窄×1.15 的连续段） | 退化成 `[0,120]` | 那是速诊，不是出厂值 |
| 设计比例（0.16..0.46）× 81 帧 | `[12,37]` | 与出厂 `[30,59]` 对不上 |
| 最直觉的「区间首尾相似」 | `IoU(f0030, f0059) = 0.41` | **不成立** |

⇒ `holdRange` 是**窗口**语义（窗口内来回循环），不是「循环点」语义；历史上就是人眼定稿的。
硬做自动标定 = 装一个「参数看着合法、体验随机」的拎起。
所以服务侧 `frames/hold_range.py` **只体检、一个字都不写进 manifest**。

### 中文否定句会把那个动作喂给模型

2026-09-19 教训：`details` 里写「画面里不做任何上升、离地、悬空、飘浮、托起的动作」，
**等于把「悬空」这个词直接喂给模型**。

⇒ `section` / `details` / `strict` 一律写**纯正向**描述
（写「始终正面朝向镜头」，**不要**写「不转身、不侧身」）；
禁止项只留在英文 `negative` 里。

### 视频时长锁死 12 秒

`VIDEO_DURATION = "12"`（常量，写进提示词文本的 `duration` 与它无关）。
**动作脚本的时间轴必须按 12 秒写**，否则模型会自己填满空档（= 加戏）。

---

## 四、已验证记录

### 2026-09-20：grab-release 第二次重出（时间轴改成 12 秒）

改了什么：

- 时间轴按 12 秒重写，**保持段占四分之三以上**（`1.0-10.2 秒`，9.2 秒）；
- `duration` 5 → 12；
- `section` / `details` / `strict` 里的中文否定句（「不转身、不侧身、不横向撑开」）
  全部改成正向描述；
- 英文 `negative` 补 `turning around / turning sideways / side view / changing orientation / rotating body`。

实测（`output/精修-量动作区间.py`，0 算力）：

| 项 | 上一版（5 秒脚本 + 12 秒视频） | 本版（12 秒脚本） |
|---|---|---|
| 帧数 | 287 | 289 |
| 头顶余量 y0 min | 82 | 74 |
| 触顶边帧 | 0/287 | 0/289 |
| 横向撑开 | **撑到 604~633**（crop box 只到 ~609，左右被裁） | 峰值同量级，但**稳定段在 562~568，框内** |
| 稳定段候选 | **没有**（姿态一路漂移） | **f0086 ~ f0205（120 帧 ≈ 5 秒）** |
| 末帧 vs 首帧 | 差 2px | 差 **1px** |

⇒ **「按 12 秒写脚本」解决了「模型加戏填空档」这个病根**，素材第一次出现了可用的稳定保持窗口。

对照靶子（内置 05 建国）：`grab-release` = 81 帧 + `holdRange: [30, 59]`（30 帧窗口 ≈ 1.26 秒）。
本版候选窗口 120 帧 ≈ 5 秒，**比内置的更宽裕**。

**人定窗口**：mp4 上量到 `f0086~f0205`；换算到**打包后的帧**（打包会跳掉 2 帧过渡）
是 **`f0104~f0169`（66 帧）**，写进 manifest 的 `holdRange: [104, 169]`。
服务侧 `hold_range.py` 自己挑的是 `f0220~f0286`（尾巴那个「没被拎起」的坐姿，IoU 0.908）——
**语义完全错**，再次印证「窗口只能人来定」。

### 2026-09-20：装进 7228（换包闭环跑通）

- 打包 sha `df8d9429…cc53e`（38 MB，1149 个文件）→ 挂 job → artifact `0f7d867b…0fa6`；
  artifact 与本地 zip **sha 一致**（打包确定性再次验证）。
- 换包结果：预览目录整树 sha == 安装目录整树 sha == `a4c577f8c0fcc340`；
  库 `photo_avatar_artifacts.sha256` = `df8d9429…`；两处 manifest 的
  `grab-release.holdRange` 都是 `[104, 169]`（287 帧）。
- 过程中踩的两个坑（已写进 ⑤）：**超时 SIGTERM + stdout 缓冲导致「假空输出」**、
  **批量删除守卫拦住 `rmtree(staging)`**。

### 2026-09-20：`frameRange` / `frameTarget` 落地 + 节奏第一次对齐建国

口径先统一成**结果导向**：不管人工的手法，只要效果和建国一样 ⇒ 于是把「效果」量成数字
（见「零、」），量出 grab-release 的长度是建国的 **3.5×**、静止废料 **5.38s**。

实现（`frames/packing.py` 的 `plan_frame_indices` / `map_to_planned_index`，**0 算力**）：

```jsonc
// actions/grab-release.json
"holdRange": [104, 169],   // 原始帧坐标（人量的那套）
"frameRange": [0, 236],    // 丢掉 f237 之后的静止
"frameTarget": 81          // 均匀重采样到 81 帧（= 建国的帧数）
```

打包日志把映射结果打了出来（**人定的数字必须看得见**）：

```
[精修] grab-release：原始 287 帧 → 取 [0, 236] 内的 81 帧（3.40s）
[精修] grab-release：holdRange [104, 169]（原始帧）→ [35, 57]（包内 81 帧）
```

**逐段实测对比**（`精修-分段节奏表.py`，全部按 42ms/帧）：

| 段 | 精修前（287 帧 12.05s） | **精修后（81 帧 3.40s）** | 建国（81 帧 3.40s） |
|---|---|---|---|
| 变姿 | f0007~f0096 → 3.78s | f0002~f0036 → **1.47s** | f0001~f0029 → 1.22s |
| 保持 | 窗口 66 帧 2.77s | 窗口 23 帧 0.97s | 窗口 30 帧 1.26s |
| 收回 | f0168~f0236 → 2.90s | f0056~f0080 → **1.05s** | f0060~f0080 → 0.88s |
| 静止废料 | **128 帧 5.38s** | 0.80s（落在保持窗内） | 0 帧 |

⇒ **总长、变姿、收回全部落到建国的同量级**（原先差 2.4~3.5×）。窗口宽度 23 vs 30 帧
（循环，不影响手感）；保持段内只有 0.1~0.4%/帧 的呼吸级变化，比建国（2.9~10.8%）**更静**。

打包 sha `7fe0867c…39836`；`frameCount` 942 = 287×3 + **81**。

⚠️ 换包时发现的新情况：**裁区间会让文件清单变短**（1149 → 943），
而 `换上预览包.py` 原来对「清单不一致」是**直接中止**的（只会原地覆盖、从不删）。
⇒ 已改成把多出来的旧帧**挪进留档区的 `_淘汰帧/`**（**不删** —— 批量删除守卫会拦）。
**以后每裁一次动作都会走这条路径**，不是一次性问题。

### 2026-09-20：yawn / lick 也压到建国节奏（同一套键，0 算力）

`grab-release` 之后把同一套流程套到另外两支。**但先摆画面再定数字** —— 这一步是本轮的关键：

先量（0 算力）：

| 支 | 原生 | 画面变化量峰值 | `预览-联络表.py` 摆出来看到的 |
|---|---|---|---|
| `yawn` | 287 帧 12.05s | **0.7%/帧**（建国 1.4%） | 嘴张得很开、眼睛半眯 —— **哈欠本身很好** |
| `lick` | 287 帧 12.05s | 1.4%/帧 | 低头 → 保持（嘴被胸前毛挡住）→ 抬头 |

⚠️ **只看「画面变化量」会把 yawn 误判成「哈欠很弱」**：口部是**小面积**动作，摊不出大变化量。
⇒ 本轮补了 `output/预览-联络表.py`（0 算力：白底均匀抽 N 帧，每格标**原始帧号 / 秒数**，
图上量的下标可直接填进 `frameRange` / `holdRange`）。
**机械指标只排雷，肉眼判定权威**（见 `参考-资产生产与取景契约.md`）。

定稿（老王的「按动作自然结束点」= 与首帧 IoU≥0.99、且其后 bbox 偏差 ≤1px 的那一帧）：

| 支 | `frameRange` | `frameTarget` | 依据（原始抠像帧下标） |
|---|---|---|---|
| `yawn` | `[0, 227]` | 119 | f0216 才到 IoU 0.98，**f0227 到 0.99**，其后 bbox 偏差 1px |
| `lick` | `[0, 249]` | 119 | f0243 到 0.98，**f0249 到 0.99**，f0250 起 bbox 偏差 1px |

（两支的 `frameTarget` 都取 **119 = 建国帧数** ⇒ 5.00s；和 grab-release 取 81 是同一个道理。）

精修后**包内**实测：

| 项 | yawn | lick | 建国（yawn / lick） |
|---|---|---|---|
| 帧数 × 帧时长 | **119 × 42ms = 5.00s** | **119 × 42ms = 5.00s** | 119 × 42ms = 5.00s |
| 真实动作段 | f0048~f0075 + f0084~f0110（2.31s） | f0021~f0041 + f0097~f0118（1.80s） | 2.90s / 3.00s |
| 静 | 2.69s | 2.81s | 2.10s / 1.97s |
| 首末帧差 | **0px** | 1px | 1px |

打包 sha `fe855ca0…de7bb`，`frameCount` **606** = 287（idle）+ 119 + 119 + 81。
manifest 与建国逐项对上：`yawn`/`lick` 都是 `loop=false` 且都在 `idleSchedule.entries` 里
（`{weight:1, minIntervalMs:30000, maxIntervalMs:60000}`），`grab-release` 带 `holdRange: [35, 57]`。

⚠️ **与建国的已知差距（精修修不掉，要修只能重出）**：建国的 `lick` 在保持段里
**能看见舌头出来理毛**；本支保持段嘴被胸前毛挡住、眼睛闭着，等于「低头停住」。
⇒ 先上 0 算力版本，**是否重出留待老王拍板**。

⚠️ **验收报告里 `1-尾巴完整` 仍是 false**（`failedCriteria` 里三支），但这是**既有状态**：
`foregroundAreaVariation` 超 0.06 阈值（idle 0.076 / lick 0.163）+ grab-release 有 1px 贴边帧，
**与上一版逐字节相同**（同一份抠像产物）⇒ 不是精修引入的回归。机械指标只排雷。

---

## 五、还没做的

| 项 | 状态 | 说明 |
|---|---|---|
| `frameRange` / `frameTarget`（裁区间 + 重采样） | ✅ **已实现**（2026-09-20） | 落点：`assets/motion-prompts/actions/<id>.json` + `frames/packing.py` 的 `plan_frame_indices`。0 算力 |
| 三支动作全部对齐建国节奏 | ✅ **已完成**（2026-09-20） | `grab-release` 81 帧 3.40s / `yawn` 119 帧 5.00s / `lick` 119 帧 5.00s —— 与建国**同帧数同总长** |
| `holdRange` 自动标定 | **不做**（有意的） | 见上「为什么不能自动标定」 |
| 交互动作的「姿态是否合适被拎起」 | 人工判定 | 机械指标只排雷，肉眼判定权威（见 `参考-资产生产与取景契约.md`） |
| `holdRange` 窗口宽度对齐（建国 30 帧 vs 我们 66 帧） | 待定 | 窗口太宽，长时间拖拽会看到它在小循环里「晃」 |
| **精修参数 per-pet 化 / 自动推导** | ⏳ **有意先不做**（2026-09-20 老王拍板：「等第二只新宠真出现」） | 见下「六」 |

---

## 六、新宠怎么办：现在还需要人量一次（已知、已量化）

**现状（2026-09-20 核实）**：`frameRange` / `frameTarget` / `holdRange` 写在**全局**
`assets/motion-prompts/actions/<id>.json`，而产线取用时只 `load_action(action_id)`
（`frame_pipeline.py:784`）——**不带 `pet_id`**
⇒ 这三个数字是**对着 7228 那一支素材量出来的，却对每只新宠生效**。

**所以「通用」的边界是**：

| 环节 | 状态 |
|---|---|
| 提示词（identity / coat / `{{COAT_LEN}}`） | ✅ 通用（gpt-4o 看照片自动判） |
| 取景收敛 / 抠像 / 四项验收 / `idleSchedule` | ✅ 通用 |
| 上传 → 会话 → 换包三处落点 | ✅ 通用（4750、7228 两只不同素材已跑通） |
| `frameTarget` | ✅ **恒定靶子**（81 / 119 / 119）⇒ 可写死，不用逐宠量 |
| `frameRange` / `holdRange` | ❌ **逐素材的绝对帧下标** ⇒ 换素材必偏 |

**偏了的后果**：裁少了 → 把「收回段」裁掉一半（动作没做完就跳回 idle）；裁多了 → 留静止废料；
`holdRange` 指错位置 → 拎起循环的是错姿态（极端情况映射后 `lo==hi`，打包守卫会报错，不会静默出包）。
⚠️ 而且它改的是**全局文件** ⇒ 顺手会污染 7228 下次重打包。

### 判据可以自动，且已实测能复现人工数字（0 算力）

规则 = 首个「与首帧 **IoU≥0.99** 且其后 **bbox 偏差 ≤1px**」的帧（那帧之后全是静止尾）。
拿 7228 三支的**原始抠像帧**跑：

| 支 | 人工定稿 | 自动（th=0.99 + bbox≤1px） |
|---|---|---|
| `yawn` | 227 | **227** ✓ |
| `lick` | 249 | **249** ✓ |
| `grab-release` | 236 | 245（+9 帧 = 0.38s；人工那个来自 `精修-分段节奏表.py` 的 IoU≥0.92 returned） |

⇒ 真要收口时的两步（**都不是现在做**）：
1. **`frameRange` 从素材自动算**（判据就是上表那条）+ `frameTarget` 查靶子表 ⇒ 新宠 0 人工、天然 per-pet；
   兜底：素材里没有「回到首帧姿态」的时刻时判据返回 `None` ⇒ **必须显式处理**（退回不裁 + 记日志），
   不能静默出包。
2. **`holdRange` 保留人工**（语义活：09-20 实证自动候选会挑中「端坐」段），
   可用 `byPet` 覆盖落进 config，避免污染别的宠。

**在那之前，新宠那天的清单**：三支动作各跑一遍 `精修-分段节奏表.py` + `预览-联络表.py`，
按上表判据量出 `frameRange` 右端，填进 `actions/<id>.json`（**记得先抄下 7228 的现值**，
不然重打包 7228 会跟着变），再打包 → 换包。
