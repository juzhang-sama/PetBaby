# 写实风产品化落地（2026-09-13）

> 前提：唯一路线已定（照片 → 母版 → 绿幕视频 → 抠像 → `frame-sequence-v1`）。
> 2026-09-13 `scripts/一键出宠.py` 已把整条链在本机跑通（果冻照片，288 帧，四判据全 PASS）。
> 本文只讲**怎么把这条链搬成产品能力**，不讲算法。调研报告见 `路线收敛调研-2026-09-11.md`。

---

## 一、摸清的硬约束（决定切法，别绕过）

1. **artifact 只认 PNG，且一个 job 只能有一个 artifact。**
   `job_store._decode_artifact` 硬校验路径必须以 `.png` 结尾；`app.py` 的
   `/v1/photo-avatar/artifacts/{id}` 写死 `media_type="image/png"`。
   → 最终产物（288 帧 + manifest）**必须打成 zip** 才走得通现有通道，需要扩契约。

2. **job 结果不跨后端重启。** `job_store._load_state` 对
   `step != "renderTextureAtlas"` 的 succeeded job：删 artifact + 标记 failed
   （`"job result unavailable after restart"`）。视频链路耗时 5~15 分钟，
   客户端必须在下一次后端重启前把包下载并安装完。**这条要确认能不能接受。**

3. **服务缺 scipy。** `requirements.txt` 有 numpy/pillow，**没有 scipy**；
   `poc_抠像.py` 依赖 `ndimage.label / ndimage.sum / binary_closing /
   binary_opening / gaussian_filter`。启动脚本的探测串也只探
   `fastapi, uvicorn, httpx, PIL, numpy`。
   → **加依赖**（不改算法），并同步更新探测串。重写这 5 个调用有质量回归风险。

4. **步骤名是硬编码白名单**：`contracts._STEPS` / `_PIXEL_STEPS`、
   `job_store._STEPS`。`attempt` 上限 3。

5. **`creation_photo_avatar_*` 八个命令全绑在 `PixelPhotoAvatarManager`**
   （`lib.rs:1070-1179`）→ 新路线要按 route 分派，不是加命令。

---

## 二、步骤切分：两个，不是六个

| step | 内容 | 产物 | 为什么这样切 |
|---|---|---|---|
| `generateMotionSource` | 母版 → 绿幕首帧（取景收敛）→ 视频 | mp4，**留在服务侧 scratch，不上传** | 视频是唯一大额支出（12s 标准 480p = **5.69 算力**），单独一步才能做到「只重试这一步」 |
| `packFrameSequence` | 抠像 → 四判据 → 打包 schema7 → WebP → zip | zip（最终包 artifact） | 后半段便宜（0 算力），挂了整段重跑无所谓 |

**不拆成六步**：Rust 侧状态机要跟着长 6 倍，而中间步骤没有独立重试价值。
**不合成一步**：视频失败要重付 5.69，粒度太粗。

---

## 三、改动清单

### Python（`services/appearance-generation`）

| 文件 | 改动 | 状态 |
|---|---|---|
| `frames/packing.py` | `poc_组合循环打包.py` 搬入 + 入参校验 + 拒绝覆盖非空目录 | ✅ 已完成（黄金回归 288 帧逐字节一致） |
| `frames/matting.py` | `poc_抠像.py` 的 chroma 路径 + autocrop + 过渡帧/暖机 | ✅ 已完成（黄金回归 288/288 帧逐字节一致；14 tests） |
| `frames/_paths.py` | `rel_to` / `sha256_of`（原先 matting + packing 各有一份，抽出来共用） | ✅ 已完成（抠像黄金回归复跑 288/288 帧仍逐字节一致） |
| `frames/first_frame.py` | `poc_绿幕首帧.py`（含取景自检 `tailSwingFit` + `framing_ok` 闸口） | ✅ 已完成（3 张图字节一致；见下方「首帧的两处差异」） |
| `frames/acceptance.py` | `poc_验收.py` 四判据 + 证据图 | ✅ 已完成（5 张证据图字节一致；报告除输出目录名外逐键一致） |
| `frames/pipeline.py` | 串起上面五件 + WebP + zip | ✅ 已完成（端到端 288 帧全过；见下方「第 4 片」） |
| `job_store.py` | `_STEPS` 加两 step；artifact 允许 `.zip`；按扩展名给 media type；scratch 目录（放中间 mp4） | ✅ 已完成 |
| `contracts.py` | 新 step 的请求形状 + route 校验（第三种请求形状 `FrameStepRequest`） | ✅ 已完成（18 个用例） |
| `app.py` | 按 route 分派两个新 step；`_job_wire` 支持 `resultType: "frameSequence"`；artifact media type 走 store | ✅ 已完成 |
| `frame_pipeline.py`（新） | 两个 step 的服务侧装配 + `FrameSequenceArtifact` | ✅ 已完成（`packFrameSequence` 真实现，`generateMotionSource` 见 5b） |
| `audit.py` | `_MODELS` 白名单加 `seedance-2.0-guanfang` | ✅ 已加（见下方「白名单」） |
| `requirements.txt` + `启动照片分身受控后端.ps1` | 加 `scipy`（含探测串） | ✅ 已加（scipy 1.18.1 已装进后端解释器） |

#### 首帧的两处差异（第 3 片，2026-09-13）

1. **有意修的**：`analyze_master` 的 `thinStructureRatio` / `veryThinStructureRatio`
   旧实现是 `try: import cv2` —— 装了 opencv 出数字、没装出 `null`。
   **同一个脚本换台机器跑就产不同的 JSON**（实测：`D:\DevTools\Python312` 有 cv2 出数字，
   后端解释器没有出 `null`）。对一个要进服务的模块，这是不可接受的隐藏依赖。
   已固定成 scipy（本模块的既有依赖），**数值与 cv2 路径逐位相同**
   （同一母版都是 `0.0114 / 0.0224`），而 `output/` 下 8 只宠物的历史记录**全都有数字** ——
   说明 `null` 才是异常（那一次恰好跑到了没有 cv2 的解释器）。
2. **不是差异的差异**：`首帧分析.json` 里 3 个路径字段记的是**产物写在哪**，
   回归时写到了 `01-首帧-回归/` 而不是 `01-首帧/scale-90/`，所以字符串不同 —— 数值全同。
   `验收报告.json` 的 5 个 `evidence.*` 同理。

回归产物留在原地产物旁边的 `01-首帧-回归/`、`04-抠像-回归2/`、`05-验收-回归/`（`output/` 已被 gitignore）。

脚本侧保持**薄 CLI 包装**（`scripts/poc_抠像.py` 等 import 服务模块），
**不 fork 逻辑** —— 这是本次搬迁定的规矩。

### Rust（`apps/desktop/src-tauri`）

| 文件 | 改动 |
|---|---|
| `storage/migrate.rs` | `route` CHECK 加 `frame-video-v1` |
| `creation/photo_avatar/frame_manager.rs` | 新：照 `pixel_manager.rs` 骨架驱动两个 step |
| `creation/photo_avatar/domain.rs` | 新 route / step 枚举 + 快照类型 |
| `runtime_assets/frame_sequence_builder.rs` | 新：解 zip → 校验 → 安装。**`loader::validate_asset_manifest` 已接受 V7，不用改校验器**；复用 `staging_directory_for` + `install_staged_assets`（已带备份/回滚） |
| `lib.rs` | `creation_photo_avatar_*` 按 route 分派 |
| `creation/finalization.rs` | **不用改**：`record_photo_avatar_runtime` 的 `style_id` 已跟随 `manifest.renderer` |

### TS（`apps/desktop/src`）

| 文件 | 改动 |
|---|---|
| `settings/photo-avatar-creation-view.ts` | 步骤文案（母版/首帧/视频/抠像/验收）+ 轮询间隔（5~15 分钟，别 1 秒一查） |
| `creation/api.ts` | 新 route 参数 |

---

## 四、落地顺序（每步可独立验收）

1. ✅ **打包搬入 + 黄金回归**（2026-09-13：288 帧逐字节一致）
2. ✅ **抠像搬入 + 黄金回归**（2026-09-13：同一支 `绿幕视频-135364519.mp4` → 288/288 帧逐字节一致）
3. ✅ **首帧 + 验收搬入**（2026-09-13：首帧 3 张图字节一致、验收 5 张证据图字节一致）
4. ✅ **`frames/pipeline.py` 串链**（2026-09-13：见下方「第 4 片」）
5. ✅ **契约扩展**：`job_store` / `contracts` / `app` / `frame_pipeline` + 测试（2026-09-13：见下方「第 5 片」）
5b. ✅ **写实风「提示词契约化」+ `generateMotionSource` 实现**（2026-09-13：见下方「第 5b 片」）
6. **Rust**：migration → `frame_manager` → `frame_sequence_builder`
7. **TS**：文案与轮询
8. **端到端**：应用内上传一张照片，走完全程

第 4 步做完，服务侧就**具备生产 schema 7 包的能力**；第 5 片做完，两个 step 进得了 job 契约
（`packFrameSequence` 已可真跑）；第 6 步做完，应用里就能装。

#### 第 4 片：串链（2026-09-13）已完成

`build_frame_sequence(video, out_dir, ...)` = 抠像 → 四判据 → 打包 WebP → zip，
**不碰任何 API、跑一次 0 算力**。`out_dir` 必须空（中间产物上百个文件，
原地重跑要批量删除会被本机守卫拦），调用方给每个 job 一个新 scratch 目录。

用已存的 `绿幕视频-135364519.mp4` 端到端复跑（`output/…/12-流水线-回归/`），结论：

| 比对项 | 结果 |
|---|---|
| 抠像帧 vs 黄金产物 | **288/288 逐字节一致**（证明 pipeline 没动算法） |
| WebP 解码 vs PNG 源帧 | **alpha 288/288 逐像素无损** |
| WebP 有损量 | 全图平均 9.4/255，**不透明区（alpha≥200）只有 2.5/255** |
| 肉眼 | 整帧 + 最差区域放大 3 倍均**分辨不出**（`_对比/` 下两张图） |
| manifest | 归一扩展名后**逐键相同**，只剩已知的 `_provenance.generator` |
| zip | **二次打包字节相同**（条目排序 + 时间戳固定成 `(1980,1,1)`） |
| zip 自洽 | 288 项 sha256 与 manifest 全对得上（Rust 安装时要校的就是这个） |

交付物：`<out_dir>/<petId>.zip` **9.7 MB**（PNG 源帧 72.6 MB / 288 帧，约 7.5 倍）。

**顺带收掉**：`MIN_FRAMING_MARGIN` 与 `MARGIN_MIN` 两份定义已合成一处 ——
常量落在**无依赖**的 `frames/__init__.py`，`first_frame.py` 与 `scripts/一键出宠.py` 都从那里 import。

⚠️ **两个 step 的边界**：`generateMotionSource`（母版+首帧+视频，5.69 算力）产出的 mp4
留服务侧 scratch，**不放进这个 zip**；`packFrameSequence` 只吃 mp4。
⚠️ **证据图不在这条链的产物里**：zip 只有运行时包（manifest + 帧），
而人工确认要看 `05-验收/` 的证据图。「证据图怎么送到客户端」是第 5 片契约扩展要定的
（一个 job 只能有一个 artifact），本片不做假设。

---

#### 第 5 片：契约扩展（2026-09-13）已完成

新增**第三种请求形状** `FrameStepRequest`（`route = "frame-video-v1"`），
与 `StepRequest`（legacy）/ `PixelStepRequest`（`pixel-v1`）并列，`parse_step_request` 按 route 分派。
11 个字段，其中 2 个是写实风特有的：

```
route sessionId revision providerSessionId step attempt consentVersion
sourceImages  petId displayName species
```

两个 step **共用一份字段**，差别只落在 `sourceImages` 上（`_require_exact_fields` 要求精确匹配）：

| step | sourceImages | 输入从哪来 |
|---|---|---|
| `generateMotionSource` | 照片 1..8 张 | 母版从照片生成 |
| `packFrameSequence` | **必须为空** | 上一 step 落在 `scratch/<providerSessionId>/motion-source.mp4` |

→ `packFrameSequence` 收下照片只会在实施层变成一个「悄悄忽略」的分支，**在契约层就拒**。
→ `packFrameSequence` 必须带 `providerSessionId`（没有它找不到 scratch 里的 mp4）。

**产物与媒体类型**：`FrameSequenceArtifact` 让 `job_store` 把 zip 写进 `artifacts/<id>.zip`，
`artifact_media_type()` 按扩展名回 `application/zip`（artifact 端点原先硬编码 `image/png`）。
`read_artifact` 的返回类型没动（被测试大量 `== bytes` 那样用），另开一个方法问 MIME。

**证据图不送客户端（本片定的一处契约）**：zip 里只有运行时包，`05-验收/` 的证据图留在服务侧。
理由：客户端要人工确认的是「桌宠动起来像不像」（第 6 片装进预览位），不是那五张图；
而一个 job 只能有一个 artifact。`overallPassed` / `failedCriteria` 随 `resultType: "frameSequence"`
回去，够客户端提示「检测到哪条异常」。要改的话代价是「一 job 多 artifact」，先不做。

**`generateMotionSource` 当时还没实现**（第 5b 片补上，见下）：它抛 `FramePipelineError`
（→ 客户端看到 `temporaryUnavailable`，**不是** `invalidInput` —— 请求本身没错，
是服务还没这个能力）。缺口在**提示词**：真正要发给 lk888 的两份提示词原先躺在
`output/_通用母版提示词-2026-09-12.txt` 与 `output/_通用组合循环提示词-2026-09-12.txt`
里，**而 `output/` 在 `.gitignore` 里** —— 服务根本拿不到它们；而且那是两份
「给人看的完整文档」，真正的提示词夹在「与建国版的区别」和「保存位置」之间，
靠脚本按 `【主提示词】` / `-----` 切出来。

##### 白名单：`provider_model` 不是随便记的字符串

`audit.py` 的 `_MODELS = {"gpt-4o", "gpt-image-2"}` 是**允许值白名单**，
`AuditContextV1.from_state` 会重新校验 —— 写个不在里面的名字，会在 `reserve()` 阶段
就以 `AuditContractError` 炸掉（测试先撞上的，不是生产）。已加 `seedance-2.0-guanfang`：
那条链上母版只要 0.06 算力，5.69 全花在视频上，追溯时记它才诚实。

##### artifact 不跨重启（沿用既有约束）

`job_store._load_state` 对 `status == "succeeded" and step != "renderTextureAtlas"`
会删 artifact 并标 `failed`（只有 `renderTextureAtlas` 例外，它的 wire 不需要内存里的 result）。
对写实风代价可接受且是**有意选的**：用户重试时 `packFrameSequence` 0 算力重跑，
mp4 还在 scratch 里，**不会重付视频那 5.69**。这正是「存中间 mp4 兜底」那条决策的用处。

##### 验证

- 契约 18 个用例（含 route 分派、7 处拒绝）；`frame_pipeline` 8 个（含 ffmpeg 现场合成的**真端到端**：
  合成绿幕视频 → 抠像 → 验收 → WebP → zip → 解压校验 manifest 与 zip 自洽）；
  `job_store` 4 个（zip 落盘 / 重启行为 / 扩展名白名单 / 篡改 hash 被拒）；`app` 4 个（wire / zip 端点 / 未实现步骤 / 契约拒绝）。

---

#### 第 5b 片：提示词契约化 + `generateMotionSource`（2026-09-13）已完成

原以为这一片「动提示词质量，需要老王参与」。**查完发现不用** —— 该做的只有搬家：

| 原以为的缺口 | 实际情况 |
|---|---|
| `骨架.txt` 是给人看的文档，要拆 | `骨架.txt` 是**一次性动作（yawn/lick）**那条路的，`generateMotionSource` 走 idle-combo，用的是另外两份 |
| 宠物档案只有 3 个 POC 期的，与「对话式语义填充」冲突 | 那两份 idle 提示词**已经通用**：母版只有 `{{SPECIES}}`/`{{COAT_LEN}}` 两个替换位，组合循环提示词**一个都没有**（身份/毛色全交给首帧图） |
| 要重写提示词 | **一个字都不用改** —— 重写就是换一份提示词，等于是换产品 |

真正的问题只有一个：**那两份提示词在 `output/` 下，而 `output/` 被 `.gitignore` 忽略**。
服务拿不到 = 这条链一离开这台机器就断。

##### 做法：搬家 + 逐字节证明

正文**没有手抄**，是用老脚本自己的切分逻辑（`poc_生成绿幕视频.py::section`、
`poc_生成母版.py` 里那段 `-----` 切分）从原文档抽出来的，抽完逐字节比对：

| 提示词 | 结果 |
|---|---|
| 母版 × 4（`cat`/`dog` × `short`/`long`） | **与老链路逐字节一致** |
| 组合循环（含负向 / 不含负向） | **与老链路逐字节一致** |

新家：

```
services/appearance-generation/src/photo_avatar_backend/
    assets/motion-prompts/
        master.txt                     机器用，含 {{SPECIES}}/{{COAT_LEN}}
        loop-idle-combo.txt            机器用（主提示词）
        loop-idle-combo.negative.txt   机器用（负向词）
        README.md                      人读的：参数、下游管线、验收、golden 哈希表
    frames/prompts.py                  render_master_prompt / render_loop_prompt
```

`frames/test_prompts.py` 钉了 **8 个 golden sha256**（4 个带毛长 + 2 个不带 + 2 个循环）。
提示词一动测试就红 —— 这不是「更新一下哈希了事」，是要意识到这会改变所有新宠物的产出。

##### `generateMotionSource` 实现

`母版（gpt-image-2）→ 首帧取景收敛（免费阶梯）→ 视频（Seedance）`，mp4 落
`scratch/<providerSessionId>/motion-source.mp4`。三条防线保证不白花那 5.69：

1. **已有 mp4 直接复用**（一次上游都不打）。后端重启会把 job 结果清掉
   （`_load_state` 对非 `renderTextureAtlas` 的成功态标 failed）→ 同 session 重试
   白捡回那支视频。要真重跑就换 `providerSessionId`（= 用户点「重新生成」）。
2. **取景收敛在免费阶梯上做完再进视频**（`SCALE_LADDER` 0.90→0.75）。全挂 = 照片的问题，
   报 `invalidInput` 并**不提交视频**（测试里断言 `submit_video` 没被调过）。
3. **上游错误码原样透出**：`contentPolicy` 这类不可重试的错误不包成
   `temporaryUnavailable`，否则上层会拿同一份提示词再撞一次审核。

另外两处顺带定死：

- **mp4 原子落地**（先写 `.part` 再 `replace`）。`packFrameSequence` 只判断
  `video.is_file()`，半支视频会被当成完整的往下跑。
- **只上报视频这一个 provider task id**。一个 job 的 `lk888_task_id` 是单选
  （上游删除要用它），报第二个不同的 id 会被 `job_store` 拒掉、整个 step 失败；
  母版那个记在 `MotionSource.master_task_id` 里回客户端。

##### wire 形状

`generateMotionSource` **没有 artifact**（mp4 一个字节都不往客户端送）：

```json
{"resultType": "motionSource", "videoBytes": 9600000, "reused": true,
 "masterTaskId": null, "videoTaskId": null, "firstFrameScale": null,
 "firstFrameLeftMargin": null, "firstFrameRightMargin": null}
```

失败码：`FramePipelineError.code` 默认 `temporaryUnavailable`，取景全挂显式给
`invalidInput`；`Lk888Error` 照旧带自己的 `code`。

##### 一处待老王拍板：毛长档位

`{{COAT_LEN}}` 是 `short-haired` / `long-haired` 二选一的**提示**，但
`FrameStepRequest` 里没有毛长字段（UI 也问不出来）。服务路径**不提这一档**
（渲染成 `…for this exact cat.`），依据是提示词下一段本来就要求「毛长照照片一模一样」，
真源是照片。

- 人工出宠（`一键出宠.py --coat short`）知道毛长，走带提示的那条。
- 两条渲染结果只差这两个词，各自的 golden 哈希都钉住了。
- ~~若发现长毛猫的母版质量依赖这句提示 → 要么加进请求契约，要么先跑一次
  `gpt-4o` 分析照片自动判定~~ → **已拍板走后者，见下面「第 5c 片」**：
  服务路径现在让 gpt-4o 看照片判，判不出/分析失败才退到「不提」。

##### 验证

- `frames` 新增 `test_prompts.py` **30 个**（species×coat 矩阵、占位符残留、golden 哈希、
  归一化「只差物种与毛长」）；`test_frame_pipeline.py` 从 8 → **18 个**（`_FakeClient`
  全程不打 API：happy path / 取景收敛到 0.85 / 全挂不提交视频 / 复用不重付 /
  只上报一个 task id / contentPolicy 不被吞 / 超时可重试 / 路径与闸口）。
- `test_app.py` 15 个：新增 motionSource wire（无 artifactUrl）、取景失败到客户端是
  `invalidInput`、内部故障仍是 `temporaryUnavailable`。
- 另跑一次**迁移前后的黄金回归**（临时脚本，已删）：6 条提示词全部逐字节一致。

##### 顺带清理

`scripts/一键出宠.py` 的提示词渲染退化成薄包装（`write_prompt` 写**渲染结果**留档），
删掉了脚本里那份 `section()` 等价物与 `assert_no_placeholder` —— 提示词只由服务渲染一处。

#### 第 5c 片：gpt-4o 看照片判毛长（2026-09-13）已完成

第 5b 片留下的**待拍板项**，老王选了「让 gpt-4o 分析照片自动判定」那条（另一条是加进请求契约）。

##### 做法：不给它 step 名，做成 `generateMotionSource` 的内部第一步

新增 `frame_pipeline.analyze_photo_facts` + `PhotoFacts`，插在母版**之前**：

```
generateMotionSource = [1/4] 看照片 → [2/4] 母版 → [3/4] 首帧收敛 → [4/4] 视频
```

形状照抄既有的 `analyzeIdentity`：`client.analyze_json(prompt, images, schema)`
（走 `/v1/chat/completions`，`config.analysis_model` = `gpt-4o`，**白名单里本来就有**）。

不新增 step 名的三个理由：① 躲开那**三处硬编码 step 白名单**
（`contracts._FRAME_STEPS` / `_PIXEL_STEPS` / `job_store._STEPS`）；② 不对外多出一个
「可重试的独立进度」；③ 它的产物（一个词）没有任何交付价值，不值得一次往返。

##### 硬规矩：这一步**只能降级，不能失败**

`_coat_hint` 只吞 `Lk888Error` 与 `FramePipelineError`，吞掉后 `coat=None` 照常出母版。

- 依据：少一句「long-haired」**母版照样出得来**（提示词下一段本来就要求「毛长照照片一模一样」）。
- 反过来，一次失败会带走后面 **5.69 算力**的视频 —— 一个纯省钱的可选步骤不该有这个权力。
- `AttributeError` 之类的**代码错误不在吞的范围内**：那是 bug，要炸出来。
  （这也是为什么 `test_frame_pipeline._FakeClient` 必须显式实现 `analyze_json` —— 漏了会红。）
- 复用分支在它**之前**：复用不重跑，连这一次 gpt-4o 都不打（既有用例 `client.calls == []` 继续守这条）。

##### 回答形状

严格 `json_schema`，两个字段，**「看不出来」用 `unknown` 哨兵值**而不是 `["string","null"]`
联合类型（严格模式对联合类型支持参差，哨兵值在两边都只是普通字符串）：

| 字段 | 取值 | 用途 |
|---|---|---|
| `species` | `cat` / `dog` / `unknown` | **只记日志**：与请求不一致时打印 ⚠️，**不改行为** |
| `coat` | `short` / `long` / `unknown` | 进 `render_master_prompt(species, coat)` |

`unknown`、答非所问（`"medium"`）、缺字段、回包不成形 —— **四种都抹平成同一个结果（不提）**，
对下游是同一件事。物种以请求为准的理由：它是产品写进 manifest 的字段，不该被一次模型判断推翻；
但两者不一致是「照片传错了」的信号，值得留痕。

##### 验收：4/4（真打 API）

`scripts/poc_照片分析验收.py`（**会花钱**：4 次 ≈ **0.01 算力**）。真值取自
`docs/验证记录/L1真实照片验收记录-2026-09-11.md` 第 3 节物种表 + 第 5 节人眼判定毛长：

| 源图 | 真值 | gpt-4o 判 |
|---|---|---|
| `果冻.jpg`（短毛猫） | cat / short | cat / short ✅ |
| `长毛猫.jpeg`（长毛猫） | cat / long | cat / long ✅ |
| `短毛犬.jpg`（比格） | dog / short | dog / short ✅ |
| `金毛.webp`（长毛犬） | dog / long | dog / long ✅ |

**覆盖 短/长 × 猫/犬 四格**，因此回答了真正要紧的那个问题：**没有「永远说 long」的偏置**
（这才是这个功能的主要风险 —— 判错一次就等于让短毛猫照着 long-haired 画）。
另测了单张 vs 一批、以及三张同一只猫（`samples/`）—— 答案稳定。

> 中间踩过一个坑：先拿「银渐层母版」「内置猫预览」两张**画稿**当反例，
> 银渐层那张被判 `long`。它**不是照片**（提示词说的就是 "pet photos"），
> 是模糊边缘的画稿，off-label，不能当反例。**反例必须是真照片。**

##### 成本

**≈ 0.005 算力/次**（实测 4 次共 0.01）。相对母版 0.06 + 视频 5.69 可忽略。
每单 `generateMotionSource` 多一次 gpt-4o，**试错重跑（attempt 2/3）会重付这一次** ——
比起同 attempt 也会重跑的母版（0.06），不值一提，没做缓存。

##### 测试

`test_frame_pipeline.py` 18 → **29 个**：走链期望改成
`analyze_json → submit_image → …`；毛长进提示词；分析失败只丢提示（且**留痕**）；
contentPolicy 也只丢提示；5 种不可用回包参数化；物种不一致只记日志；分析看到全部照片。

#### 第 6 片：Rust 侧（已定设计，动工中）

服务侧两步都能真跑（第 5b/5c 片），缺的只剩 Rust 消费。**像素风那条路是完整模板**，
帧路线照它抄，别另发明一套。

##### 已定设计（2026-09-13 拍板）

1. **同名「同表换 route」，不另起 run 表。**
   `photo_avatar_step_attempts` / `_profiles` / `_artifacts` 都 FK 到
   `photo_avatar_runs(session_id)`；另起一张就得连带再建 2~3 张 + 复制整套
   reserve/set/fail 逻辑。而 `route` 这列本来就是为「多产线」加的（v12 引入）。
   「PK = session_id、一条 session 一条 run」**不是问题**：一个创建会话只走一条路线，
   route 在 `begin` 时定死，`regenerate` 读回同一个值；真换路线 = 同 session 重来，
   覆盖旧 run 正是应有行为。
2. **`runs.style_profile_id` 放开为可空（NULL），不塞哨兵值。**
   塞 `'frame-video-v1'` 会把「路线」和「画风」两个刻意分开的维度混进一列
   （老王拍板「画风 = 独立维度」）。NULL 才是诚实的「不适用」。
   读取侧安全：全仓库只有 `pixel_snapshot`（store.rs:632）读它，而它带
   `route='pixel-v1'` 过滤，**永远看不到 NULL**。
3. **四张表的 `route` 取值域统一**到 `('live2d-v5','pixel-v1','frame-video-v1')` ——
   保持「route 在四张表里是同一个取值域」这条不变量，比省一个字符串值钱
   （`profiles` 实际不写写实风的行，但取值域一致才不会让人以为「这里不许写」）。
   `profile_kind` 是另一个维度，不动。
4. **命令层不并列新命令**：九个 `creation_photo_avatar_*` 仍是同一组，
   `begin` 多一个**可选** route/画风参数（默认现役），命令内**按 route 分派**给对应 manager。
   理由：上传 → 预览 → 人工确认 → 安装这层壳**是同一个**，缺的只是「生成什么」；
   并列新命令 = fork 壳，等像素风作为「一种画风」回来时还要再加一套。
5. **`store.reserve_pixel_attempt` 泛化（读 run 行的 route 再校验 step），不复制一份。**
   错误串按 caller 传进来的 label 拼，保证像素侧字符串逐字不变（有测试钉着）。
6. **`download_artifact` 加一条 zip 校验路径**（照它已有的 `_PNG_ARTIFACT` / `_VIDEO_ARTIFACT`
   分派结构加一种），**不动** PNG 那条。
7. **poll deadline 变成参数**：帧路线传 **900s**，像素路线仍是 300s。
   （不是全局调大 —— 让「谁慢」这件事留在调用点上。）

##### 要新建

| 文件 | 照谁抄 | 干什么 |
|---|---|---|
| `src/creation/photo_avatar/frame_manager.rs` | `pixel_manager.rs`（14KB） | `FramePhotoAvatarManager`：begin/status/regenerate/cancel + 后台线程跑两个 step |
| `src/runtime_assets/frame_sequence_builder.rs` | `pixel_avatar_builder.rs` 的 `build_preview` | 收 **zip 字节** → 解压到 staging → `validate_asset_directory` → `install_staged_assets` → `validate_preview` |

##### 要改

1. ✅ **`src/storage/migrate.rs` 已追加 v14**（2026-09-13，`cargo test --lib` **890 passed**）——
   现有四张表的 CHECK 放不开新路线，所以重建（顺序与 v12 完全一致，**不能改**：
   先改名父表 → 建新父表 → 拷 → 再逐个改名/重建子表（子表必须在父表改名之后重建，
   否则 SQLite 会把子表的 FK 改指向临时名）→ 最后 drop 旧父表）：
   - `photo_avatar_runs` / `_step_attempts` / `_profiles` / `_artifacts` 的 `route` 都放开到三个值
   - `runs.step` 加 `'generateMotionSource','packFrameSequence'`（`step_attempts.step` 同）
   - `artifacts.kind` 加 `'frameSequence'`
   - `runs.style_profile_id` 放开为可空（见上面「已定设计」第 2 条）
   - 新测试 `v14_opens_the_photo_avatar_tables_to_the_frame_route` 一次钉四件事：
     老行画风值逐字活下来 / 新 route·step·kind 放行 / 画风可为 NULL /
     **闸口没松**（route 仍只认三个值、attempt 仍硬顶 3 次、step 未列的值照拒）
   - `validate_latest_schema` 的 reference 是**用全部 MIGRATIONS 现建的内存库**
     (`:975-980`)，`require_reference_columns` 只比 `pragma_table_info`
     （type/notnull/default/pk，**不看 CHECK**）→ 追加一段 SQL 即自洽，不用手改期望清单。
2. **`src/creation/photo_avatar/provider.rs`** —— 加 `FrameProviderStepRequest`
   （字段照 `contracts.py:46-60`：route/sessionId/revision/providerSessionId/step/
   attempt/consentVersion/sourceImages/petId/displayName/species）+ `submit_frame_step`；
   `ProviderStepResultWire`(:217，`#[serde(tag="resultType", deny_unknown_fields)]`)
   加两个 variant：`MotionSource`（**无 artifactUrl**）、`FrameSequence`（zip）。
   统一解析点就是它的 `Deserialize`(:276)，别另开分支。
3. **`src/creation/photo_avatar/domain.rs`** —— `FramePhotoAvatarStep` / `FrameRemoteStep` /
   `FramePhotoAvatarSnapshot`（照 `Pixel*` 三兄弟 :250/:265/:272）。
4. **`src/creation/photo_avatar/store.rs`** —— `begin_frame_revision` / `frame_snapshot` /
   `reserve_frame_attempt` / `set_frame_step` / `fail_frame_revision_*` /
   `commit_frame_artifact`。⚠️ `reserve_pixel_attempt`(:414) 把 `route='pixel-v1'` 与
   step 名单**写死**(:431-444)、attempt 上限 3(:449) → 必须另写一份，别去改它。
5. **`src/lib.rs`** —— 帧命令 + 构造 `FramePhotoAvatarManager` 并 `app.manage` +
   两处命令清单（`generate_handler!` :95-103、内部字符串清单 :3275-3283）。
   九个 `creation_photo_avatar_*` 命令**全绑 `PixelPhotoAvatarManager`**（:1070-1171）
   → 要么按 `route` 分派，要么并列一组新命令（**倾向后者**：名字不同、前端不用猜）。
6. `photo_avatar/mod.rs` / `runtime_assets/mod.rs` 挂模块；TS `creation/api.ts`(:103-138) 加调用。

##### **不用碰**

`runtime_assets/loader.rs`（`validate_asset_manifest` 已接受 V7）、`manifest.rs`、
`frame_sequence.rs`（纯只读解析器）、`runtime_assets/migration.rs`（它管的是
v1 资产目录 → v3，与 DB schema 7 无关）。

##### 六个坑（按危险度）

1. 🔴 **`download_artifact` 会把 zip 拒掉**：`validate_artifact_bytes`(:1061) 硬编码
   `\x89PNG` 开头 + `width==2048 && height==2048`。`packFrameSequence` 交付的是
   **zip**（7.1~9.7 MB），必需一条独立的下载/校验路径（或把该函数参数化）。
   `MAX_ARTIFACT_BYTES = 20 MiB`(:1036) 对 zip 够用，不用改。
2. 🔴 **poll deadline 固定 300s 太紧**：`pixel_remote.rs:91`。**实测 `packFrameSequence`
   要 135.7 / 136.6 / 145.8 秒**（3 支现成 mp4，288 帧，本机；`scripts/_measure_打包耗时.py`
   一次性跑完即删）。加上排队、submit、下载 10 MB，300s 会**偶发**超时。
   → 帧路线的 deadline 提到 **900s**（与 Python 侧 `MAX_WAIT_SECONDS` 对齐）。
3. ~~**`photo_avatar_runs` 的 PK 是 `session_id`**(:473)：一条 session 只能存一条 run，
   `begin_*_revision` 走 `ON CONFLICT(session_id) DO UPDATE` → route 会被覆盖。
   帧路线要不要另起一张 run 表、还是复用同表换 route，**动工前先定**。~~
   → **已定：同表换 route**，理由见「已定设计」第 1 条。覆盖 route 是应有行为，不是 bug。
4. **`validate_latest_schema` 的列契约**：新段必须让 `db` 与 reference 一致；
   任何列名/类型笔误都会让**启动迁移**直接失败（不是某功能坏掉，是起不来）。
5. **`packFrameSequence` 是 0 算力但很慢**（1/4 抠像、2/4 四项验收各占大头），
   且中间产物上百个文件、按 `attempt` 分子目录。
6. **manifest 逐文件校验 sha256**：zip 内目录结构必须与 manifest 完全一致
   （Python 侧 `frames/packing.py:168-194` 定义 schema 7）。Python 侧会多写
   `_provenance` 等键 —— `RuntimeAssetManifestV7` 没有 `deny_unknown_fields`，可接受。
   目前**没有帧序列契约 fixture**：Rust `frame_sequence.rs` 与 Python `packing.py`
   可以对齐后新建一份共享 fixture（放 `src-tauri/tests/fixtures/`），
   比照 `pixel-style-v1-v2.json` 的做法。

##### 六个坑的处置状态

| # | 坑 | 处置 |
|---|---|---|
| 1 | `download_artifact` 拒 zip | **已定**：加一条 zip 校验路径（见「已定设计」6） |
| 2 | poll 300s 太紧（实测打包 136s） | **已定**：帧路线传 **900s**（见「已定设计」7） |
| 3 | runs 表 PK = session_id | **已定**：同表换 route（见「已定设计」1） |
| 4 | `validate_latest_schema` 列契约 | ✅ v14 已过（890 passed）；新段照抄 v12 的重建顺序即可 |
| 5 | `packFrameSequence` 慢 + 中间产物按 attempt 分目录 | 沿用；帧路线只有 2 个 attempt 级目录 |
| 6 | manifest 逐文件校验 sha256 / 没有契约 fixture | 待 6b：建一份共享 fixture 放 `src-tauri/tests/fixtures/` |

##### 参考：现有资产长什么样

`apps/desktop/public/builtin-pets/07-long-hair-cat/manifest.json`（schema 7，288 帧
`frames/idle-combo/f0000.webp…`，`anchorPolicy: "fixed"`，`semantics` 9 个键全指
`idle-combo`）+ `frames/idle-combo/` 共 7.11 MiB。**这就是第 6 片要产出的形状**。

## 五、已定决策（2026-09-13 老王确认）

1. **job 结果不跨重启 → 接受，但服务侧存中间 mp4 兜底。**
   不加 job 结果持久化（要动那套不可变审计，成本高、只省一单 5.69）。
   真正要防的是**用户中途关应用**（后端跟着死）—— 所以把 mp4 落在
   `scratch/<sessionId>/motion-source.mp4`：重试同一 session **直接复用，不重付视频**；
   用户点「重新生成」时显式要求重跑（否则会一直复用旧的）。
   白做代价从 **5.69 算力 → 0**。
2. **zip 走现有 artifact 端点**（扩 `.zip` 支持），不新加端点 ——
   新端点要把鉴权/404/tombstone/会话删除再写一遍，而现有端点只差扩展名判断 + media type。
3. **WebP 在服务侧转** —— zip 从 74MB 降到约 8MB，直接决定用户等多久。
4. **取景收敛全挂 → 让用户换照片**，但先把免费阶梯（0.90→0.75）试完；
   中途失败**不打扰用户**（自动重试静默），只在全挂时报
   「这张照片不适合，换一张正面坐姿、尾巴收拢的」。
   继续缩比换照片更糟：主体变小、与内置显示尺寸不一致。
