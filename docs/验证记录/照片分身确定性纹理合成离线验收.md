# 照片分身确定性纹理合成离线验收

## 结论

- 状态：**通过离线放行门**。
- 验收范围：Task 8 的 revision 13 只读回归、确定性 fake provider、真实 `Live2DRenderer` 动作证据和离线总门。
- 本记录不等同于真实宠物照片分身验收。fake 只证明 canonical 纹理链路、v5 角色包和动态动作能够贯通，不证明真实宠物身份相似度、照片保真度或供应商生成质量。
- Task 9 真实样本仍需单独授权和执行；本 Task 没有上传 `01-photos`，没有调用 lk888.ai，也没有产生费用。

## Revision 13 只读回归

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/验证照片分身确定性纹理合成.ps1 -Revision13Only
```

结果：通过。

- artifact：`services/appearance-generation/output/task-11-runtime/artifacts/a3eb6a61778c48cd85136aa4e5f67f83.png`
- SHA-256：`5503e493829423f316a348c9c96a54681383d0efb7a76417c9fa2ac0c653e9a0`
- alpha mismatch：`3,390,353`
- 模块内透明缺失：`54,911`
- 缺失区域 RGB 非零数量：`0`
- 旧 raw artifact 被 `compose_canonical_texture` 的 opaque provider 输入门拒绝。
- jobs 目录文件状态和 revision 13 artifact 在分析前后保持不变；revision 13 仍是失败且不可安装的历史产物。

## 离线总门

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/验证照片分身确定性纹理合成.ps1
```

结果：`offline-gate: PASS; revision13 immutable; no provider call performed`。

总门依次执行并通过：

- Python photo-avatar backend 测试；
- 前端测试、typecheck 和 build；
- Rust 测试；
- Rust fmt check；
- `git diff --check`。

每个长命令均由真实 `Start-Process -PassThru` wrapper 启动，wrapper 将子进程退出码写入临时 sentinel，父脚本读取 sentinel 并在 120 秒无日志增量时终止。没有使用伪造任务 ID 或无限等待。

## Fake UI 证据

命令：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File scripts/录制照片分身动作证据.ps1 `
  -Fixture balanced-success `
  -Output 'docs/验证记录/证据/照片分身确定性纹理合成-fake'
```

证据目录：`docs/验证记录/证据/照片分身确定性纹理合成-fake/`

`证据索引.json` 记录：

- package schema：`5`；renderer：`cat-spatial-live2d-v1`；`rendererReady=true`；`runtimeCheckPassed=true`；`rawManifestHash=true`；
- viewport：`420 x 520`；
- 八个动作：`breathing`、`blink`、`ear-twitch`、`tail-idle`、`pointer-focus`、`pet-happy`、`sleepy-yawn`、`half-stand-stretch`；
- 每个动作均有 `neutral`、`peak`、`fallback` 三帧，共 24 帧；另有 pet 和 drag 两种打断帧；
- 所有动作的 neutral/peak 与 peak/fallback 像素差均大于 20，最小记录值为 `518`；
- 所有截图非空且尺寸一致；`zeroV3OrStaticFallback=true`。

人工抽查了中性、峰值和打断截图中的脸、眼睑、耳根、胸腹、尾根、内部孔洞、白线和纹理错岛。每张截图只出现一只 fixture 宠物，没有标准猫、v3 角色或静态 fallback。fixture 使用高对比合成棋盘纹理来避免把 fake 误读为真实宠物身份证据。

## 数据与声明边界

- provider：deterministic fake provider；无网络请求。
- 真实供应商调用数：`0`；真实照片上传数：`0`；费用：`0`。
- fake 证明的是 canonical 合成、v5 manifest、真实 renderer 动作、打断状态和离线校验链路；不证明真实宠物可辨认性、身份相似度、毛色/花纹保真或模型质量。
- revision 13 的原始 hash、失败事实和不可安装状态被保留，未覆盖、修改或删除。
