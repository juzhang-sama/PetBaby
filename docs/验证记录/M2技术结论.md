# M2-P1A 生命感技术结论

## 阶段判定

- 总体：通过，可进入 M3（体验清单 L-07 校准窗口补测后回填）
- 领域事件与行为决策：通过
- 无惩罚状态：通过
- 分层资产与动画：通过（呼吸/眨眼/点击弹跳/拖拽抱起落地）
- 渲染档位联动：通过
- 校准窗口：通过（功能实现，L-07 人工补测）
- 性能：通过（companion CPU P95 0.391%）
- 体验人工验收：通过（L-01~L-06、L-08 实测；L-07 待补）

## 实测证据

| 能力 | 实现策略 | 结果 | 证据 |
|---|---|---|---|
| 事件决策 | decide() 纯函数 + 冷却 | 通过 | `decision.test.ts` 8 项 |
| 状态演化 | evolveState + Rust StateStore | 通过 | `state.test.ts` 6 项、`pets/state.rs` 测试 |
| 分层渲染 | LayeredSprite 居中锚点 + getBodyBounds | 通过 | `layered-sprite.test.ts` 3 项 |
| 动画 | PetAnimator（呼吸/眨眼/弹跳/抱起/落地复位） | 通过 | `pet-animator.test.ts` 9 项 |
| 档位 | RenderScheduler 三档 + ticker 联动 | 通过 | `render-scheduler.test.ts` 3 项 |
| 校准 | calibration 窗口（懒创建）+ state 表持久化 | 通过 | 命令编译运行正常 |
| 性能 | companion 档位 300s 采样 | 通过 | CPU P95 0.391%、内存 264.84 MB |

## M2 期间修复的环境与遗留问题（已归档）

1. WebView2 硬件加速在透明窗口合成失败 → `additionalBrowserArgs: --disable-gpu`
2. 位置恢复误保留 Aero Snap 全屏尺寸 → 超 95% 显示器尺寸回退默认
3. 全屏避让误判桌面（Progman 全屏窗口）→ 排除桌面 shell 窗口类
4. 双击弹跳与翻转叠加变形 → 双击只弹跳、偏移自动复位
5. 设置/校准窗口预加载 state 未注入 → 托盘懒创建

## M3 冻结决策

- **保留当前实现**：行为决策链路、无惩罚状态、LayeredSprite、PetAnimator、档位联动、校准窗口、`--disable-gpu`、全屏桌面排除。
- **M3 输入**：分层资产规范（body/eye-open/eye-closed/accent）作为生成链路资产输出目标；manifest v1 表达身份约束（正面坐姿、透明背景）。
- **M2 后置人工项**：L-07 校准窗口补测；完整 8 小时挂机；release 构建复测。

## 下一阶段条件

自动化测试（45 前端 + 25 Rust）与性能预算全部达标，体验验收通过。可编写 `M3生成链路实验计划.md`（云端形象生成实验，独立工具链，不动桌宠主流程）。

