# 首只真实照片分身验收

> **⚠️ 历史记录（旧路线）：** 本验收对应「动画电影软萌风 + Live2D」旧路线（2026-08-16），已于 **2026-08-18** 起被**像素照片分身路线**（pixel-style-v1）取代。本记录仅作历史审计留痕，不代表当前路线结论。当前视觉规范：`docs/设计/猫咪A风格视觉规范.md`（像素风）。

## 当前结论

- 状态：首个单照片探索样本已完成供应商链路，但视觉与关键动作验收失败，禁止安装。
- 授权时间：2026-08-16 10:38:05 +08:00。
- 授权范围：使用非人类宠物素材进行真实生成与结果收敛。
- 产品目标：A：动画电影软萌风，保留可辨认的宠物身份、真实体型和完整 v5 Live2D 动作。

## 调用与费用边界

- 用户未设置额外次数或费用上限。
- 工程硬上限仍按领域合同执行：`analyzeIdentity`、`completeAppearance`、`renderTextureAtlas` 每步最多三次总尝试。
- 单只角色预计成功路径为 3 次远端步骤提交，失败关闭上限为 9 次；Task 13 两只角色预计 6 次，失败关闭上限为 18 次。
- 单价与预计总费用：未确认。取得实际供应商和模型的可核验价格前不得发送请求。

## 数据生命周期

- 原图和临时产物不保留一周。
- 接受、放弃、取消或会话终止后立即删除本地原图和临时产物。
- 受控后端在对应终态立即删除源图片、临时产物和可下载地址；删除失败进入 `cleanupPending`，不得声明清理完成。
- 仅保留不含图片和密钥的审计元数据：授权时间、供应商、API/模型版本、隐私政策版本、调用次数、费用、删除请求状态和内容摘要哈希。

## 供应商与隐私预检

用户声明的目标供应商为 OpenAI，目标模型名为 `GPT-image2.0`。当前实现和本机配置存在以下不一致：

- 新照片分身桌面端只连接 `ControlledBackendProvider`，要求 `PHOTO_AVATAR_BACKEND_BASE_URL` 和 `PHOTO_AVATAR_BACKEND_TOKEN`。
- 当前进程未配置上述两项，运行时会使用 `UnconfiguredPhotoAvatarProvider`。
- 本机已有的是历史实验服务的 `LK888_API_KEY`、`LK888_BASE_URL` 和 `LK888_MODEL=gpt-image-2` 配置；`lk888.ai` 是额外的数据接收方，不能等同于 OpenAI 直连。
- 当前进程未配置 `OPENAI_API_KEY`。
- 仓库中没有实现 `/v1/photo-avatar/steps`、`/v1/photo-avatar/jobs/*` 和 `/v1/photo-avatar/sessions/*` 的生产受控后端服务。
- OpenAI 官方文档页面在本次预检中被当前网络策略和 Cloudflare 拦截，尚未取得可引用的模型、价格、数据保留和隐私政策版本。
- 尚未取得受控后端删除 API 的真实响应，也未确认上游供应商是否提供对应删除能力。

## 发请求前必须关闭的阻塞

1. 明确真实数据接收方是 OpenAI 直连还是 `lk888.ai` 聚合平台。
2. 若为 OpenAI 直连，提供受控后端使用的凭据和已确认可用的精确模型 ID；密钥不得进入桌面前端或版本库。
3. 若为 `lk888.ai`，必须记录其隐私政策版本、远端保存期限和删除能力，不能只记录上游模型名称。
4. 实现并部署受控后端合同，配置 HTTPS 地址和令牌。
5. 确认实际单价并填写预计费用；即使用户不设业务上限，也保留 18 次远端步骤提交的工程硬上限。
6. 指定本次允许上传的具体照片文件；未指定文件不得从本机目录自行挑选并上传。
7. 对受控后端删除接口执行一次不含真实照片的探针，记录状态码和响应后再上传。

## 证据

- 真实请求数：0。
- 实际费用：0。
- 上传照片：无。
- 远端删除响应：未执行。
- v5 角色包与动作证据：未生成。

## Task 10 调用前闸门（2026-08-16）

- 用户授权时间沿用本记录：2026-08-16 10:38:05 +08:00；授权目录为 `D:\PetBabyAssets\first-pet\01-photos`，只读核对为 8 张非人类宠物照片。本 Task 未读取照片内容、未上传照片。
- 当前确定的数据接收方是 `lk888.ai`，不是 OpenAI 直连。受控后端以 `gpt-4o` 执行身份分析和外观补全，以 `gpt-image-2` 执行纹理图集生成。
- 同意文案版本为 `photo-avatar-third-party-ai-lk888-no-delete-v2`。v2 风险仍包括：lk888.ai 隐私政策版本、保存期限和单价不可核验；上游公开删除 API 不受支持，只能确认自有受控后端删除。
- Task 13 两只角色的成功路径预计 6 次生成步骤提交；每步最多 3 次总尝试，两只角色的失败关闭上限为 18 次。单价及预计总费用仍不可核验，因此本记录不构成真实调用 go 决策。
- 无照片探针通过：服务 PID `22156`，loopback 地址 `http://127.0.0.1:58396`，`/healthz` 为 200，未授权步骤请求为 401，空 session DELETE 为 200 且返回 `backendCleanup=deleted`、`upstreamCleanup=unsupported`、`provider=lk888`；探针时生成调用数为 0，自有 state dir 图片数为 0。服务已在测试 `finally` 中停止。
- fake 跨进程合同回归已验证真实 FastAPI、Rust `ControlledBackendProvider`、三个步骤、artifact 下载、v5 builder、自有后端删除、上游删除不支持及零标准猫 fallback；该结果只证明开发期合同，不证明真实照片生成质量。
- 生产域名、TLS、令牌分发、日志监控、备份恢复和服务运维尚未完成。真实照片验收仍未执行，真实 lk888.ai 请求数为 0，真实费用为 0，不得宣称首只真实照片分身验收通过。

## Task 11 完整 UI 实测（2026-08-16）

- 状态：`BLOCKED`。受控后端与 debug Tauri 真实启动成功，但 Computer Use 无法取得产品窗口，未进入照片选择或提交阶段。
- 启动观测：后端 PID `5756`、Tauri PID `21900`，`GET /healthz` 返回 HTTP `200`；专用 state dir 在阻塞前为 jobs `0`、artifacts `0`、tombstones `0`。
- UI 硬阻塞：按 `node_repl` + `@oai/sky` 执行 `list_apps()` 和 `list_windows()`；两次返回列表均不含 `Desktop Pet M0`。系统只读进程观测同时确认 PID `21900` 正常响应且存在标题为 `Desktop Pet M0` 的透明主窗口。由于该窗口未由 Computer Use 返回，不能伪造窗口句柄、猜测坐标、使用 PowerShell/UIA、DOM 或接口绕过完整 UI。
- 指定素材只读计数仍为 8；照片上传数 `0`，v2 告知未接受，lk888.ai 真实步骤提交数 `0`，实际费用 `0`。
- 身份 profile、八动作与两种打断、接受安装、manifest v5 和三域终态清理均未开始，不能判定通过。
- 失败角色或证据未提交；没有安装角色，也没有创建远端 provider session。lk888 上游删除能力仍只能记为 `unsupported`，本次因为远端提交数为 0 而没有上游删除对象。
- 证据：`docs/验证记录/证据/首只真实照片分身/启动与阻塞.json`、`photo-avatar-backend.stderr.log`、`tauri.stdout.log`、`cleanup.json`。

## Task 11 续接：nullable 恢复与首次真实调用（2026-08-16）

- 状态仍为 `BLOCKED`，但原 Computer Use 窗口阻塞和 pre-begin durable draft 的 status 阻塞已分别解除。
- nullable status 修复已通过 TDD：Rust store/manager/command 聚焦测试、photo-avatar 85/85、前端 preview/view/api 42/42、typecheck、frontend build、cargo fmt 和 diff-check 均通过；证据位于 `.omo/evidence/task-11-nullable-status/`。
- 新启动后端 PID `19532`、Tauri PID `29036`，`/healthz=200`。Computer Use 唯一返回“桌面宠物设置”，原 durable draft 恢复为 collecting。
- 通过真实系统文件选择器选择 `D:\PetBabyAssets\first-pet\01-photos` 中精确 8 张照片；接受 v2 告知后，照片已提交给 lk888.ai。不能再写“上传数 0”或“费用 0”；实际费用未知。
- `gpt-4o` 的 `analyzeIdentity` attempt 1 成功；job `93e7e4e4bf454de49ddf7f3d2cec066a`，provider session `4eff9e841b34462aa66138700f828a5b`。
- `gpt-4o` 的 `completeAppearance` attempt 1 失败；job `34fbe1360b3141b1a5bdb22bc48b489d`，结构化错误码 `invalidInput`，安全化消息 `provider failed`。两次文本请求后停止，`gpt-image-2` 媒体提交数为 0，artifact 数为 0。
- 未生成完整 profile/atlas，未进入 Live2D 动态预览、八动作、两种打断、安装、manifest/hash 或三域清理；失败角色不得提交。
- 当前 draft 未取消，原照片未按取消路径清理；自有后端只有两个 job JSON、无 artifact；lk888 上游删除仍为 `unsupported`。
- 证据：`八张照片计数-nullable修复后.jpg`、`v2风险告知.jpg`、`真实生成-completeAppearance失败.jpg`、`photo-avatar-backend.stdout.log`、两个后端 job JSON。
- `completeAppearance` 的 text-only wire 已按 TDD 修复：无图请求改用字符串 content，带图请求保持多模态数组；客户端 32/32、跨进程 1/1、后端全量 141/141。修复后未获调用预算确认，因此没有第二次真实请求，不能把该推断写成已由 lk888 复验通过。

## Task 11 revision 8：atlas 提交响应缺少 task ID（2026-08-16）

- 用户授权范围内通过完整 UI 只点击一次“重新生成”，产生 revision 8；没有从命令行调用 `/steps`，也没有第二次 UI 点击。
- `analyzeIdentity` attempt 1 成功；`completeAppearance` attempt 1 暂时失败、attempt 2 成功。
- `renderTextureAtlas` attempt 1 至 3 均失败，job 分别为 `25904167e1c34f6b9a5c0faf20652f22`、`a36a25c9ed7547b28ed939195f4a942e`、`068fe0c12c51479287ae3fbec6da8bb8`；三者 `lk888TaskId` 均为空，artifact 数为 0。
- 三次安全诊断均为 `media-submit:data=object;error=absent;task_id=missing`。这只证明 HTTP 200 响应中的 `data` 是对象且没有当前合同要求的 `task_id`，不能据此猜测响应字段或声明供应商已创建异步任务。
- UI 终态仍为 `provider failed`；未进入完整动态预览、身份/体型检查、八动作、两种打断、接受安装或三域清理。
- 为定位供应商响应形状，新增只输出白名单候选字段名称与类型的诊断；禁止记录字段值、URL、prompt、图片、密钥和响应正文。TDD 为单测 RED 后 GREEN，lk888 客户端 `35/35`、Python 全量 `173/173`、`git diff --check` exit 0。
- 当前结论仍为 `BLOCKED`。只有取得真实响应形状并完成对应合同修复、复审和新一轮完整 UI 验证后，才可继续 Task 11 后续验收。

## Task 11 revision 9：白名单字段诊断（2026-08-16）

- 完整 UI 只点击一次“重新生成”，产生 revision 9；`analyzeIdentity` 和 `completeAppearance` attempt 1 均成功。
- `renderTextureAtlas` attempt 1 至 3 仍失败，job 为 `1c3446b7e5b6447ba2a3e415058c3617`、`851e9398d1ae4e31a71b0a961c6b9687`、`412bf99041884d3f9f436299c2d26024`；三者 `lk888TaskId` 均为空。
- 安全诊断仍为 `media-submit:data=object;error=absent;task_id=missing`，没有出现任何白名单候选字段；因此响应不是已支持的 `task_id`，也没有证据支持把 `id/taskId/images/url/result` 等字段当作结果。
- 为区分空 `data` 与顶层 HTTP 200 错误包，诊断增加 `data_fields`、固定顶层字段类型和固定消息标签，不记录任何值。TDD 单测 RED 后 GREEN，lk888 客户端 `36/36`、Python 全量 `174/174`、`git diff --check` exit 0。
- revision 9 日志已保存为 `photo-avatar-backend.stderr.revision9-before-top-envelope-diagnostic.log` 等四个同前缀证据文件。
- revision 10 尚未开始；一次窗口操作因检测到用户输入而被取消，没有产生新 job，也没有重复提交。

## 样本 01：真实 preview 失败（2026-08-17）

- 使用授权素材中的照片 ordinal 1，通过完整桌面 UI 创建 session `session-2658-18cc970ede974a3c-1` revision `1`；没有从命令行直接调用 provider。
- `analyzeIdentity` 和 `completeAppearance` 均在 attempt 1 成功；`renderTextureAtlas` attempt 1 超时，attempt 2 成功，成功 provider task ID 为 `107734541`。
- 真实分析选择 `body-slender-v1`。canonical SHA-256 为 `4a39b6386b29d43647f351072576f5e66ea404e12f303f4f201b79842cce0bbe`，preview manifest SHA-256 为 `35bf7bfe46afdedd0b80da6cdf4dffee80061e08c8e4118ae07b0f0ba7e7f799`。
- 运行时 framebuffer 非空，但人工视觉发现头脸切片、眼睑/眼球错位、胸颈黑线和身体接缝；该 atlas 不满足完整动态最终预览的发布标准。
- `half-stand-stretch` 的 neutral、peak、fallback 截图逐字节一致，SHA-256 均为 `9a292fef930a5e9f6455fe526849b1800fec83d1909c7325e79441327a55eed4`。其他七动作和两种打断产生不同帧，不能抵消这一关键动作失败。
- 当前会话错误进入 `previewReady`，尚未接受、未安装、未取消，也未执行本地/自有后端终态清理；lk888 上游删除能力仍为 `unsupported`。
- 结论：`FAILED`。停止后续真实样本，先补三体型真实像素动作门并修复 atlas 装配质量；禁止把本结果记为技术成功或视觉通过。
