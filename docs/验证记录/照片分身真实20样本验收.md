# 照片分身真实 20 样本验收

## 当前结论

- 状态：**DryRun 通过；样本 01 已作为探索样本执行并判定失败，样本 02 暂停**。
- 已冻结 20 个不可删样样本：1、2、4、8 张照片各 5 个。
- 样本 01 使用照片 ordinal 1，通过完整桌面 UI 调用 `lk888.ai`；实际费用未知。
- 样本 01 已生成 v5 preview package，但视觉拼接和 `half-stand-stretch` 动作门失败，未接受、未安装，也未执行终态清理。

## 授权边界

- material：`non-human-pet`
- provider：`lk888.ai`
- model：`gpt-image-2`
- sample count：`20`
- maximum attempts per sample：`3`
- 授权记录只保存在素材目录外层，不进入版本库；矩阵只记录授权文件 SHA-256，不记录照片路径或文件名。

## DryRun 命令与结果

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File scripts/照片分身真实20样本验收.ps1 `
  -PhotoRoot 'D:\PetBabyAssets\first-pet\01-photos' `
  -AuthorizationRecord 'D:\PetBabyAssets\first-pet\照片分身真实20样本授权.json' `
  -DryRun
```

结果：`dry-run: PASS; samples=20; distribution=1/2/4/8x5; photos=8; network=0`。

矩阵：`docs/验证记录/证据/照片分身真实20样本/样本矩阵.json`

矩阵 SHA-256：`a05a35c3b94c1881e29467f44faf725622041ef5c6db14c455f9e68a7be0f24a`

矩阵绑定：

- Task 8 release commit：`4855e9d13ec0f027864112bf4bceeef59a4e1209`
- body module：真实 UI 分析结果允许在 `body-slender-v1`、`body-balanced-v1`、`body-rounded-v1` 三个白名单模块中选择；矩阵分别冻结三个模块的 binding，不预先伪定实际体型。
- API contract：`lk888-media-generate-v1`
- work canvas SHA-256：`4bdb94c46d7de2726a9d15453b79ff143a6e1bd860a5ab00778a8474f6778223`
- region map SHA-256：`96bebc3bc6543c41c89f21429b84116a93f1cd91ee9970f00a73d6e4c46d55a9`
- module contract SHA-256：按实际 `bodyModuleId` 从矩阵 `moduleBindings` 选择对应 hash。
- minimum change ratio：`0.95`

同一输入连续执行两次得到相同矩阵 SHA-256；矩阵只含照片 ordinal、hash、字节数和尺寸，不含照片文件名、路径、原始字节、base64、API key 或 provider raw 数据。

## Execute 前置条件

真实执行前仍需完成：

- 审阅并接受冻结矩阵；
- 实现真实桌面 UI 样本驱动，不允许从命令行直接调用 provider；
- 确认受控后端与桌面端真实 PID 存在，端口 `8787` 和 `1420` 监听；
- 确认 Task 8 与实现路径 clean，授权记录和矩阵 hash 未变化；
- 每个 sample 最多三次 `renderTextureAtlas` attempt，并记录 task ID、耗时、错误码、raw/canonical/module/work/region hash、coverage 和 Live2D load；
- 终态删除 provider raw 文件，只保留脱敏审计数据。

## 样本 01 探索执行（2026-08-17）

- sample：`sample-01-1p-01`；照片 ordinal：`1`；session：`session-2658-18cc970ede974a3c-1`；revision：`1`。
- `analyzeIdentity` attempt 1 成功；`completeAppearance` attempt 1 成功；`renderTextureAtlas` attempt 1 超时、attempt 2 成功。
- 成功 atlas provider task ID：`107734541`；实际体型：`body-slender-v1`。
- canonical SHA-256：`4a39b6386b29d43647f351072576f5e66ea404e12f303f4f201b79842cce0bbe`；preview manifest SHA-256：`35bf7bfe46afdedd0b80da6cdf4dffee80061e08c8e4118ae07b0f0ba7e7f799`。
- 真实 WebGL framebuffer 非空，七个动作与两种打断产生不同截图；但 `half-stand-stretch` 的 neutral、peak、fallback 完全相同，三者 SHA-256 均为 `9a292fef930a5e9f6455fe526849b1800fec83d1909c7325e79441327a55eed4`。
- 人工视觉判定失败：头脸切片边界明显、眼睑/眼球错位、胸颈黑线、身体多处切片接缝，整体拼接感严重。
- 当前数据库已错误进入 `previewReady`，证明旧运行时门只验证 renderer/API 可用，未验证关键动作像素变化；该结果不得接受或安装。
- 本样本发生在矩阵升级为三体型白名单之前，同时也应视为探索样本；在本地修复并覆盖三体型真实 renderer 回归前，不执行样本 02。
- 脱敏证据索引：`docs/验证记录/证据/照片分身真实20样本/样本01-探索失败.json`。原照片、provider raw PNG 和渲染截图未进入版本库。

当前真实样本结果：`0/1` 通过；剩余 `19` 个样本未执行；实际费用未知。
