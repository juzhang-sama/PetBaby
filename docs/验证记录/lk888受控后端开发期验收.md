# lk888 受控后端开发期验收

## 结论

- 状态：开发期 fake 跨进程合同已通过；真实调用前闸门仍为 no-go。
- 本记录不代表真实照片验收通过，也不代表生产部署完成。
- 真实照片上传数：0；真实 lk888.ai 生成调用数：0；实际费用：0。

## 授权与调用边界

- 用户授权时间：2026-08-16 10:38:05 +08:00。
- 授权目录：`D:\PetBabyAssets\first-pet\01-photos`。
- 只读核对：8 张非人类宠物照片；本 Task 未读取内容、未上传。
- 数据接收方：`lk888.ai`，不是 OpenAI 直连。
- 模型分工：`gpt-4o` 用于身份分析和外观补全；`gpt-image-2` 用于 2048x2048 RGBA 纹理图集生成。
- 预计调用：Task 13 两只角色成功路径共 6 次生成步骤提交；每步最多 3 次总尝试，失败关闭上限共 18 次。
- 单价、预计总费用、lk888.ai 隐私政策版本和保存期限均不可核验，真实调用保持 no-go。

## v2 风险

- 同意文案版本：`photo-avatar-third-party-ai-lk888-no-delete-v2`。
- lk888.ai 上游公开删除 API 为 `unsupported`；开发期只能验证自有受控后端清理为 `deleted`。
- 生产域名、TLS、令牌分发、日志监控、备份恢复和服务运维尚未完成。
- fake 结果不包含真实身份质量、真实供应商可用性、真实费用或真实上游数据生命周期证据。

## fake 跨进程合同

- 启动真实 Uvicorn/FastAPI 子进程，在真实 `PipelineRunner` 的 `Lk888Client` 内注入 `httpx.MockTransport`；未访问 lk888.ai 网络。
- 独立 Cargo 子进程使用真实 Rust `ControlledBackendProvider` 通过 loopback HTTP 驱动 `analyzeIdentity`、`completeAppearance`、`renderTextureAtlas`、artifact 下载和 session DELETE。
- 断言最终 profile 为 `animated-film-soft-v1`、`body-balanced-v1`，v5 builder 输出 `schemaVersion=5` 和 `renderer=cat-spatial-live2d-v1`。
- 断言生成图集不等于标准猫 neutral texture，builder 安装的 texture 与下载 artifact 相同，不存在标准猫 fallback。
- DELETE 响应为 `backendCleanup=deleted`、`upstreamCleanup=unsupported`、`provider=lk888`。
- fake 上游生成调用为 3：`gpt-4o` 两次、`gpt-image-2` 一次；这 3 次均为本机 MockTransport 调用，真实调用为 0。

## 无照片探针

- 服务 PID：`22156`；loopback 地址：`http://127.0.0.1:58396`；测试结束后已在 `finally` 中停止。
- 健康检查：HTTP 200，`{"status":"ok"}`。
- 未授权步骤请求：HTTP 401，`{"code":"auth","message":"unauthorized"}`。
- 空 session DELETE：HTTP 200，`{"backendCleanup":"deleted","upstreamCleanup":"unsupported","provider":"lk888"}`。
- 探针时生成调用数：0。
- 自有 state dir 图片数：0。

## 发现并关闭的合同缺陷

- RED：FastAPI 真实 wire 使用 `partialProfile` 和 `artifactUrl`，Rust enum struct variant 原先只接受 `partial_profile` 和 `artifact_url`，跨进程稳定失败。
- GREEN：`ProviderStepResultWire` 对 struct variant 字段统一启用 camelCase，并加入 Python wire 直接回归。
- 该修复仅关闭桌面端反序列化合同，不改变生产部署与真实调用 no-go 结论。

## 证据

- `.omo/evidence/task-10/01-red-pytest.txt`
- `.omo/evidence/task-10/02-rust-cross-process.txt`
- `.omo/evidence/task-10/03-no-photo-probe.json`
- `.omo/evidence/task-10/04-fake-lk888-metrics.json`
- `.omo/evidence/task-10/05-green-focused-pytest.txt`
- `.omo/evidence/task-10/06-rust-wire-red.txt`
- `.omo/evidence/task-10/07-rust-wire-green.txt`
