# M4-P1B 创建闭环技术结论

## 阶段判定

- 总体：自动化验收通过；人工闭环实测（C-01~C-08）待用户回填后定级
- Rust 生成客户端：通过（submit/poll/download + 重试，wiremock 4 项测试）
- 创建域持久化：通过（迁移 v2：profiles/jobs/variants；job 状态机 3 项测试）
- 色键抠图 + 质量闸门：通过（flood-fill 内部洞检测拦截过度抠图，4 项测试）
- 任务管理：通过（后台轮询线程、进度查询、取消、断点恢复）
- 创建向导 UI：通过（上传→Key 配置→生成进度→候选选择→编译→桌面）
- 资产编译：通过（manifest v1 输出 + 单图降级标记，3 项测试）
- 多宠切换：通过（pet-asset 协议 + PetStage 资产化加载）
- 人工闭环：待用户实测

## 实测证据

| 能力 | 实现策略 | 结果 | 证据 |
|---|---|---|---|
| 生成客户端 | reqwest + wiremock 测试 | 通过 | `lk888.rs` 4 项 |
| 持久化 | 迁移 v2 + CreationStore | 通过 | `creation/mod.rs` 3 项 |
| 抠图质量 | 色键 + flood-fill 内部洞 | 通过 | `cutout.rs` 4 项 |
| 任务管理 | 轮询线程 + resume | 通过 | 编译运行正常 |
| 创建流程 | CreationFlow 状态机 | 通过 | `creation-flow.test.ts` 5 项 |
| 资产编译 | manifest v1 + 降级 | 通过 | `compiler.rs` 3 项 |
| 多宠资产 | pet-asset 自定义协议 | 通过 | 协议编译运行正常 |
| 全量闸门 | 执行M4检查.ps1 | 通过 | 前端 51 + Rust 42 + Python 24 |

## 产品状态

- 小范围测试版能力齐备：创建闭环（照片→候选→桌面）、多宠管理、离线可用、降级保护、隐私提示、API Key 应用内配置。
- 待用户实测项（C-01~C-08）通过后即达到"小范围测试版"交付标准。
- 后续迭代（非 M4 范围）：神态精进、rembg 增强、声音、Agent、主动养成、多宠同屏。

## M4 未验证范围

- 人工闭环全流程（等待用户实测回填）
- 8 小时挂机内存趋势（全程未执行）
- release 构建性能复测
- Windows 10 真机

## 下一阶段条件

用户实测 C-01~C-08 回填本结论。全部通过后，路线图 M4 闸门达成——桌面宠物小范围测试版交付；后续迭代按产品路线推进（Agent/声音/养成等）。
