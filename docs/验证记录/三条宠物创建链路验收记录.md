# 三条宠物创建链路验收记录

## 1. 验收范围与环境

本记录覆盖上传创建、引导组合、直接认领三条链路的自动化回归、隔离故障注入与真实 Tauri 操作。自动化结果只证明代码契约；真实窗口、外部服务和文件删除仅按本轮实际执行范围记为通过。

| 项目 | 值 |
|---|---|
| 验收日期 | 2026-08-11 |
| 验收基线 | `b01f6276589d4cb41d0b3de3f4917049db5c8280` |
| 分支 | `feature/three-creation-paths-design` |
| 工作树 | `D:\petBaby\desktop-pet\.worktrees\three-creation-paths-design` |
| 操作系统 | Windows NT 10.0.26200.0 |
| Node / npm | v24.16.0 / 11.13.0 |
| Rust / Cargo | 1.97.1 / 1.97.1 |
| 隔离应用标识 | `com.desktop-pet.task15-8e5480bf` |
| 隔离开发端口 | 14321 |

自动化 TEMP/TMP、Tauri 配置、app-data 和日志均位于本轮唯一隔离根。未操作默认用户 app-data；未修改或终止其他 worktree 的进程。真实上传使用仓库合成素材 `apps/desktop/public/test-assets/pet-probe.png`，未使用个人照片。API Key 仅通过 UI 判断是否已配置，本文档不记录其值。

## 2. fresh 自动验证

以下命令在停止本轮 Tauri PID 树后串行执行，不复用历史结论：

| 命令 | 结果 | 计数或说明 |
|---|---|---|
| `npm test` | 通过 | Vitest 67 个文件、605 个测试通过，0 跳过 |
| `npm run typecheck` | 通过 | `tsc --noEmit`，退出码 0 |
| `npm run build` | 通过 | Vite 112 个模块；creation content 与两种 runtime 均进入产物 |
| `cargo test --lib` | 通过 | 首轮 450 通过；最终重跑 450 通过、0 失败、0 ignored |
| `cargo fmt --all -- --check` | 首次发现格式差异，机械格式化后通过 | 仅 `adoption.rs` 新测试一处折行，无语义修改 |
| `git diff --check` | 通过 | 无空白错误 |

skip 审计只发现 4 个既有 `skipIf(!hasLocalCubismSdk)` 条件测试；本轮实际 Vitest 输出为 0 跳过。未发现 `.only`、Rust `#[ignore]` 或未解释的静态成功态。

最终 Rust 验证的第一次尝试在 240 秒工具上限处超时，遗留的精确自有 cargo/test PID 树已验证后终止；测试进程当时已连续 5 秒无 CPU 增量。原命令随后 fresh 重跑在 86.8 秒通过，唯一被 harness 标记超过 60 秒的测试为 `creation::adoption::tests::retry_fails_closed_when_adoption_provenance_is_missing_or_corrupt`，并最终返回 `ok`。本记录不把超时尝试计为通过。

## 3. 隔离故障注入

| 场景 | 结果 | 事实 |
|---|---|---|
| 组合草稿跨设置关闭/应用重启 | 通过 | 同一 session、recipe 与步骤恢复；未重复创建 pet |
| finalizing 响应丢失/进程终止 | 通过 | 重启收敛到 completed 或可重试事实；无重复 pet |
| 同一认领模板并发唯一 | 通过 | 同一模板只保留一个 pet/session |
| 删除认领实例后释放模板 | 通过 | `cat-misty` 恢复可认领；正式 25 文件聚合 SHA 未变化 |
| 单个组合部件损坏 | 通过 | 只影响相关部件，健康配方仍可用 |
| 单个认领模板损坏 | 初始失败，TDD 修复后通过 | 坏模板带 `unavailableReason`，其余模板仍可预览/认领 |
| 桌面窗口不可用 | 通过 | 旧宠物保留，新 session 可重试 |

故障数据只写入临时 app-data；未破坏 `pet-live2d-v1` 或 tracked `creation-content`。正式认领目录在删除前后聚合 SHA256 均为 `e2d0af9658e7d00dd93e6688c993dc4b5e4b086843261e1eafd6a103c7424d62`。

## 4. 真实 Tauri 三链路结果

### 4.1 引导组合

- 三入口均可见。
- 真实覆盖身体、耳朵、眼睛、鼻嘴、尾巴、毛色/花纹和命名，生成动态候选并完成 `Task15组合猫8e5480bf`。
- 正式 pet 为 `pet-7cd8-18caab3c858f8c34-2`，session 为 `session-7cd8-18caab3c858f0764-1`。
- 真实操作发现恢复步骤与“请选择身体”文案不一致，以及候选锁定后 UI 仍允许伪返回；均先写 RED，再做最小修复。draft 阶段的返回修改仍保留。

### 4.2 直接认领

- 目录显示 8 个正式模板；真实动态打开并完成 `cat-misty`，其余模板保留可认领状态。
- 初次认领：`Task15雾雾8e5480bf`，pet `pet-8ad0-18caad25939567f0-11`，session `session-8ad0-18caad2593972220-12`。
- 经用户对精确目标的 action-time 确认后，仅删除上述隔离测试 pet。UI 提示删除成功，active 安全回退默认 Live2D，组合宠物和其余模板未受影响。
- 重新认领：`Task15雾雾重领8e5480bf`，pet `pet-12a0-18caaf0c238d87b0-3`，session `session-12a0-18caaf0c238f818c-4`；同模板再次显示已认领，其余 7 个仍可认领。

### 4.3 上传创建

- UI 明示原图会上传到第三方 gpt-image-2；选择的文件为仓库合成图 `pet-probe.png`。
- 外部生成成功：job `job-507c-18cab058435b6ec0-2`，task `101507582`，session `session-4504-18caae6c92907be0-2`，pet `pet-4504-18caae6c9290c618-3`。
- 唯一候选进入 review；两次候选截图 SHA256 分别为 `10d6865b3c347556dcfcd686fed6ac1d242dc89fb307dbaa25836653207941bf` 与 `6e583130e79604242f4a133e386c1d4187ec7a257ab916841cc6a92db74d79e1`，帧不同；棋盘格背景下未见不透明底色。
- 命名 `Task15上传猫8e5480bf` 后 finalize，UI 仅在成功切换后显示“宠物已出现在桌面”。数据库名称长度为 17 个字符，未发生截断。
- 真实切换顺序 upload → composer → adoption → default Live2D → upload 全部成功；重启后 upload 仍为“当前使用”。
- 上传 pet 未删除：本轮没有获得对该精确目标的删除确认，因此按安全约束保留。

## 5. 动态与运行时证据

- 组合、认领和上传正式用户宠物均编译为 `animated-image`，未记录 `static-png` 降级。
- 组合/认领真实桌面运行时的连续帧不同，肉眼可见轻微胸腹呼吸与整体摆动；脸眼区域未随呼吸明显形变。
- 上传候选双帧哈希不同；切换、重启与 active 恢复通过。隐藏设置窗口后，Sky 没有把透明桌宠窗口枚举为可单独定位窗口，因此没有为 upload 正式桌面窗口追加独立像素哈希，不能把候选双帧扩写为透明窗口双帧证据。
- 默认 Live2D 的正式文件哈希契约、切换到默认及切回用户宠物均通过；未出现 fallback 或 hash mismatch。

## 6. 本轮 TDD 修复

1. Tauri 资源 glob 打包会扁平化 `creation-content`：改为目录映射并增加配置契约测试。
2. 单个认领模板内容损坏会阻塞整个目录：增加 Rust RED、`unavailableReason` 契约和 UI 局部禁用。
3. 组合 restore 已恢复 recipe/步骤但显示空身体文案：恢复态文案与事实对齐。
4. candidateReady 后端已锁定但 UI 仍允许返回：锁定前后导航，保留 draft 返回修改。
5. Windows EOL 导致默认 Live2D cdi3 清单哈希漂移：增加逐文件真实 SHA 契约、固定正式 JSON EOL 并更新 cdi3 哈希。

## 7. 未执行项与已知限制

- 未逐一打开其余 7 个认领模板的真实动态预览；只验证目录、可认领状态、`cat-misty` 动态预览与完整释放闭环。
- 未在真实外部服务中制造生成失败；生成失败重试只由自动化故障注入证明。
- 未在真实 GUI 中制造 upload 编译失败，也未执行未完成 upload 的“放弃”；相关精确清理由自动化覆盖。
- 未删除 upload pet，因为没有该精确目标的 action-time 用户确认。
- 未执行 8 小时挂机、release 性能复测或 Windows 10 真机兼容测试。

因此，本轮可确认三条创建链路的核心创建、切换和重启闭环；上述未执行边界不得记为真实 GUI 通过。提交后仍需独立审查，不以本记录自行声明 Approved。

## 8. 证据位置

- 组合素材总览：`docs/验证记录/证据/三条宠物创建链路/组合素材总览.png`
- 认领目录总览：`docs/验证记录/证据/三条宠物创建链路/认领目录总览.png`
- 本轮隔离日志：唯一 TEMP 根下 `logs/`（ignored，不含 API Key）
- 详细执行流水与安全事件：ignored `.superpowers/sdd/task-15-report.md`
