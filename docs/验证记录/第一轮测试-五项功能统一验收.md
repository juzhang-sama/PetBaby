# 第一轮测试：五项功能统一验收

## 验收结论

截至 2026-08-12，在 `feature/first-round-pet-improvements` 的 `d807ef1c1d8a3dbdfc58f955e5f073f4553d6fd2` 上，抠图安全、宠物资料编辑、桌宠显示尺寸、真实校准和 Windows 双窗口模式的统一自动化门禁通过：Rust `all-targets` 共 674 项通过，前端共 921 项通过、4 项按既有环境条件跳过，Rust 全目标检查、TypeScript 类型检查和生产构建均成功；跨功能 focused 组合及 Windows 一键启动器静态/专用契约也全部通过。

本轮没有启动真实桌宠应用，没有双击或实际运行一键启动器的 Tauri 启动路径，没有操作真实 HWND、Explorer、真实数据库、真实偏好、真实 app-data、真实图片、真实 provider 或用户素材。下文“第一次真人测试”清单全部为 **未执行**。因此当前可声明的是：**五项功能统一自动化验收通过；Windows/Tauri 真人验收与发布验收尚未完成。**

## 基线、环境与边界

- 日期与时区：2026-08-12，Asia/Shanghai。
- 工作区：独立 linked worktree `D:\petBaby\desktop-pet\.worktrees\first-round-pet-improvements`。
- 分支：`feature/first-round-pet-improvements`；验收前 `HEAD=d807ef1`，工作树干净。
- 系统：Microsoft Windows NT 10.0.26200.0；Windows PowerShell 5.1.26100.8875。
- 工具链：Node.js v24.16.0、npm 11.13.0、Cargo 1.97.1、rustc 1.97.1。
- Rust 命令目录：`apps/desktop/src-tauri`；npm 命令目录：`apps/desktop`。
- 所有正式主闸门和 focused 命令严格串行执行。
- 自动化只使用单元测试、临时 SQLite/文件夹、Fake Win32 adapter、FakeIo、假 DOM、mock port、合成 RGBA/PNG 以及启动器临时 fake command。
- 本 Task 只允许新增本文档；未修改产品、测试或启动脚本。

## 输入文档与计划核对

本轮完整读取并整合了以下五份既有记录：

1. `第一轮测试-抠图安全验收.md`
2. `第一轮测试-宠物资料编辑验收.md`
3. `第一轮测试-桌宠尺寸验收.md`
4. `第一轮测试-真实校准验收.md`
5. `第一轮测试-Windows双窗口模式验收.md`

也完整读取了五份实施计划的最终验收 Task。编号以计划原文为准：抠图安全、真实校准和 Windows 双窗口模式为 Task 6；宠物资料编辑与桌宠显示尺寸计划各只有 5 个 Task，其最终验收实际为 Task 5。本文没有把不存在的 Task 6 当作证据。

## Fresh 串行主闸门

以下结果均在同一 `HEAD=d807ef1` 上 fresh、串行取得：

| 顺序 | 工作目录 | 命令 | 结果 |
| --- | --- | --- | --- |
| 1 | `apps/desktop/src-tauri` | `cargo test --all-targets --no-fail-fast` | exit 0；lib 674 passed、0 failed、0 ignored、0 measured；binary 0 tests；92.30 秒 |
| 2 | `apps/desktop/src-tauri` | `cargo check --all-targets` | exit 0；dev/test targets 检查通过 |
| 3 | `apps/desktop` | `npm test -- --run` | exit 0；80 个文件通过、1 个文件跳过；921 passed、4 skipped、0 failed；13.31 秒 |
| 4 | `apps/desktop` | `npm run typecheck` | exit 0；`tsc --noEmit` 通过 |
| 5 | `apps/desktop` | `npm run build` | exit 0；Vite 8.2.0 转换 80 个模块，141 ms |

生产构建实体核对：

| 入口 | 大小 | 结果 |
| --- | ---: | --- |
| `dist/index.html` | 650 bytes | 存在 |
| `dist/settings.html` | 22,276 bytes | 存在 |
| `dist/assets/` | 11 个文件 | 存在 |

`apps/desktop` 源根与 `dist` 均只有 `index.html`、`settings.html` 两个 HTML 入口；Vite `rollupOptions.input` 也只注册这两项。旧 `calibration.html`/`src/calibration.ts` 不存在。

## 跨功能 focused 自动化矩阵

### Rust

所有命令均串行，结果均为 exit 0、0 failed、0 ignored：

| 命令 | 有效执行 | 主要覆盖 |
| --- | ---: | --- |
| `cargo test --lib storage::migrate::tests:: -- --test-threads=1` | 43 passed | v8 资料迁移、回填、Gregorian CHECK、回滚、幂等及整库迁移回归 |
| `cargo test --lib profile -- --test-threads=1` | 62 passed | 资料类型/repository/命令/mutation，并包含匹配到的 motion profile 回归 |
| `cargo test --lib calibration -- --test-threads=1` | 13 passed | 按宠物 typed calibration、边界、损坏值 fail-closed、load/save 命令 |
| `cargo test --lib window_mode::tests:: -- --test-threads=1` | 47 passed | 模式事务、ACK、revision、取消、恢复、shutdown；0.33 秒且无挂起 |
| `cargo test --lib generation::cutout::tests:: -- --test-threads=1` | 14 passed | 浅色/内洞 needs-review、原生 alpha、深色 acceptable 与质量报告 |
| `cargo test --lib generation::tasks::tests:: -- --test-threads=1` | 40 passed | raw/cutout/report/motion profile 持久化、事务、重试、路径与夹具隔离 |
| `cargo test --lib creation::finalization::tests:: -- --test-threads=1` | 46 passed | needs-review 定稿/恢复阻断、正常定稿、回滚和 junction 防护 |
| `cargo test --lib preferences::tests:: -- --test-threads=1` | 13 passed | preferences v2、尺寸、窗口模式、可见性和原子 round-trip |
| `cargo test --lib lib_tests:: -- --test-threads=1` | 31 passed | profile/calibration/window mode Tauri commands、注册、窗口限制和竞争 |

这些筛选有名称交集，不能把各行 passed 简单相加作为“独立测试总数”；整仓唯一总数以主闸门 674 为准。

### TypeScript

一次组合命令明确列出 27 个测试文件，得到 27 files passed、480 passed、0 failed、0 skipped，exit 0。覆盖：

- 设置页：profile editor/catalog/contract、calibration、window mode、display size、pet switch 和 `settings.ts` 装配。
- 运行时：switch、size、motion、geometry、stage、calibration、mode、fullscreen、slot、host、main。
- renderer：static PNG、animated image、Live2D 和 presentation controller。
- 生成复核：upload needs-review、candidate dynamic preview 和 finalizer。

该组合验证五项在同一前端代码基线上的契约兼容性，但仍是 Vitest 假 DOM/mock port，不是 Tauri WebView 真人操作。

## 设置页唯一生命周期与双入口核对

静态实体计数确认 `settings.html` 中 `view-list`、`list`、`window-mode-section`、`display-size-slider`、`calibration-section`、`pet-profile-editor` 各恰好一套。`settings.ts` 中也只有一个 `PetCalibrationCatalogCoordinator`、一个 `PetProfileEditor`、一个 `renderList()` 定义和一次 `wireSettingsPageLifecycle()` 装配；资料编辑与校准共同由同一宠物目录刷新，尺寸和窗口模式在同一设置页生命周期中初始化/销毁。

因此当前设置页没有第二套 current-pet lifecycle；生产构建仍只有桌宠 `index.html` 与设置 `settings.html` 两个入口。

## Windows 一键启动器静态验收

核对对象：根目录 `一键启动桌宠开发环境.cmd` 与 `scripts/start-desktop-pet-dev.ps1`。

### 静态结果

- PowerShell AST parse：0 errors。
- CMD：全 ASCII；`%~dp0scripts\start-desktop-pet-dev.ps1` 目标存在。
- CMD 参数契约：包含 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File`。
- CMD 传播 `%ERRORLEVEL%`，失败时提示并 pause，最终 `exit /b` 原退出码。
- PowerShell 以 `$PSScriptRoot` 定位仓库和 `apps/desktop`，检查 npm、cargo、本地 Tauri CLI、三份 Cubism runtime 文件。
- 唯一真实启动语句为 `npm run tauri -- dev`；没有第二个 Vite/`npm run dev`/`Start-Process` 启动路径。
- 静态源码不含显式下载器、URL、provider、secret 或生产 creation-content 操作；依赖不完整时会执行 `npm install`，该命令可能访问 npm registry。本轮仅以 fake npm 验证契约，未验证真实联网安装。

### 专用契约测试

运行 `npm test -- --run src/startup-scripts-contract.test.ts`：1 file passed、10 passed、0 failed、0 skipped，exit 0。测试使用系统临时目录和 fake `npm.cmd`/cargo command；`ValidateOnly` 用例明确不启动 Tauri，其他退出码用例调用的也是 fake npm，而不是真实应用。

本轮没有双击 CMD，没有不带 `-ValidateOnly` 运行真实 PowerShell 启动器，也没有执行真实 `npm run tauri -- dev`。

## Warning、skip 与取证偏差

### Warning

- `cargo test --all-targets` 的 lib target 报告 10 条 warning：1 个 unused import、8 个 unused/dead-code 项和 1 条 Windows linker stdout 信息。
- `cargo check --all-targets` 的 lib target 报告 9 条 unused/dead-code warning。
- lib test 及 focused Rust 命令重复报告 `src/generation/lk888.rs:53` 的 1 条 `unfulfilled_lint_expectations`。
- 这些 warning 没有导致编译或测试失败；本 Task 未修改对应代码，也没有运行 `cargo fix`。

### Skip

前端全量的 1 个跳过文件、4 个跳过测试全部来自 `src/runtime-live2d/cubism-runtime-lifecycle.test.ts` 的 `skipIf(!hasLocalCubismSdk)`：adapter 重建、独立/共享 WebGL shader context 释放，以及 context loss 清理。它们是当前环境覆盖缺口；本轮 focused Live2D renderer 校准测试实际执行并通过。

### 初次误筛选/无效静态查询

以下发现阶段命令没有计入验收证据，修正后均已重新执行：

1. 初次用 `rg --files src | rg '\\.test\\.ts$'` 时，Windows 路径分隔符与筛选表达式不匹配，exit 1、无结果；随后直接列出 `rg --files src` 并从真实清单构造 27 文件 focused 命令。
2. 两次 settings HTML/生命周期计数查询因 PowerShell 双引号和正则括号解析错误，exit 1；随后改为单引号或 `[regex]::Escape`，取得本文记录的唯一实体计数。

没有自动化产品测试失败，也没有为取得绿色结果而修改代码、删除测试数据或放宽断言。

## 提交链、差异范围与受保护主检出

- `origin/main...d807ef1`：84 个提交，115 files changed，28,946 insertions、839 deletions。远端 `origin/main` 落后于本机 `main`，因此该范围还包含五项之前的一键启动器和迁移加固。
- 从本机第一轮计划基线 `34479f1` 到 `d807ef1`：69 个提交，103 files changed，24,512 insertions、833 deletions；提交类型为 docs 6、feat 20、fix 34、refactor 3、test 6。
- 五份分项验收文档，以及 cutout/tasks/finalization、profile/repository/migration、preferences/size、calibration、window_mode 和对应前端设置/运行时文件均存在于提交范围并已跟踪。
- 受保护主检出 `D:\petBaby\desktop-pet` 保持在 `main@34479f1`，本轮未在其中写文件。其 status 显示 `Cargo.toml` 为 M，但 working blob 与 main HEAD blob 完全相同，没有内容 diff；未跟踪的 `docs/新对话开发交接摘要.md` 不存在于 feature HEAD。
- `.superpowers/sdd/五项统一验收报告.md` 当前不存在，并由 `.gitignore` 第 8 行 `.superpowers/` 覆盖；本文没有创建或提交该 ignored 报告。

## 第一次真人测试清单（本轮未执行）

以下步骤必须由真人在可回滚的 Windows 测试会话中按顺序执行。每一步的“实际/证据”栏当前均留空，状态统一为 **未执行**。

### 0. 准备、备份与总停止条件

准备：

1. 记录操作者、开始时间、应用提交、Windows build、WebView2、Shell/第三方桌面软件、显示器拓扑和各屏 DPI。
2. 完全退出生产桌宠；备份真实用户 app-data、数据库和 preferences，但之后不要把备份或真实目录用作测试目录。
3. 创建一次性测试 app-data/测试偏好和测试专用宠物；准备脱敏的浅色内洞 PNG、原生 alpha PNG、深色 PNG、static PNG 宠物、生成动画宠物和 Live2D 测试宠物。禁止使用真实用户图片、真实生产 provider、生产 API key。
4. 准备录屏、截图、脱敏日志、Task Manager 采样表；Explorer 重启和故障注入只在明确可恢复的测试会话中进行。

总停止条件：发现测试进程指向真实用户 app-data/数据库/preferences；需要调用真实 provider；出现数据丢失、窗口无法恢复、Explorer/Shell 异常、持续高 CPU/GPU、应用崩溃或无法安全退出。命中任一条件立即停止，保留现场证据，不继续下一项。

记录：操作者 ______；开始时间 ______；commit ______；测试 app-data ______；备份位置 ______；环境/证据目录 ______；准备结果 ______。

### 1. 抠图安全

操作与预期：

1. 上传浅色/内洞 fixture，确认原图和抠图同时显示、中文原因可见、完成按钮不可用，只能重新生成或放弃。
2. 通过测试诊断入口尝试直接定稿同一 needs-review 候选，预期返回质量错误，session/候选/宠物状态不变且 runtime manifest 不产生。
3. 分别上传原生 alpha 和深色 fixture；预期原生透明边缘肉眼保持，深色 acceptable 候选可正常定稿。
4. 在 needs-review 可恢复状态关闭并重启测试应用；预期恢复后仍被质量闸门阻断。
5. 只在一次性沙箱布置 generation job junction；预期安全报错且外部 sentinel 不变。

本项停止条件：真实 provider 被调用；任何 needs-review 候选可完成；外部 sentinel 改变；manifest 意外生成；真实用户素材或目录被访问。

记录：状态 未执行；实际 ______；候选/session ID ______；截图/日志 ______；异常 ______。

### 2. 宠物资料编辑

操作与预期：

1. 编辑一只用户宠物的名称、性别、出生日期；预期保存后同一设置页列表立即显示 canonical 名称。
2. 完整退出并重启测试应用；预期资料仍保留。
3. 保存合法闰日，再输入未来日期；预期闰日成功、未来日期被拒且旧值不变。
4. 确认内置宠物没有编辑按钮；注入保存失败，预期输入不丢失，Escape/取消和焦点恢复可用。
5. 从两个设置窗口对同一宠物并发保存与删除；预期只有一个 mutation 进入，另一方有明确反馈，不出现半更新。

本项停止条件：写入真实数据库；内置宠物可编辑；失败后字段被清空；两操作同时成功；重启后资料丢失或数据库不一致。

记录：状态 未执行；pet ID ______；重启前/后值 ______；并发结果 ______；截图/日志 ______。

### 3. 桌宠显示尺寸

操作与预期：

1. 在主屏 100% 和副屏 125%/150%（含负坐标副屏）逐一切换 50/75/100/125/150%；预期脚底/底部中心近似不动，窗口、画面和点击区域一致。
2. 分别在主副屏拖动后再缩放；预期显示器归属、逻辑位置、拖动和 hit region 正确。
3. 手动隐藏及全屏 suppression 时改变尺寸；预期不自动 show/focus，恢复显示后使用新尺寸。
4. 各 DPI/位置保存后完整退出重启；预期 `displayScale` 和逻辑 geometry 正确恢复，无二次 DPI 放大。
5. 在测试注入点制造 hit-region 刷新失败；预期窗口、renderer、hit region 和偏好精确回滚，隐藏窗口不被显示。

本项停止条件：窗口跑出可恢复屏幕范围；透明区阻挡输入；隐藏态被强制显示；偏好或 geometry 分裂；回滚失败。

记录：状态 未执行；显示器/DPI ______；五档矩形与锚点 ______；重启结果 ______；截图/日志 ______。

### 4. 真实校准

操作与预期：

1. 对 static PNG、生成动画图、Live2D 三类宠物分别将呼吸设为 0 和 5，肉眼确认幅度差异及脚底稳定。
2. 分别将眨眼设为 0.5x 和 2x，每组连续计时至少 30 秒并记录次数；Live2D 缺眼参数模型只应有轻微 140ms canvas fallback。
3. 将反馈设为 0 和 1，分别单击/双击；预期视觉强度变化但行为选择不变。
4. 验证预览后取消、设置窗口关闭恢复 saved；保存后重启保留；切换至少两只宠物时参数按宠物隔离；保存中关闭和关闭失败重试不分裂。
5. 隐藏宠物 60 秒后恢复；预期无突发补帧。用 Task Manager 记录隐藏前/中/后 CPU、内存，确认计时器未造成显著上升。

本项停止条件：任一 renderer 崩溃/持续闪烁；隐藏态持续耗用明显上升；取消不能恢复；保存/切宠串值；需要生产 provider 才能继续。

记录：状态 未执行；三类 pet/renderer ID ______；30 秒眨眼计数 ______；CPU/内存 ______；截图/日志 ______。

### 5. Windows 双窗口模式

操作与预期：

1. companion：确认不抢焦点、保持置顶；宠物点击/透明区穿透/拖动正确；全屏隐藏恢复不覆盖 manual hide。
2. 标准 Explorer 下切 desktop：记录 canonical snapshot、真实 parent class/Z 序/rect/strategy；验证 Win+D、切应用、前台全屏时仍处桌面层，往返无双窗口或任务栏残影。
3. 在 desktop 下锁屏/解锁、睡眠/唤醒和重启 Explorer；预期自动隐藏、重挂和恢复。用测试故障注入连续五次失败，预期自动回 companion。
4. 在双屏和 100%/125%/150% DPI 下以非 100% 桌宠尺寸跨屏拖动并切模式；覆盖 hidden 时切换、切换中退出和重启恢复。
5. 分别观察真实 WorkerW 成功或明确失败、真实 BottomFallback 非阻塞提示；注入两策略失败，预期 companion 的位置、尺寸、置顶、hit region 和可见性完整恢复。

本项停止条件：Explorer/Shell 无法恢复；宠物失联或无法点击/穿透；模式 snapshot 与物理事实持续矛盾；窗口位置/尺寸/置顶不能回滚；测试影响真实用户偏好。

记录：状态 未执行；每步 desired/actual/strategy/suppressions ______；parent/Z序/rect ______；截图/日志 ______；结束时间 ______。

## 最终判定

- Fresh 串行整仓自动化门禁：**通过**。
- 五项跨功能 focused 自动化组合：**通过**。
- 设置页单一 current-pet lifecycle 与两个生产 HTML：**通过静态实体和构建核对**。
- Windows 一键启动脚本：**静态解析和临时 fake-command 契约通过；真实启动未执行**。
- 真实 Windows/Tauri 第一次真人测试：**未执行**。
- 发布结论：真人清单全部完成并保存证据前，不可声明五项整体验收或发布验收完成。
