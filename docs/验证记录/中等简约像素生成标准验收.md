# 中等简约像素生成标准验收

验收日期：2026-08-21

## 结论

三只真实宠物均通过正式 lk888 生产链生成，默认风格为 `pixel-style-v2-animation-ready`。三张母版均为 `1024x1024 RGBA PNG`，逻辑网格为 160，可见颜色为 24；`route=pixel-v1`、`renderer=animated-image-v1` 保持不变，Live2D 未进入新生成路径。

动作侧已生成呼吸、眨眼和摇尾三类证据。9 个动作全部循环闭合，地面锚点变化为 0，最大可见颜色数为 24。摇尾已从叠加像素改为“修复底板 -> 单一尾巴层旋转 -> 固定尾根接缝”，旧尾会离开原位置，不再产生双尾或加粗。

## 真实生成记录

| 宠物 | 输入 SHA-256 | lk888 task ID | 输出 SHA-256 | 保护色 |
|---|---|---:|---|---:|
| 长毛黑白猫 | `72f7c0b5f0b6658030d891956b8b017d32d260dc1fe79de8dca2d4a7ea9c63f6` | `111701986` | `7da1db32d80ee05c22dbdfafcb2552908ead3ba16690e2deb1d320bdbdbe4b3f` | 4 |
| 圆脸狸花猫 | `db1e026fddc8574099ee28f1308b763e5552b70482a431fc8391619b4842d243` | `111706583` | `99d63fb51c669caf72bb4ab1a9a31675cdccd9b21a40c1098185093f254aeb9f` | 3 |
| 修长黑猫 | `db825570b7eb2deec0bde907089a551bd707e5698c0db7c0af532a169de22c4a` | `111711389` | `ebfb0fa508d4824a8b1dcad17bf9bda4fc354c1f4456ff4af29e4eb033b1f1a2` | 0 |

三项任务均在第 1 次远程尝试成功，无失败重试。任务状态账本为 `output/中等简约像素标准验收-2026-08-21/生成任务.json`。

## v2 机械审计

三份母版审计均满足：

- `schemaVersion=2`
- `styleProfileId=pixel-style-v2-animation-ready`
- `provider=lk888`、`providerModel=gpt-image-2`
- `width=1024`、`height=1024`
- `logicalGridSize=160`
- `paletteColorLimit=24`、`visibleColorCount=24`
- `quantizeMethod=maxcoverage`、`dither=none`
- `protectedAccentSlots=4`
- `downsample=box`、`upsample=nearest`
- `partialAlphaPixels=0`
- 最大连通主体占比为 1.0，四边留白均满足风格合同。

风格档案 SHA-256 为 `2a48f382d0d0a579010ffae2ce90a7693d364a0cf64e5463e0ce7bf0291ee4ab`；认可参考图 SHA-256 为 `75171817d27aee72439f373317ad0a3f43bdb2f8a76b0f8c55e24c306ac46c85`。

## 动作结果

| 宠物 | 呼吸峰值变化像素 | 眨眼峰值变化像素 | 摇尾峰值变化像素 | 结果 |
|---|---:|---:|---:|---|
| 长毛黑白猫 | 3833 | 104 | 732 | 通过 |
| 圆脸狸花猫 | 5744 | 105 | 502 | 通过 |
| 修长黑猫 | 1963 | 122 | 241 | 通过 |

每只宠物的呼吸为 8 帧、眨眼为 5 帧、摇尾为 9 帧。动作审计位于 `output/中等简约像素标准验收-2026-08-21/动作审计.json`；GIF、逐帧 PNG 和单宠物动作总览位于各宠物的 `动作/` 及 `动作总览.png`。

尾巴非零角度帧额外验证：旧轮廓均有像素移除，新轮廓均有像素增加，可见面积差不超过 64 个逻辑像素；三只尾根保持连接，未观察到透明切口、完整旧尾残留或双尾叠影。

## 人工视觉结论

- 长毛黑白猫：白鼻梁、绿色眼睛、黑白大色块、胸毛和深色大尾巴清晰保留；摇尾两侧峰值仍为单一尾巴。
- 圆脸狸花猫：圆脸、金眼、额头 M 纹、主脸纹、结实体型和环纹尾可读；旋转后环纹连续，未牵动身体轮廓。
- 修长黑猫：修长体型、高尖耳、琥珀眼和长尾可读，暗部层次未糊成整块黑色；完整闭眼已无琥珀色残留。
- 眨眼未改变眼距或主要脸纹；呼吸未移动地面锚点；摇尾未移动脚部或主体锚点。
- 当前呼吸使用保守的上身微幅起伏，机械和视觉可用，但仍偏整体抬升；局部胸腔/腹部形变可作为后续质量优化，不阻塞本次中等简约标准切换。

## 验证命令

```powershell
python -m pytest scripts/test_中等简约动作.py -q
python scripts/验证中等简约动作就绪.py "output/中等简约像素标准验收-2026-08-21"
python -m py_compile scripts/中等简约尾巴.py scripts/中等简约动作.py scripts/验证中等简约动作就绪.py
```

结果：动作测试 `10 passed`；真实动作生成输出 `MOTION READY pets=3`；Python 编译退出码 0；相关 Python no-excuse 审计零违规。

## 独立视觉 QA

尾巴分层与透明孔修复完成后，使用两路全新、无旧评审上下文的只读审查重新验收当前产物。两路均返回 `PASS`、置信度 `HIGH`、阻塞项为空，且各自明确覆盖：

- 66/66 张 `1024x1024 RGBA` 动作 PNG；
- 9/9 个循环 GIF；
- 4/4 张动作总览；
- 3/3 张完整 22 帧联系表。

两路共同确认：三只宠物身份特征保持清晰；眨眼无眼色残留；呼吸不移动脚部和地面锚点；摇尾始终只有一条连续尾巴，未出现双尾、尾根断裂、透明孔或身体随尾移动；中文宠物名与动作标签无裁切。呼吸 GIF 会把重复的 8 张源 PNG 合并为 5 个编码帧，但总时长仍为 1440 毫秒，顺序保持对称闭环。

## 产物索引

- 静态总览：`output/中等简约像素标准验收-2026-08-21/三宠物v2母版总览.png`
- 动作总览：`output/中等简约像素标准验收-2026-08-21/三宠物动作总览.png`
- 生成任务：`output/中等简约像素标准验收-2026-08-21/生成任务.json`
- 动作审计：`output/中等简约像素标准验收-2026-08-21/动作审计.json`
- 动作标注：`output/中等简约像素标准验收-2026-08-21/annotations/`

## 完成定义复核

- 新生成默认值为 `pixel-style-v2-animation-ready`。
- Python、Rust、TypeScript 对 v1/v2 profile 和 audit 的双读已在 Task 1 至 Task 7 验证。
- 历史 `pixel-style-v1` 资产、哈希和读取路径未重写。
- 三只真实宠物的静态母版与三类动作证据已生成。
- 两路独立视觉 QA 已全量覆盖 66 PNG、9 GIF、4 总览和 3 联系表并通过。
- Live2D 继续冻结，未重新进入生成或动作路径。
