# 标准猫分层源验收

## 结论

- 任务：Task 2，标准猫可绑定分层源。
- 状态：`DONE`。
- 画布：`2048 × 2048 RGBA`。
- 图层合同：`LayerContractV1`，20 个必需图层且名称唯一。
- 正式 KRA：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\标准猫-A-v1.kra`。
- 正式 KRA SHA-256：`6D5C43DF67327F6C52E2260E6AC5626F8202DAECC09CF54225594AA00DB4F62B`。

Task 2 已满足进入角色包阶段的门槛。独立的 `tail`、`bodyUnderTail`、`tailReserve` 图层可支持后续“身体藏到屏幕边缘外，只保留动态尾巴”的运行时要求。

## TDD 与结构门槛

### RED

使用 `scripts/夹具/标准猫分层缺失夹具.json` 运行角色包校验器，退出码为 `1`，并明确报告包括 `bodyUnderTail` 在内的缺层及合同字段错误，证明缺失分层不会被误放行。

### GREEN

对正式 `图层合同.json` 运行 `scripts/验证猫咪角色包.ps1`，退出码为 `0`：

```text
LayerContractV1 valid: D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\图层合同.json
```

KRA 容器校验确认：

- ZIP 与 `maindoc.xml` 可读；
- 20/20 必需图层均存在且名称唯一；
- `tailReserve`、`occlusionReserve`、`bodyUnderTail` 默认隐藏；
- 合同中的 `kraLayerPath` 与实际图层一致。

## 像素门槛

`scripts/测试标准猫遮挡补画.py` 对完整底图、局部 reserve、隐藏状态和中性重组执行像素验证，结果为通过：

| 指标 | 结果 |
| --- | ---: |
| 特征对齐内点 | 49 |
| 对齐中位误差 | 2.391 px |
| 中性重组差异值数量 | 0 |
| `bodyUnderTail` 非透明像素 | 114312 |
| `tailReserve` 非透明像素 | 95428 |
| `occlusionReserve` 非透明像素 | 672775 |

2026-08-13 的角色包实机回归又发现母版内部遗留的小型透明孔。生成脚本现会先修复不与画布边缘连通、面积不超过 64 像素的透明孔；修复后像素门槛重新通过，正式 KRA 已由修复后的 ORA 重建。旧正式 KRA 备份为 `D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\备份\标准猫-A-v1-透明孔修复前-20260813-235410.kra`。

测试同时约束：reserve 不能是透明占位；补画像素不能越出母版主体；耳后补画不能污染眼部；隐藏尾巴时尾巴像素必须消失；补画边界必须与母版衔接；中性重组必须与母版 RGBA 零差异。

## 逐层视觉证据

证据目录：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\工作稿\验收证据`。

| 证据 | 验收结果 |
| --- | --- |
| `01-中性重组.png` | 与批准母版一致 |
| `03-隐藏双眼.png` | 眼部完整移除，无透明破洞或邻层污染 |
| `04-隐藏双耳.png` | 耳后头部连续，头顶轮廓自然 |
| `05-隐藏口鼻.png` | 口鼻下脸部已补全，无透明破洞 |
| `06-隐藏前肢.png` | 胸腹底图连续，无拉伸条纹或硬接缝 |
| `07-隐藏尾巴.png` | 尾巴完整移除，尾根后的身体连续，无硬切口 |

眼、口鼻的“完全隐藏”只用于验证底图完整性；运行时眨眼与表情将通过形变和参数驱动，不把该验证姿态当作最终动作姿态。

## 可复现产物与回退

- 工作稿生成：`scripts/生成标准猫完整底图工作稿.py`。
- 像素回归：`scripts/测试标准猫遮挡补画.py`。
- 结构合同校验：`scripts/验证猫咪角色包.ps1`。
- 补画参考：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\工作稿\标准猫-A-v1-补画参考.png`。
- 上一候选备份：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\备份\标准猫-A-v1-候选1-20260813-2038.kra`。
- 原始结构基线：`D:\PetBabyAssets\cat-a-live2d-v1\标准猫\02-分层源\备份\标准猫-A-v1-结构基线-20260813-1739.kra`。

失败的早期自动补画实验没有覆盖正式 KRA，相关一次性脚本已从仓库清理，避免被后续任务误用。
