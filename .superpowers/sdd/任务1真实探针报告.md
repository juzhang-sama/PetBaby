# Task 1 真实 Cubism 探针报告

## RED

先增加适配器驱动、alpha 采样与初始化失败诊断测试；首次运行因运行时 API 未接入而失败，随后发现 Vitest 默认 node 环境无 DOM，改为注入 canvas/root 工厂后重新得到功能失败（`window is not defined`），再补齐实现。

## GREEN

实现官方 Cubism Framework/Core 动态加载、Wanko model3/moc3/纹理加载、渲染器初始化、持续 update/draw、alpha 采样与 adapter-error 诊断；`npm test -- src/runtime-live2d/probe.test.ts`：8 tests passed。

## 验证命令与结果

- `$env:CUBISM_SDK_ROOT='D:\Live2D\CubismSdkForWeb-5-r.5'; npm run prepare:cubism`：成功复制 Core/Framework 到忽略目录。
- `npm test -- src/runtime-live2d/probe.test.ts`：通过（8/8）。
- `npm run typecheck`：官方 SDK 源码在 strict/noUncheckedIndexedAccess 下产生既有类型错误，未能通过。
- `npm run build` 与 Tauri 真实窗口探针：本次未完成，需在具备 GPU/WebView 环境中继续。

## 提交

`e0e08ba feat: wire official Cubism adapter into live2d probe`

## 顾虑

- SDK 与 Wanko 资产均只复制到 `.vendor`/运行时目录，未纳入 Git。
- Core 的全局 `Live2DCubismCore` 注入及生产打包路径仍需在真实 Tauri 启动中确认。
- 纹理加载依赖浏览器 `Image.decode()`；无 DOM/图形环境时会返回可诊断的 `adapter-error`。
